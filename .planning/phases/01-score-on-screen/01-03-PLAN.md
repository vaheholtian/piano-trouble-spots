---
phase: 01-score-on-screen
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - fixtures/README.md
autonomous: false
requirements: [SCORE-01, SCORE-03]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "npm test runs the whole suite green (19 tests) and every run-path gate holds: index.html has zero module-type script tags, exactly one pinned OSMD CDN URL, three local classic script tags, and no ES-module syntax in src/ (D-13)"
    - "fixtures/README.md lists, for each of the five ladder rungs, the file, time signature, and bar-by-bar contents per staff with the expected note count, so the user can compare the screen against it at the piano (D-04)"
    - "The user sat at the FP-60X with the laptop, opened index.html by double-click, and loaded each of the five ladder files; each rendered the right notes on the right staff, the status showed N/N noteheads mapped (5, 5, 10, 8, 41), and the count survived a window resize (ROADMAP success criteria 3 and 4, D-11, D-16, VRFY-01)"
    - "The user opened fixtures/05-yanni-4-measures.mxl and a real MuseScore .mxl export from their Scores folder; both rendered (SCORE-01 .mxl in real Chrome)"
  artifacts:
    - path: "fixtures/README.md"
      provides: "Human-readable ladder manifest for the at-the-piano check"
      contains: "05-yanni-4-measures"
  key_links:
    - from: "fixtures/README.md"
      to: "fixtures/0*.musicxml"
      via: "one section per rung naming the file and its expected contents"
      pattern: "01-right-hand"
  prohibitions:
    - requirement_id: SCORE-01
      category: values
      statement: "MUST NOT count this phase done on test output alone: the at-the-piano checkpoint below is blocking-human and is never auto-approved"
    - requirement_id: SCORE-03
      category: transparency
      statement: "MUST NOT silently drop, merge, or invent score notes: any N/N mismatch the user sees at the piano is reported as a phase gap, not explained away"
---

## Phase Goal

**As a** pianist practising alone, **I want to** open a tiny MusicXML exercise and see it as real notation, **so that** every note is modelled precisely enough that alignment can be built on it.

<objective>
Close the phase the way the project constraint demands: run every automated gate once more, hand the user a plain manifest of what each ladder file should look like, and then stop for the real check at the Roland FP-60X — every rung opened in Chrome by double-click, right notes on the right staff, every notehead mapped, surviving a resize, plus the .mxl path.

Purpose: the prototype was never played once. ROADMAP success criterion 4 ("User sat at the FP-60X with the laptop and loaded each ladder file") is the only proof that SCORE-01 is met in the environment that will actually be used.

Output: fixtures/README.md, a green full suite, and the user's approval (or a list of what was wrong) recorded in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-score-on-screen/01-CONTEXT.md
@.planning/phases/01-score-on-screen/01-01-SUMMARY.md
@.planning/phases/01-score-on-screen/01-02-SUMMARY.md
@index.html
@fixtures/01-right-hand.musicxml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — full suite, run-path gates, and the ladder manifest for the piano session</name>
  <files>fixtures/README.md</files>
  <read_first>
    - fixtures/01-right-hand.musicxml, fixtures/02-left-hand.musicxml, fixtures/03-both-hands.musicxml, fixtures/04-chords.musicxml, fixtures/05-yanni-4-measures.musicxml (the exact contents the manifest describes)
    - .planning/phases/01-score-on-screen/01-02-PLAN.md ("Expected rows" section: the 41 rung 5 rows to summarise per bar)
    - .planning/phases/01-score-on-screen/01-02-SUMMARY.md (whether the rung 5 trim fallback ran)
    - index.html and src/score-renderer.js (what the status line and table say, so the manifest quotes the exact expected status text)
  </read_first>
  <action>
1. Write `fixtures/README.md` for a human at the piano, plain prose and small tables, no JSON. One section per rung with: file name; time signature; what appears on the treble staff and on the bass staff, bar by bar (rung 1: C4 D4 E4 F4 G4 quarters treble, whole-bar rest bass; rung 2: rest treble, C3 D3 E3 F3 G3 bass; rung 3: both; rung 4: C-E-G then F-A-C half-note chords treble over C3 then F3 half notes bass; rung 5: per bar, right-hand chords and left-hand eighths exactly as listed in plan 02's table, in words such as "bar 2 right hand: F#5+D6 quarter, E5+C#6 dotted quarter, D5+B5 eighth; left hand: G2 D3 G3 A3 eighths, B3 quarter"); the exact status line the app must show, for example `01-right-hand.musicxml: 1 bars, 5 notes, 5/5 noteheads mapped` (counts: 5, 5, 10, 8, 41; bars: 1, 1, 1, 1, 4); and one line on how to rebuild rung 5 (`node scripts/build-rung5.cjs`, source file, whether trimming is currently needed). Add a short "How to check at the piano" list: double-click index.html, open each file, compare with this page, resize the window, check the status again, then open the .mxl.

2. Run the gates and fix anything red before committing: `npm test` (all 19 green), `node --check src/score-model.js src/inspect-table.js src/score-renderer.js` (one at a time), the index.html gates in the verify block below, and `git ls-files --error-unmatch` on all seven fixture files (five rungs, the .mxl, the tie fixture). Commit `fixtures/README.md`.
  </action>
  <verify>
    <automated>npm test && test "$(grep -v '<!--' index.html | grep -c 'type="module"')" -eq 0 && test "$(grep -c 'opensheetmusicdisplay@2.1.2/build/opensheetmusicdisplay.min.js' index.html)" -eq 1 && test "$(grep -o 'src="src/[a-z-]*\.js"' index.html | wc -l)" -eq 3 && ! grep -qE '^\s*(import|export) ' src/score-model.js src/inspect-table.js src/score-renderer.js && git ls-files --error-unmatch fixtures/01-right-hand.musicxml fixtures/02-left-hand.musicxml fixtures/03-both-hands.musicxml fixtures/04-chords.musicxml fixtures/05-yanni-4-measures.musicxml fixtures/05-yanni-4-measures.mxl test/fixtures/tie.musicxml >/dev/null && test -f fixtures/README.md</automated>
    <fails_when>non-zero exit from any link: "# fail 0" absent from the npm test summary, a module-type script tag present, the CDN URL count not exactly 1, local script tag count not 3, ES-module syntax found in any src file, git reporting "error: pathspec" for any of the seven fixture files, or fixtures/README.md missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# pass 19` and `# fail 0`
    - `grep -c '01-right-hand' fixtures/README.md`, `grep -c '05-yanni-4-measures' fixtures/README.md` each print at least 1; `grep -c '41/41 noteheads mapped' fixtures/README.md` prints at least 1; `grep -c 'build-rung5' fixtures/README.md` prints at least 1
    - `git ls-files --error-unmatch fixtures/01-right-hand.musicxml fixtures/02-left-hand.musicxml fixtures/03-both-hands.musicxml fixtures/04-chords.musicxml fixtures/05-yanni-4-measures.musicxml fixtures/05-yanni-4-measures.mxl test/fixtures/tie.musicxml` exits 0 (all seven fixtures tracked)
    - `grep -v '<!--' index.html | grep -c 'type="module"'` prints `0`
  </acceptance_criteria>
  <done>All automated gates are green in one run and fixtures/README.md tells the user exactly what each rung must look like and what the status line must say.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: At the FP-60X — open every ladder file in Chrome and confirm notation, mapped counts, resize, and .mxl</name>
  <what-built>Phase 1 slice: index.html opens by double-click in Chrome (no server, no build), loads OpenSheetMusicDisplay 2.1.2 from jsDelivr (internet needed once, cached after), renders any .musicxml/.xml/.mxl, extracts the plain score model, lists every note in the inspect table, and reports N/N noteheads mapped. Five ladder files live in fixtures/ with fixtures/README.md describing each; rung 5 also exists as fixtures/05-yanni-4-measures.mxl. All 19 node:test fixture tests pass.</what-built>
  <how-to-verify>
Sit at the FP-60X with the laptop (the piano is not connected to the app in this phase; it is there so you can play each file and check the written notes by ear as well as by eye). Keep fixtures/README.md open.

1. Double-click index.html. If the status says the library did not load, connect to the internet once and reload.
2. For each of the five files in order — fixtures/01-right-hand.musicxml, 02-left-hand.musicxml, 03-both-hands.musicxml, 04-chords.musicxml, 05-yanni-4-measures.musicxml — click "Open MusicXML", choose the file, and check three things against fixtures/README.md:
   a. The notation shows the right notes on the right staff (treble vs bass), the right rhythm, and the right time signature. Play it on the piano if anything looks doubtful.
   b. The status line reads exactly the expected text, ending in 5/5, 5/5, 10/10, 8/8 and 41/41 noteheads mapped; no row in the table says MISSING.
   c. The table's bar, staff, voice, onset, duration, pitch and MIDI columns match the sheet (spot-check a few rows on rungs 1-4; on rung 5 check bar 2 and bar 3 row by row).
3. With rung 5 open, drag the window to about half width, then wide again: the notation reflows, the status still says 41/41, the table is unchanged.
4. Open fixtures/05-yanni-4-measures.mxl: same notation, same 41/41.
5. Open a real MuseScore export: C:\Users\vaheh\OneDrive\Documents\MuseScore4\Scores\Yanni - Reflections of Passion-Piano.mxl. It must render without an error toast; note the mapped count it reports (model correctness for a full piece is not a Phase 1 requirement per D-02, but "renders and reports" is).
6. Choose a file that is not MusicXML (for example prototype/test-scale.mid): a red toast "Could not open ..." appears, the notation and table clear, and opening rung 1 again works.
  </how-to-verify>
  <resume-signal>Type "approved" if every rung passed steps 2a-2c, the resize kept 41/41, and both .mxl files rendered. Otherwise list the rung, the step, and what you saw (for example "rung 4: chord shows E4 on bass staff" or "rung 5: 40/41 mapped, row 17 MISSING").</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `fixtures/README.md` — the ladder manifest (no code symbols)
- The phase's completion evidence: the user's checkpoint response recorded in `01-03-SUMMARY.md`

<verification>
- `npm test` green (19 tests) and every index.html / src run-path gate holds in one command chain
- The blocking-human checkpoint is answered "approved", or its issues are recorded as gaps for `/gsd-plan-phase 1 --gaps`
</verification>

<success_criteria>
- ROADMAP Phase 1 success criteria 1-4 are all true: the five files render in Chrome as real notation, every note is modelled and inspectable, the ladder is in the repo with tests, and the user tried every file at the FP-60X
- VRFY-01 is honoured for Phase 1: no phase-complete without the piano session
</success_criteria>

<output>
Create `.planning/phases/01-score-on-screen/01-03-SUMMARY.md` when done, quoting the user's checkpoint response verbatim (which rungs passed, what was seen for the real .mxl piece, and any mismatch).
</output>
