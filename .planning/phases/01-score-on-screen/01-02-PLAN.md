---
phase: 01-score-on-screen
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - fixtures/02-left-hand.musicxml
  - fixtures/03-both-hands.musicxml
  - fixtures/04-chords.musicxml
  - fixtures/05-yanni-4-measures.musicxml
  - fixtures/05-yanni-4-measures.mxl
  - scripts/build-rung5.cjs
  - test/fixtures/tie.musicxml
  - test/score-model.test.cjs
  - src/score-model.js
autonomous: true
requirements: [SCORE-01, SCORE-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Rungs 2, 3 and 4 each have a node:test asserting the exact expected note list (bar, onset, staff, voice, pitch, MIDI, duration) through the real OSMD parser: rung 2 is C3 D3 E3 F3 G3 on staff 2 voice 5; rung 3 is both hands at the same five onsets; rung 4 is two three-note chords on staff 1 over C3 and F3 half notes on staff 2 (D-04, D-05, D-07)"
    - "Rung 5 (Yanni, 4 bars of 3/4) is produced by scripts/build-rung5.cjs from the user's .mscz with the MuseScore 4 CLI, has exactly 4 measures and 41 notes, and its test asserts all 41 rows listed in this plan (D-06, D-07)"
    - "fixtures/05-yanni-4-measures.mxl is a real MuseScore-exported compressed file whose extracted model deep-equals the .musicxml model, and rung 1 loaded as a File object equals rung 1 loaded as a string (SCORE-01 .mxl and browser handoff)"
    - "A tie fixture with 5 written noteheads yields 3 model notes: C4 at bar 1 beat 0 lasting 4 beats with tiedNoteCount 2, E4 at bar 2 beat 0 lasting 2, and G4 at bar 2 beat 2 lasting 6 beats across the barline with tiedNoteCount 2 (D-03; edge SCORE-03 adjacency: ties merge)"
    - "Notes at exactly equal onsets stay separate model notes with distinct ids: the three notes of a rung 4 chord and the two hands of rung 3 at beat 0 (edge SCORE-03 adjacency: chords and hands do not merge)"
    - "Onsets at the first beat (0/1) and the last written position of a bar (4/1 in 5/4 rung 1, 5/2 in 3/4 rung 5) are exact reduced rationals and no untied note extends past its bar length (edge SCORE-03 boundary)"
    - "Model note order is canonical and stable: bar, then onset by cross-multiplication, then staff, then voice, then MIDI; extracting rung 5 twice deep-equals (edge SCORE-03 ordering)"
    - "A rest-only file yields notes [] and still lists its measure; extraction of a chord containing the same pitch twice throws 'Duplicate note id' (edge SCORE-03 empty; validation throws, not silent)"
    - "toQuarterBeats is exact for whole 4/1, half 2/1, quarter 1/1, eighth 1/2, dotted quarter 3/2, dotted eighth 3/4 and reduces MuseScore's unreduced 12/8 to 3/2; addRationals and compareRationals never use floats (edge SCORE-03 precision)"
    - "ScoreModel.extract runs with the DOM globals deleted from the Node process, proving the model module has no DOM dependency (D-08)"
  artifacts:
    - path: "fixtures/02-left-hand.musicxml"
      provides: "Ladder rung 2: left hand C3 D3 E3 F3 G3, one 5/4 bar"
      contains: "<voice>5</voice>"
    - path: "fixtures/03-both-hands.musicxml"
      provides: "Ladder rung 3: both hands C D E F G together, one 5/4 bar"
      contains: "<staff>2</staff>"
    - path: "fixtures/04-chords.musicxml"
      provides: "Ladder rung 4: two right-hand triads over two bass half notes, one 4/4 bar"
      contains: "<chord/>"
    - path: "fixtures/05-yanni-4-measures.musicxml"
      provides: "Ladder rung 5: the user's four real bars, MuseScore CLI export, trimmed to 4 measures"
      contains: "<beats>3</beats>"
    - path: "fixtures/05-yanni-4-measures.mxl"
      provides: "Rung 5 as a MuseScore-exported compressed MusicXML container"
    - path: "scripts/build-rung5.cjs"
      provides: "Reproducible rung 5 build: MuseScore CLI export, trailing-empty-measure trim, .mxl export"
      contains: "MuseScore4.exe"
    - path: "test/fixtures/tie.musicxml"
      provides: "Synthetic tie fixture: tie within a bar and a tie across a barline"
      contains: "<tied type=\"stop\"/>"
    - path: "test/score-model.test.cjs"
      provides: "Rung 2-5, .mxl, tie, purity, rational, ordering, empty and duplicate-id tests"
      contains: "05-yanni-4-measures.mxl"
  key_links:
    - from: "scripts/build-rung5.cjs"
      to: "fixtures/05-yanni-4-measures.musicxml"
      via: "child_process.execFileSync of MuseScore4.exe -f -o <out> <source>"
      pattern: "execFileSync"
    - from: "test/score-model.test.cjs"
      to: "fixtures/05-yanni-4-measures.mxl"
      via: "new File([bytes], name) passed to osmd.load (binary path, Pitfall 4)"
      pattern: "new File\\("
    - from: "test/score-model.test.cjs"
      to: "src/score-model.js"
      via: "globalThis.ScoreModel.extract on every fixture sheet"
      pattern: "ScoreModel"
  prohibitions:
    - requirement_id: SCORE-03
      category: transparency
      statement: "MUST NOT silently drop, merge, or invent score notes: a fixture whose parsed note count differs from the written noteheads is a test failure or a documented tie merge, never a quiet adjustment of the expected list to match the parser"
---

## Phase Goal

**As a** pianist practising alone, **I want to** open a tiny MusicXML exercise and see it as real notation, **so that** every note is modelled precisely enough that alignment can be built on it.

<objective>
Expand the proven rung-1 slice to the whole verification ladder and pin the score-model contract with fixture tests: rungs 2 to 4 hand-authored, rung 5 built reproducibly from the user's MuseScore file (uncompressed and compressed), a synthetic tie fixture, and the edge tests (ordering, boundary, adjacency, empty, precision, purity) that make the one-way model shape trustworthy before Phase 2 persists it.

Purpose: D-07 makes the ladder files the fixtures for the model tests; every later phase's alignment accuracy is judged on these exact files, so every note in them is asserted here by bar, onset, staff, voice, pitch, MIDI and duration.

Output: fixtures/02..05 (.musicxml) plus fixtures/05 (.mxl), scripts/build-rung5.cjs, test/fixtures/tie.musicxml, extended test/score-model.test.cjs, and any fix to src/score-model.js the tests expose.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-score-on-screen/01-CONTEXT.md
@.planning/phases/01-score-on-screen/01-RESEARCH.md
@.planning/phases/01-score-on-screen/01-01-SUMMARY.md
@src/score-model.js
@test/score-model.test.cjs
@test/osmd-node-env.cjs
@fixtures/01-right-hand.musicxml
</context>

## Expected rows (the D-07 contract, in the model's canonical order)

Row format used by every rung test: `[bar, onset, staff, voice, pitch, midi, duration]` with onset and duration as reduced `num/den` strings in quarter-note beats. A test helper `rowsOf(model)` maps each `model.notes[i]` to `[measure, onset.num + '/' + onset.den, staff, voice, pitch.name, pitch.midi, duration.num + '/' + duration.den]`.

**Rung 2** (`fixtures/02-left-hand.musicxml`, 5/4, staff 2 voice 5 only):
`[1,'0/1',2,5,'C3',48,'1/1']`, `[1,'1/1',2,5,'D3',50,'1/1']`, `[1,'2/1',2,5,'E3',52,'1/1']`, `[1,'3/1',2,5,'F3',53,'1/1']`, `[1,'4/1',2,5,'G3',55,'1/1']`

**Rung 3** (`fixtures/03-both-hands.musicxml`, 5/4, both staves):
`[1,'0/1',1,1,'C4',60,'1/1']`, `[1,'0/1',2,5,'C3',48,'1/1']`, `[1,'1/1',1,1,'D4',62,'1/1']`, `[1,'1/1',2,5,'D3',50,'1/1']`, `[1,'2/1',1,1,'E4',64,'1/1']`, `[1,'2/1',2,5,'E3',52,'1/1']`, `[1,'3/1',1,1,'F4',65,'1/1']`, `[1,'3/1',2,5,'F3',53,'1/1']`, `[1,'4/1',1,1,'G4',67,'1/1']`, `[1,'4/1',2,5,'G3',55,'1/1']`

**Rung 4** (`fixtures/04-chords.musicxml`, 4/4):
`[1,'0/1',1,1,'C4',60,'2/1']`, `[1,'0/1',1,1,'E4',64,'2/1']`, `[1,'0/1',1,1,'G4',67,'2/1']`, `[1,'0/1',2,5,'C3',48,'2/1']`, `[1,'2/1',1,1,'F4',65,'2/1']`, `[1,'2/1',1,1,'A4',69,'2/1']`, `[1,'2/1',1,1,'C5',72,'2/1']`, `[1,'2/1',2,5,'F3',53,'2/1']`

**Rung 5** (`fixtures/05-yanni-4-measures.musicxml`, 3/4, divisions 2, voices 1 and 5; derived at planning time from the MuseScore CLI export of the user's file; the same 41 tuples came out of the real OSMD parser for both the .musicxml and the .mxl):

| # | bar | onset | staff | voice | pitch | midi | duration | id |
|---|-----|-------|-------|-------|-------|------|----------|----|
| 1 | 1 | 0/1 | 1 | 1 | B4 | 71 | 3/1 | m1-s1-v1-b0_1-p71 |
| 2 | 1 | 0/1 | 1 | 1 | D5 | 74 | 3/1 | m1-s1-v1-b0_1-p74 |
| 3 | 1 | 0/1 | 1 | 1 | B5 | 83 | 3/1 | m1-s1-v1-b0_1-p83 |
| 4 | 1 | 0/1 | 2 | 5 | G2 | 43 | 1/2 | m1-s2-v5-b0_1-p43 |
| 5 | 1 | 1/2 | 2 | 5 | D3 | 50 | 1/2 | m1-s2-v5-b1_2-p50 |
| 6 | 1 | 1/1 | 2 | 5 | G3 | 55 | 1/2 | m1-s2-v5-b1_1-p55 |
| 7 | 1 | 3/2 | 2 | 5 | B3 | 59 | 1/2 | m1-s2-v5-b3_2-p59 |
| 8 | 1 | 2/1 | 2 | 5 | G3 | 55 | 1/2 | m1-s2-v5-b2_1-p55 |
| 9 | 1 | 5/2 | 2 | 5 | D3 | 50 | 1/2 | m1-s2-v5-b5_2-p50 |
| 10 | 2 | 0/1 | 1 | 1 | F#5 | 78 | 1/1 | m2-s1-v1-b0_1-p78 |
| 11 | 2 | 0/1 | 1 | 1 | D6 | 86 | 1/1 | m2-s1-v1-b0_1-p86 |
| 12 | 2 | 0/1 | 2 | 5 | G2 | 43 | 1/2 | m2-s2-v5-b0_1-p43 |
| 13 | 2 | 1/2 | 2 | 5 | D3 | 50 | 1/2 | m2-s2-v5-b1_2-p50 |
| 14 | 2 | 1/1 | 1 | 1 | E5 | 76 | 3/2 | m2-s1-v1-b1_1-p76 |
| 15 | 2 | 1/1 | 1 | 1 | C#6 | 85 | 3/2 | m2-s1-v1-b1_1-p85 |
| 16 | 2 | 1/1 | 2 | 5 | G3 | 55 | 1/2 | m2-s2-v5-b1_1-p55 |
| 17 | 2 | 3/2 | 2 | 5 | A3 | 57 | 1/2 | m2-s2-v5-b3_2-p57 |
| 18 | 2 | 2/1 | 2 | 5 | B3 | 59 | 1/1 | m2-s2-v5-b2_1-p59 |
| 19 | 2 | 5/2 | 1 | 1 | D5 | 74 | 1/2 | m2-s1-v1-b5_2-p74 |
| 20 | 2 | 5/2 | 1 | 1 | B5 | 83 | 1/2 | m2-s1-v1-b5_2-p83 |
| 21 | 3 | 0/1 | 1 | 1 | E5 | 76 | 1/1 | m3-s1-v1-b0_1-p76 |
| 22 | 3 | 0/1 | 1 | 1 | C#6 | 85 | 1/1 | m3-s1-v1-b0_1-p85 |
| 23 | 3 | 0/1 | 2 | 5 | A2 | 45 | 1/2 | m3-s2-v5-b0_1-p45 |
| 24 | 3 | 1/2 | 2 | 5 | E3 | 52 | 1/2 | m3-s2-v5-b1_2-p52 |
| 25 | 3 | 1/1 | 1 | 1 | G5 | 79 | 3/2 | m3-s1-v1-b1_1-p79 |
| 26 | 3 | 1/1 | 1 | 1 | E6 | 88 | 3/2 | m3-s1-v1-b1_1-p88 |
| 27 | 3 | 1/1 | 2 | 5 | A3 | 57 | 1/1 | m3-s2-v5-b1_1-p57 |
| 28 | 3 | 2/1 | 2 | 5 | C#4 | 61 | 1/2 | m3-s2-v5-b2_1-p61 |
| 29 | 3 | 2/1 | 2 | 5 | E4 | 64 | 1/2 | m3-s2-v5-b2_1-p64 |
| 30 | 3 | 5/2 | 1 | 1 | F#5 | 78 | 1/2 | m3-s1-v1-b5_2-p78 |
| 31 | 3 | 5/2 | 1 | 1 | D6 | 86 | 1/2 | m3-s1-v1-b5_2-p86 |
| 32 | 3 | 5/2 | 2 | 5 | B3 | 59 | 1/2 | m3-s2-v5-b5_2-p59 |
| 33 | 3 | 5/2 | 2 | 5 | D4 | 62 | 1/2 | m3-s2-v5-b5_2-p62 |
| 34 | 4 | 0/1 | 1 | 1 | E5 | 76 | 3/1 | m4-s1-v1-b0_1-p76 |
| 35 | 4 | 0/1 | 1 | 1 | C#6 | 85 | 3/1 | m4-s1-v1-b0_1-p85 |
| 36 | 4 | 0/1 | 2 | 5 | C#4 | 61 | 1/2 | m4-s2-v5-b0_1-p61 |
| 37 | 4 | 1/2 | 2 | 5 | B3 | 59 | 1/2 | m4-s2-v5-b1_2-p59 |
| 38 | 4 | 1/1 | 2 | 5 | A3 | 57 | 1/2 | m4-s2-v5-b1_1-p57 |
| 39 | 4 | 3/2 | 2 | 5 | B3 | 59 | 1/2 | m4-s2-v5-b3_2-p59 |
| 40 | 4 | 2/1 | 2 | 5 | C#4 | 61 | 1/2 | m4-s2-v5-b2_1-p61 |
| 41 | 4 | 5/2 | 2 | 5 | A3 | 57 | 1/2 | m4-s2-v5-b5_2-p57 |

Rung 5 measures: numbers 1..4, starts 0/1, 3/1, 6/1, 9/1, length 3/1 each, timeSignature `{ beats: 3, beatType: 4 }`. Every rung 5 note has `tiedNoteCount: 1` (the file has no ties, grace notes, tuplets or repeats — confirmed at planning time).

**Tie fixture** (`test/fixtures/tie.musicxml`, 4/4, 3 bars; 5 written noteheads, 3 model notes):
`[1,'0/1',1,1,'C4',60,'4/1']` tiedNoteCount 2, `[2,'0/1',1,1,'E4',64,'2/1']` tiedNoteCount 1, `[2,'2/1',1,1,'G4',67,'6/1']` tiedNoteCount 2. Measures: starts 0/1, 4/1, 8/1, length 4/1.

<tasks>

<task type="auto">
  <name>Task 1: Ladder rungs 2, 3, 4 as fixtures with exact note-list tests</name>
  <files>fixtures/02-left-hand.musicxml, fixtures/03-both-hands.musicxml, fixtures/04-chords.musicxml, test/score-model.test.cjs, src/score-model.js</files>
  <read_first>
    - fixtures/01-right-hand.musicxml (the grand-staff skeleton to copy: divisions 2, staves 2, clefs, staff 1 = voice 1, staff 2 = voice 5, backup, measure rests)
    - test/score-model.test.cjs (existing harness, helpers and the rung 1 test to extend, not duplicate)
    - src/score-model.js (compareNotes canonical order; only touch it if a rung test exposes a bug)
    - .planning/phases/01-score-on-screen/01-CONTEXT.md (D-04, D-05, D-07)
    - The "Expected rows" section of this plan (authoritative values; do not derive expectations by running the extractor)
  </read_first>
  <action>
Author three more rungs with the exact same skeleton as rung 1 (D-05) and assert their note lists (D-07). Write the expected arrays from the "Expected rows" section above, by hand, before running the tests; never paste extractor output into an expectation.

1. `fixtures/02-left-hand.musicxml`: work-title `Rung 2 - Left hand C D E F G`, 5/4, divisions 2. Staff 1 voice 1: one whole-measure rest (duration 10). Backup 10. Staff 2 voice 5: C3 D3 E3 F3 G3 as quarter notes (duration 2 each, `<staff>2</staff>`, `<voice>5</voice>`).

2. `fixtures/03-both-hands.musicxml`: work-title `Rung 3 - Both hands C D E F G`, 5/4. Staff 1 voice 1: C4 D4 E4 F4 G4 quarters. Backup 10. Staff 2 voice 5: C3 D3 E3 F3 G3 quarters.

3. `fixtures/04-chords.musicxml`: work-title `Rung 4 - Two chords`, 4/4 (`<beats>4</beats>`), divisions 2. Staff 1 voice 1: chord 1 = C4 (duration 4, type half) then E4 and G4 each with `<chord/>` as the first child of `<note>` (same duration and type); chord 2 = F4 then A4 and C5 with `<chord/>`. Backup 8. Staff 2 voice 5: C3 half (duration 4), F3 half.

4. In `test/score-model.test.cjs` add the `rowsOf(model)` helper described in "Expected rows" and three tests named "rung 2 (left hand C D E F G) produces the exact expected note list", "rung 3 (both hands) produces the exact expected note list", "rung 4 (two chords) produces the exact expected note list", each: `loadSheet(readFixture('fixtures/0N-....musicxml'))`, `extract`, `assert.deepEqual(rowsOf(model), EXPECTED_RUNG_N)`, `assert.ok(model.notes.every(n => n.tiedNoteCount === 1))`, and `assert.equal(new Set(model.notes.map(n => n.id)).size, model.notes.length)`. For rung 4 additionally assert the three chord notes share `onset` `{num:0,den:1,beats:0}` but have three distinct ids ending `-p60`, `-p64`, `-p67` (edge: adjacency — equal onsets do not merge). For rung 3 assert notes 0 and 1 are `m1-s1-v1-b0_1-p60` then `m1-s2-v5-b0_1-p48` (staff 1 before staff 2 at an equal onset: canonical order). Assert `model.measures[0].timeSignature` is `{ beats: 5, beatType: 4 }` for rungs 2 and 3 and `{ beats: 4, beatType: 4 }` for rung 4.

Run `node --test test/score-model.test.cjs`. If a rung test fails on a model field, fix `src/score-model.js` (never the expectation) and re-run. Commit fixtures, tests, and any model fix together.
  </action>
  <verify>
    <automated>node --test test/score-model.test.cjs</automated>
    <fails_when>non-zero exit, or "# fail 0" absent from the summary, or "# pass 8" absent (5 from plan 01 plus 3 rungs)</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test test/score-model.test.cjs` prints `# pass 8` and `# fail 0`
    - `grep -c '<beats>5</beats>' fixtures/02-left-hand.musicxml` prints `1`; `grep -c '<beats>5</beats>' fixtures/03-both-hands.musicxml` prints `1`; `grep -c '<beats>4</beats>' fixtures/04-chords.musicxml` prints `1`
    - `grep -o '<chord/>' fixtures/04-chords.musicxml | wc -l` prints `4` (two triads, two chord-continuation notes each)
    - `grep -o '<pitch>' fixtures/02-left-hand.musicxml | wc -l` prints `5`; `grep -o '<pitch>' fixtures/03-both-hands.musicxml | wc -l` prints `10`; `grep -o '<pitch>' fixtures/04-chords.musicxml | wc -l` prints `8`
    - `grep -c '<staves>2</staves>' fixtures/02-left-hand.musicxml` prints `1`; same for fixtures/03-both-hands.musicxml and fixtures/04-chords.musicxml
    - `grep -c "rung 2 (left hand" test/score-model.test.cjs` prints `1`; same for `rung 3 (both hands` and `rung 4 (two chords`
    - `grep -c 'function rowsOf' test/score-model.test.cjs` prints `1`
  </acceptance_criteria>
  <done>Rungs 2 to 4 exist in fixtures/, follow rung 1's skeleton, and each has a green test asserting its exact rows, chord separation, canonical staff order, and time signature.</done>
</task>

<task type="auto">
  <name>Task 2: Rung 5 built from the user's MuseScore file (uncompressed and .mxl) with a reproducible script and exact tests</name>
  <precondition>"C:/Program Files/MuseScore 4/bin/MuseScore4.exe" exists and "C:/Users/vaheh/OneDrive/Documents/MuseScore4/Scores/Yanni - 4 measures.mscz" exists (read-only ls of both paths)</precondition>
  <files>scripts/build-rung5.cjs, fixtures/05-yanni-4-measures.musicxml, fixtures/05-yanni-4-measures.mxl, test/score-model.test.cjs</files>
  <read_first>
    - .planning/phases/01-score-on-screen/01-CONTEXT.md (D-06: the exact MuseScore CLI command, the preferred user trim, and the mandated fallback; Specifics: the rung 5 content)
    - .planning/phases/01-score-on-screen/01-RESEARCH.md (Pitfall 4: .mxl bytes must stay binary)
    - test/score-model.test.cjs and test/osmd-node-env.cjs (helpers; File is on global from the shim)
    - The rung 5 table in "Expected rows" above (authoritative 41 rows)
  </read_first>
  <action>
Produce rung 5 exactly the way D-06 prescribes, scripted so it can be re-run when the user edits the piece.

1. `scripts/build-rung5.cjs` (Node, CommonJS, no dependencies): constants `MUSESCORE_EXE = process.env.MUSESCORE_EXE || 'C:\\Program Files\\MuseScore 4\\bin\\MuseScore4.exe'`, `SOURCE = process.env.RUNG5_SOURCE || 'C:\\Users\\vaheh\\OneDrive\\Documents\\MuseScore4\\Scores\\Yanni - 4 measures.mscz'`, `OUT_XML = path.resolve(__dirname, '..', 'fixtures', '05-yanni-4-measures.musicxml')`, `OUT_MXL` likewise with `.mxl`. Steps, each logged to stdout:
   - Step 1 export: `execFileSync(MUSESCORE_EXE, ['-f', '-o', OUT_XML, SOURCE], { stdio: 'inherit' })` (absolute output path; `-f` ignores MuseScore warnings so the run is non-interactive). Fail loudly with the exe path if it throws.
   - Step 2 trim (the D-06 fallback): read OUT_XML as UTF-8, match every measure block with a regex tolerant of attributes (`<measure number="N" ...> ... </measure>`), and from the last measure backwards remove each block that contains no `<pitch>` element, stopping at the first measure that has one. If any were removed, write the file back and print `Trimmed <N> trailing empty measures (bars <first>-<last>). Preferred fix: delete those bars in MuseScore and save, then re-run; this step is then a no-op.` and set a `trimmed` flag; if none, print `No trailing empty measures; source already trimmed.` At planning time the source still had 43 measures, 39 of them empty, so expect the trim to run and say so in the SUMMARY (D-06).
   - Step 3 compressed export: `execFileSync(MUSESCORE_EXE, ['-f', '-o', OUT_MXL, OUT_XML], { stdio: 'inherit' })` — MuseScore imports the (trimmed) MusicXML and writes the compressed container. Verified at planning time: the model extracted from this .mxl is identical, tuple for tuple, to the .musicxml model (41 notes), so no zip library is needed and none may be added (fflate/jszip are not in the Package Legitimacy Audit).
   - Step 4 report: print the final measure count (count of `<measure ` in OUT_XML) and pitched note count (count of `<pitch>`), and exit non-zero if the measure count is not 4 or the pitch count is not 41.
   Run `node scripts/build-rung5.cjs` and commit both fixtures with the script.

2. Tests in `test/score-model.test.cjs`:
   - "rung 5 (Yanni, four real bars) produces the exact expected note list": `assert.deepEqual(rowsOf(model), EXPECTED_RUNG_5)` with the 41 rows from the table typed as literals, `assert.deepEqual(model.notes.map(n => n.id), EXPECTED_RUNG_5_IDS)` with the 41 ids from the table, `assert.deepEqual(model.measures, [...])` for the four measures (numbers 1-4, starts 0/1 3/1 6/1 9/1, length 3/1, timeSignature `{ beats: 3, beatType: 4 }`), `model.notes.length === 41`, every `tiedNoteCount === 1`.
   - "rung 5 extracted twice is identical": two independent `loadSheet` + `extract` calls, `assert.deepEqual(model1, model2)` (edge: ordering is deterministic).
   - "no untied note extends past its bar": for rungs 1 to 5, `compareRationals(addRationals(n.onset, n.duration), measure.length) <= 0` for every note, where `measure` is the model measure with `number === n.measure` (edge: boundary); and assert rung 5 contains a note at onset `5/2` (the last eighth of a 3/4 bar) and rung 1 a note at onset `4/1`.
   - "rung 5 .mxl loads through the binary path and matches the .musicxml model": `const bytes = fs.readFileSync(path to fixtures/05-yanni-4-measures.mxl); const file = new File([bytes], '05-yanni-4-measures.mxl');` then `loadSheet(file)`, `extract`, `assert.deepEqual(mxlModel.notes, xmlModel.notes)` and `assert.deepEqual(mxlModel.measures, xmlModel.measures)`.
   - "rung 1 loaded as a File (the browser handoff) equals rung 1 loaded as text": `new File([fs.readFileSync(rung 1 path)], '01-right-hand.musicxml')` through `loadSheet`, `assert.deepEqual(fileModel, textModel)`.
   Run the suite until green. If MuseScore's output differs from the table on any of the 41 rows, stop and report the diff in the SUMMARY rather than editing the table: the table is what the user's file contained at planning time and a change means the piece was edited.
  </action>
  <verify>
    <automated>node scripts/build-rung5.cjs && test "$(grep -o '<measure ' fixtures/05-yanni-4-measures.musicxml | wc -l)" -eq 4 && test "$(grep -o '<pitch>' fixtures/05-yanni-4-measures.musicxml | wc -l)" -eq 41 && head -c 2 fixtures/05-yanni-4-measures.mxl | grep -q PK && node --test test/score-model.test.cjs</automated>
    <fails_when>non-zero exit from any link: MuseScore export error, measure count not 4, pitch count not 41, the .mxl not starting with the zip signature PK, or "# fail 0" absent from the test summary (expected "# pass 13")</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/build-rung5.cjs` exits 0 and prints either `Trimmed ` or `No trailing empty measures`
    - `grep -o '<measure ' fixtures/05-yanni-4-measures.musicxml | wc -l` prints `4`; `grep -o '<pitch>' fixtures/05-yanni-4-measures.musicxml | wc -l` prints `41`; `grep -c '<beats>3</beats>' fixtures/05-yanni-4-measures.musicxml` prints `1`
    - `head -c 2 fixtures/05-yanni-4-measures.mxl` prints `PK`; `git ls-files --error-unmatch fixtures/05-yanni-4-measures.mxl fixtures/05-yanni-4-measures.musicxml` exits 0
    - `grep -c 'execFileSync' scripts/build-rung5.cjs` prints at least 2; `grep -c 'MuseScore4.exe' scripts/build-rung5.cjs` prints at least 1
    - `grep -c 'm4-s2-v5-b5_2-p57' test/score-model.test.cjs` prints at least 1 (the 41st id is literal in the test)
    - `grep -c 'new File(' test/score-model.test.cjs` prints at least 2 (.mxl and rung 1 handoff tests)
    - `node --test test/score-model.test.cjs` prints `# pass 13` and `# fail 0`
  </acceptance_criteria>
  <done>Rung 5 exists as a 4-measure, 41-note MuseScore export in both .musicxml and .mxl form, rebuilt by one script, and the tests assert all 41 rows, the four measures, determinism, bar boundaries, and .mxl/.musicxml equivalence. The SUMMARY states whether the trim fallback ran.</done>
</task>

<!-- planner-discipline-allow: TieTypes -->
<task type="auto">
  <name>Task 3: Tie fixture and score-model contract tests (D-03, purity, precision, empty, duplicate id)</name>
  <files>test/fixtures/tie.musicxml, test/score-model.test.cjs, src/score-model.js</files>
  <read_first>
    - src/score-model.js (tie folding branch, rational helpers, duplicate-id throw)
    - .planning/phases/01-score-on-screen/01-RESEARCH.md (Pattern 2 tie resolution via NoteTie.StartNote; Pitfall 3: TieTypes is unrelated)
    - .planning/phases/01-score-on-screen/01-CONTEXT.md (D-03, D-08, D-10)
    - test/score-model.test.cjs (helpers; keep one file)
    - prototype/piano-core.test.cjs (lines 98-101: assert.throws with a message regex)
  </read_first>
  <action>
Exercise the tie rule the ladder never does (D-03) and pin the remaining contract edges.

1. `test/fixtures/tie.musicxml` (test-only, not a ladder rung): work-title `Tie fixture`, 4/4, divisions 2, the rung skeleton (two staves; staff 2 voice 5 holds a whole-measure rest of duration 8 in every bar after a `<backup><duration>8</duration></backup>`). Staff 1 voice 1: bar 1: C4 half (duration 4, `<tie type="start"/>` right after `<duration>`, plus `<notations><tied type="start"/></notations>`), then C4 half with `<tie type="stop"/>` and `<tied type="stop"/>`. Bar 2: E4 half; G4 half with tie start. Bar 3: G4 whole (duration 8, type whole) with tie stop. Written noteheads: 5.

2. Tests:
   - "a tied pair collapses to one note with the combined duration, within a bar and across a barline": `assert.deepEqual(rowsOf(model), EXPECTED_TIE)` (3 rows from "Expected rows"), `model.notes.map(n => n.tiedNoteCount)` deep-equals `[2, 1, 2]`, `model.notes.length === 3` while `(readFixture('test/fixtures/tie.musicxml').match(/<pitch>/g) || []).length === 5`, ids `m1-s1-v1-b0_1-p60`, `m2-s1-v1-b0_1-p64`, `m2-s1-v1-b2_1-p67`, and `model.measures` has 3 entries with starts 0/1, 4/1, 8/1.
   - "toQuarterBeats is exact for every note value in the ladder": using `const { Fraction } = require('opensheetmusicdisplay/build/opensheetmusicdisplay.min.js')`: `new Fraction(0, 1, 1)` (whole) gives `{num:4,den:1,beats:4}`, `new Fraction(1, 2)` gives `{2,1,2}`, `new Fraction(1, 4)` gives `{1,1,1}`, `new Fraction(1, 8)` gives `{1,2,0.5}`, `new Fraction(3, 8)` gives `{3,2,1.5}`, `new Fraction(3, 16)` gives `{3,4,0.75}`, `new Fraction(12, 8)` gives `{6,1,6}` (reduced); `addRationals({num:1,den:2},{num:1,den:3})` gives `{5,6,...}`; `compareRationals({1,2},{2,4}) === 0` and `compareRationals({5,2},{3,1}) < 0`.
   - "a rest-only file yields no notes but still lists its measure": inline XML string (one 4/4 bar, two staves, whole-measure rests only) through `loadSheet`; `assert.deepEqual(model.notes, [])`, `model.measures.length === 1`, and `JSON.parse(JSON.stringify(model))` deep-equals model.
   - "a chord that repeats a pitch is rejected, not silently merged": inline XML with staff 1 voice 1 chord C4 + C4 (`<chord/>`) quarter; `assert.throws(() => M.extract(sheet), /Duplicate note id m1-s1-v1-b0_1-p60/)`.
   - "extract has no DOM dependency": load rung 1 normally, then save and `delete global.document`, `delete global.window`, `delete global.navigator` inside a `try`, call `M.extract(sheet)` and assert 5 notes, restore the three globals in `finally`.
   - "TieTypes is never consulted": `grep`-free behavioural check is the tie test above; additionally assert `grep -c 'TieTypes' src/score-model.js` semantics in code: read `src/score-model.js` with fs and `assert.equal(source.includes('TieTypes'), false)`.
   Fix `src/score-model.js` if any contract test fails. Run the suite until green, commit.
  </action>
  <verify>
    <automated>node --test test/score-model.test.cjs</automated>
    <fails_when>non-zero exit, or "# fail 0" absent from the summary, or "# pass 19" absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test test/score-model.test.cjs` prints `# pass 19` and `# fail 0`
    - `grep -o '<tied type="stop"/>' test/fixtures/tie.musicxml | wc -l` prints `2`; `grep -o '<pitch>' test/fixtures/tie.musicxml | wc -l` prints `5`; `grep -o '<measure ' test/fixtures/tie.musicxml | wc -l` prints `3`
    - `grep -c 'NoteTie' src/score-model.js` prints at least 1; `grep -c 'StartNote' src/score-model.js` prints at least 1; `grep -c 'TieTypes' src/score-model.js` prints `0`
    - `grep -c 'Duplicate note id' src/score-model.js` prints `1` and `grep -c 'Tie continuation without a start note' src/score-model.js` prints `1`
    - `grep -c 'delete global.document' test/score-model.test.cjs` prints `1`
    - `grep -c "new Fraction(3, 16)" test/score-model.test.cjs` prints `1`
  </acceptance_criteria>
  <done>The tie rule, rational exactness, canonical ordering, empty input, duplicate-id rejection, and DOM-free extraction are all asserted by green tests; the model contract is pinned before Phase 2 persists it.</done>
</task>

</tasks>

## Artifacts this phase produces

Added by this plan (on top of plan 01's symbols):

- Files: `fixtures/02-left-hand.musicxml`, `fixtures/03-both-hands.musicxml`, `fixtures/04-chords.musicxml`, `fixtures/05-yanni-4-measures.musicxml`, `fixtures/05-yanni-4-measures.mxl`, `test/fixtures/tie.musicxml`
- `scripts/build-rung5.cjs` (CLI entry, no exports): env overrides `MUSESCORE_EXE`, `RUNG5_SOURCE`
- Test helpers in `test/score-model.test.cjs`: `rowsOf(model)`, `EXPECTED_RUNG_2` .. `EXPECTED_RUNG_5`, `EXPECTED_RUNG_5_IDS`, `EXPECTED_TIE`
- No new runtime symbols; `src/score-model.js` may receive bug fixes only

<verification>
- `node --test test/score-model.test.cjs` green with 19 tests after Task 3
- `node scripts/build-rung5.cjs` reproducible: 4 measures, 41 pitched notes, .mxl starts with PK
- All six fixture files tracked by git (`git ls-files fixtures test/fixtures`)
</verification>

<success_criteria>
- Every ladder rung has a fixture test asserting its exact note list (SCORE-03, D-07); rung 5 matches the 41 rows recorded at planning time
- The .mxl path of SCORE-01 is exercised by a real MuseScore-exported compressed file
- Tie resolution (D-03), rational exactness (D-10), canonical ordering, empty input, and DOM-free extraction (D-08) are pinned by tests
</success_criteria>

<output>
Create `.planning/phases/01-score-on-screen/01-02-SUMMARY.md` when done. It MUST state whether `scripts/build-rung5.cjs` trimmed trailing empty measures (D-06 fallback) and, if so, remind the user to delete bars 5-43 in MuseScore and save.
</output>
