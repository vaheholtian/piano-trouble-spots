---
phase: 03-what-you-actually-played
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/align.js
  - src/aggregate.js
  - src/paint.js
  - src/capture-app.js
  - src/score-renderer.js
  - index.html
  - scripts/check-run-path.cjs
  - test/align.test.cjs
  - test/aggregate.test.cjs
autonomous: true
requirements: [ANLZ-01, ANLZ-03, AGGR-01, AGGR-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "After one recorded pass of C D F F G on rung 1, the E4 notehead is red on the rendered notation and the other four stay black, painted through ScoreRenderer.state.svgMap on the notehead children with no OSMD re-render (ANLZ-01, AGGR-03 slice, D-17, D-18)"
    - "Clicking the red E4 shows 'E4, bar 1 beat 3: wrong in 1 of 1 assessed passes (played F4 once)' in #detail; after later passes the same panel rewrites itself from the current aggregate without another click (AGGR-01 slice, AGGR-03 slice, D-19, review 03-04 MEDIUM)"
    - "Every fixture's note verdicts are compared completely (review 03-01 MEDIUM round 3): the keys of PassResult.notes equal the keys of f.expected.notes exactly, so an omitted or extra verdict fails the test"
    - "Tokens follow seq order with an absolute chord window (review 03-01 HIGH round 3): a later-seq note-on stamped 60 ms before the open token's first note-on starts its own token (rung 1 C D E, F at 1940, G → all played, F deviationMs −560, totalCost 3808), one stamped 40 ms before joins it (F at 1960 → E played, F4 extra at within:2, F missed, totalCost 7000), and pass.notes is deep-equal before and after; the crossover with E at 2600 is built as the chronological events C 1000, D 1500, F 1900, F 2500, E 2600, G 3000 and gives totalCost 6680 with E wrong played F4 and an extra E4 at before:4"
    - "All twelve fixtures from 03-01 pass through Align.alignPass with their hand-derived labels: origin is the first accented click at or after the pass start and never moves (D-01, D-02), shifted pitches on the clicks read as all wrong (D-08), a correction credits the right note with the extra attached to its gap (D-05, D-10), an abandoned pass leaves later slots not-reached with a half-beat grace (D-11), and a zero-note pass is not an attempt (D-14)"
    - "A pass is painted and counted only when final: Align.isFinal(pass, clicks, sessionEnded, tunables) is false until the session ends or the recorded timeline holds an accented click at or after the pass start and a click at or after end + (markGraceBeats + 1) × localBeatMs(b); for every fixture and every final prefix of its clicks, Align.stableFields(alignPass on the prefix) deep-equals Align.stableFields(alignPass on the whole timeline), where stableFields removes expectedTime from unreached slots only, so the live verdicts equal the replay (review 03-02 HIGH round 1; the whole-result deep equality was false, review 03-02 HIGH round 2)"
    - "The finalization boundary test matches the reach formula (review 03-02 HIGH round 2): rung 1 C D E F, G unplayed, end 2750 — clicks through 2500 (G's click genuinely absent, as live at the mark) give isFinal false and G not-reached; clicks through 3000 give isFinal false and G missed (3000 <= 2750 + 250); clicks through 3500 give isFinal true and G missed"
    - "A session is analysed only against the model it was recorded against and painted only when that model is the one on screen: A.session carries pieceId and model, analyzeCurrent never reads ScoreRenderer.state.model for an existing session, renderAnalysis resets the paint when A.session.model !== ScoreRenderer.state.model, and a load generation counter discards a stale loadLatestSession result (review 03-02 HIGH round 1); start() and endSession() advance that counter before their first await, and loadLatestSession applies its result only when its generation is still current and no live session exists, so Start pressed while a restoration is in flight keeps the live session on screen (review 03-02 HIGH round 3)"
    - "A failed piece load during capture ends capture cleanly (review 03-02 HIGH round 3): the piece-unloaded listener calls endSession('piece-unloaded') before any await, so the metronome and resample timer stop synchronously; the metronome's time-signature callback reads the session's own model captured at Start, never ScoreRenderer.state.model; the ended session keeps its model, passes and clicks in A.session (live false) and its raw events, passes and clicks stay stored, so nothing is painted while no score is on screen and reopening that piece restores its marks through loadLatestSession"
    - "Reopening the same file keeps painting (review 03-02 HIGH round 2): when the piece-loaded listener's verified content hash equals A.session.pieceId, it rebinds A.session.model to the newly extracted model (same bytes give the same structural note ids, Phase 1 D-09), clears the result cache and re-analyses, so the live session that capture-app deliberately keeps on a same-hash reload is painted on the new map"
    - "Final PassResults are reused in memory within a session (review 03-03 MEDIUM round 2): analyzeCurrent aligns only passes that became final since the last call and keeps earlier final results in A.resultCache (cleared on any session change or model rebind); safe because stableFields of a final pass never changes and nothing reads the one field that can; nothing is persisted (D-20)"
    - "deviationMs of every played note is its own matched event's timeStamp minus its fixed-origin expected time, so a rolled chord keeps each note's timing (review 03-02 MEDIUM)"
    - "src/align.js and src/aggregate.js run under plain node:test with no DOM, MIDI or audio global referenced (ANLZ-03); scripts/check-run-path.cjs syntax-checks them and pins the thirteen-script load order"
    - "Tempo groups exist from the first commit: Aggregate.foldSession groups final attempts by their origin click's bpm, lists tempo-changed passes outside every group and zero-note passes as non-attempts (D-16, D-14); Align.ANALYSIS_VERSION is 1 and nothing is persisted (D-20)"
    - "Gap denominators: GapCounts counts extras only in passes where the gap is assessed, carries unassessedPasses and lowerBoundPasses, treats a gap with no certain extra next to an ambiguous note as unassessed, and prints 'at least' when a counted pass had ambiguous notes nearby; lowerBoundPasses <= passesWithExtra <= assessedPasses is asserted for folds over every fixture's PassResult and over hand-built ambiguous-neighbour and no-origin results (review 03-01 HIGH round 1, review 03-01 MEDIUM round 2)"
    - "Boundary (ANLZ-01 boundary probe, explicit): an expected time exactly at end + grace is reached and one millisecond later is not-reached; two note-ons exactly chordWindowMs apart are one token and one millisecond more are two; isFinal is false with the last click one millisecond before end + 1.5 beats and true exactly at it. Precision (ANLZ-01 precision probe, explicit): every cost is an integer in thousandths and PassResult.totalCost reproduces the contract's worked numbers (correction 3680, shifted start 15000, crossover with E at 2300 5040, chronological crossover C D F F E G with E at 2600 6680)"
    - "Single-pass gap sentences keep their ambiguity information (review 03-04 HIGH round 3): Aggregate.gapAssessment(result, gapKey) returns { baseAssessable, extras, ambiguityAdjacent } from a full PassResult and is the one rule both foldSession and describeGap use; describeGap's pass source is { kind: 'pass', result } (the whole PassResult, never a filtered extras list), so the messy pass view reads 'Extra notes between E4 and F4: at least A4, B4, A4, B4 (ambiguous notes nearby)'"
    - "AGGR-01 boundary (explicit): equal wrong and missed counts colour the note wrong; AGGR-01 empty probe (explicit): foldSession(model, []) returns zero tempo groups and every note untested; AGGR-01 adjacency (explicit): results at bpm 120 and 121 form two groups; AGGR-01 ordering probe (explicit): tempo groups are ordered by first pass ordinal, wrong-pitch lists by count descending then MIDI ascending; AGGR-01 precision probe (explicit): counts are integers and rates are printed as 'w of a', never a percentage"
  artifacts:
    - path: "src/align.js"
      provides: "globalThis.Align: ANALYSIS_VERSION, REASONS, DEFAULT_TUNABLES (with reachClock 'origin' and lateEntryProbeTokens), resolveTunables, resolveOrigin, expectedTime, localBeatMs, buildSlots, tokenizePlayed, pairCost, alignAtLag, alignPass, analyzeSession, isFinal, stableFields"
      contains: "globalThis.Align"
    - path: "src/aggregate.js"
      provides: "globalThis.Aggregate: foldSession, gapAssessment, colourFor, viewForTempoGroup, viewForPass, describeNote, describeGap (pass source = the whole PassResult), headingFor, wordCount"
      contains: "globalThis.Aggregate"
    - path: "src/paint.js"
      provides: "globalThis.Paint: COLOURS, paintNotes, resetAll, clearExtras, paintExtras"
      contains: "globalThis.Paint"
    - path: "src/capture-app.js"
      provides: "Analysis state A (session with pieceId and model, results, resultCache, aggregate, view, pendingOrdinals, detail, loadGen), analyzeCurrent, renderAnalysis, refreshDetail, loadLatestSession, same-hash model rebind in the piece-loaded listener, load-generation advance in start() and endSession(), capture ended by piece-unloaded, C.sessionModel for the metronome's time signatures, click-timeline finalization hook, notation click delegation"
      contains: "pendingOrdinals"
    - path: "test/align.test.cjs"
      provides: "Fixture-driven engine tests, boundary and origin variants, worked-cost checks, final-prefix property over stableFields"
      contains: "isFinal"
    - path: "test/aggregate.test.cjs"
      provides: "Hand-built PassResult folds: D-14, D-16, D-18, D-19 sentences, gap invariant, JSON round trip"
      contains: "foldSession"
  key_links:
    - from: "src/capture-app.js"
      to: "src/align.js"
      via: "analyzeCurrent() calls Align.isFinal per completed pass and aligns the final ones (Align.analyzeSession in Task 1; Align.alignPass for each final pass not yet in A.resultCache from Task 2) against A.session.model and A.session.clicks after a mark, after a click that finalizes a pending pass, at start(), at endSession(), and in loadLatestSession()"
      pattern: "Align\\.isFinal"
    - from: "src/capture-app.js"
      to: "src/paint.js"
      via: "renderAnalysis() calls Paint.paintNotes(ScoreRenderer.state.svgMap, view.noteKinds) only when A.session.model === ScoreRenderer.state.model, and re-runs on the piece-rendered event"
      pattern: "Paint\\.paintNotes"
    - from: "src/score-renderer.js"
      to: "src/capture-app.js"
      via: "renderAndMap() dispatches a piece-rendered CustomEvent after buildSvgMap() so the repaint always sees the rebuilt map"
      pattern: "piece-rendered"
    - from: "src/align.js"
      to: "src/score-model.js"
      via: "every onset comparison and addition goes through ScoreModel.compareRationals / ScoreModel.addRationals"
      pattern: "ScoreModel\\.(compareRationals|addRationals)"
  prohibitions:
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT move, snap or re-anchor the pass origin to the first played note or to a better-fitting downbeat; the origin is the first accented click at or after the pass start and deviationMs is always measured from it (D-01, D-02)"
    - requirement_id: ANLZ-01
      category: values
      statement: "MUST NOT drop, filter or velocity-gate any non-marker note-on; note-offs are never used for pitch classification (D-09)"
    - requirement_id: AGGR-01
      category: transparency
      statement: "MUST NOT count a zero-note pass as an attempt, count a tempo-changed pass inside a tempo group, count an unreached slot as clean or missed, or count a pending (not yet final) pass anywhere (D-11, D-14, D-16)"
    - requirement_id: AGGR-01
      category: values
      statement: "MUST NOT persist alignment results or write to any raw event, pass, click or session record; analysis is derived data held in memory (D-20, CAPT-05)"
    - requirement_id: AGGR-03
      category: transparency
      statement: "MUST NOT paint a session's marks on a score other than the one it was recorded against (review 03-02 HIGH)"
    - requirement_id: ANLZ-03
      category: transparency
      statement: "MUST NOT tune a weight or window to make a fixture pass without recording the fixture name and the new value in the docs/analysis-rules.md tunables table"
---

## Phase Goal

**As a** pianist drilling right-hand C D E F G alone at the FP-60X, **I want to** see wrong, missed and extra notes painted on the notation after each pass, with honest counts across the passes at one tempo, **so that** I can tell a recurring mistake from a one-off slip and trust what the score shows me.

<objective>
The tracer: one path, every layer. A marked pass of C D F F G on rung 1 goes raw events → PassSegmenter → `Align.alignPass` (fixed origin from the click timeline, slots, tokens, the in-time DP with the contract's cost model) → `Aggregate.foldSession` → `Paint.paintNotes` → a red E4 on the real notation and the D-19 sentence in the detail panel when it is clicked. Task 2 then makes that path trustworthy under real conditions the cross-AI review found: a pass is judged only once the click timeline covers it (live equals replay), a session is analysed and painted only against its own score across piece switches, deviations come from each matched event, and the detail panel regenerates. Every one of 03-01's twelve fixtures is green under node:test, and 03-01's headless harness turns fully green without being edited.

Purpose: ROADMAP success criteria 1, 2 (single-pass part) and 3 on rung 1, proven at the desk so the piano session (03-05) is spent on judgement, not defects.

Output: src/align.js, src/aggregate.js, src/paint.js, the capture-app hooks, the piece-rendered event, index.html analysis DOM, check-run-path list, test/align.test.cjs, test/aggregate.test.cjs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-what-you-actually-played/03-CONTEXT.md
@.planning/phases/03-what-you-actually-played/03-RESEARCH.md
@.planning/phases/03-what-you-actually-played/03-PATTERNS.md
@docs/analysis-rules.md
@scripts/check-paint.cjs
@src/score-model.js
@src/score-renderer.js
@src/pass-segmenter.js
@src/storage.js
@src/metronome.js
@src/midi-capture.js
@src/capture-app.js
@index.html
@scripts/check-run-path.cjs
@test/pass-segmenter.test.cjs

<interfaces>
<!-- Contracts this plan implements against. Shapes are fixed in docs/analysis-rules.md (03-01); scripts/check-paint.cjs is the executable spec for the DOM. -->

Module wrapper (src/pass-segmenter.js lines 14-15, 70-73), to copy for align.js, aggregate.js, paint.js:
  'use strict'; globalThis.Name = (() => { ... return { ... }; })(); if (typeof module !== 'undefined') module.exports = globalThis.Name;

Engine inputs:
  model: ScoreModel (measures[].start rational, notes[] with id/measure/onset/pitch.midi/pitch.name, canonical order)
  pass: { ordinal, startTimeStamp, endTimeStamp | null, notes: [{ seq, note, velocity, timeStamp }] }
  clicks: [{ pageTime, bar, beat, bpm, accent }] sorted by pageTime (Storage.readClicks order or C.clicks)

Engine outputs (from the contract): PassResult (with entryLag, totalCost), SessionAggregate, NoteCounts, GapCounts (with unassessedPasses), PaintView — see docs/analysis-rules.md "Shapes".

Renderer surface (src/score-renderer.js): ScoreRenderer.state.svgMap (Map noteId -> vf-notehead container; set fill on el.children), ScoreRenderer.state.model, ScoreRenderer.state.renderCount, renderAndMap() (called from loadPiece before piece-loaded is dispatched, and from the debounced resize handler), loadPiece(file).

Capture surface (src/capture-app.js): C.session, C.events, C.clicks, C.pieceId, C.db, onClick(click) (line 208, pushes to C.clicks), renderPassList() (lines 299-313), mark() (317-349), start() (439-508), endSession() (514-564; its final pass-list loop at 552-558), restore() (53-140), the piece-loaded listener (600-624, which awaits hashing before endSession('piece-change')), piece-unloaded (626-628); helpers $ / toast from score-renderer.js; MidiCapture.noteName(midi).

Storage reads for the load path: Storage.listSessionsForPiece(db, pieceId), Storage.getPiece(db, pieceId) (record carries `model`), Storage.readRawEvents(db, sessionId), Storage.readClicks(db, sessionId); session fields startedPerf, endedPerf, endReason, bpmAtStart.

Scheduler facts (src/metronome.js lines 10-12, 65-69): clicks are recorded about SCHEDULE_AHEAD_S = 0.1 s before they sound, on LOOKAHEAD_MS = 25 ms ticks.

DOM contract asserted by scripts/check-paint.cjs: #analysisHeading (contains '<bpm> BPM', 'No session' with no session), #detail (exact sentences, default text), CaptureApp.analysis.pendingOrdinals, CaptureApp.analysis.session, notehead click via a bubbling MouseEvent on the svgMap element, fills on element.children[0], #status dataset.renderCount.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a wrong E4 in one marked pass is painted red on the notation and explained on click" — one path only, through align, aggregate, paint and the capture app</name>
  <precondition>C:\Program Files\Google\Chrome\Application\chrome.exe exists (or CHROME_EXE is set), the machine has internet access for the pinned CDN bundles, and node scripts/check-paint.cjs currently exits 1 (red harness from 03-01)</precondition>
  <reversibility rating="costly">The fixed-origin rule and the click-indexed expectedTime lookup implemented here are what Phase 4's timing verdicts and their fixtures are built on (CONTEXT D-01); changing them later re-derives every stored pass's expected timeline and rewrites those fixtures.</reversibility>
  <files>src/align.js, src/aggregate.js, src/paint.js, src/capture-app.js, src/score-renderer.js, index.html, scripts/check-run-path.cjs, test/align.test.cjs, test/aggregate.test.cjs</files>
  <read_first>
    - docs/analysis-rules.md in full (cost model, readings, tie rule, shapes, gap assessability, sentence templates, colours, tunables)
    - test/fixtures/align/*.json (all twelve; the tests load them by file name)
    - scripts/check-paint.cjs (the DOM ids, sentences and driving sequence the app must satisfy)
    - src/capture-app.js in full (the C state object, onClick 208-220, mark() 317-349, start() 439-508, endSession() 514-564, restore() 53-140, the piece-loaded listener 600-624, renderPassList() 299-313)
    - src/score-renderer.js (loadPiece 34-65, buildSvgMap 83-99, renderAndMap 148-167, the resize handler 169-177, the ScoreRenderer global at line 207)
    - src/pass-segmenter.js and test/pass-segmenter.test.cjs (module wrapper, immutability assertions at lines 100-107)
    - scripts/check-run-path.cjs lines 32-43 and 67-83 (EXPECTED_LOCAL_SCRIPTS and requiredMarkers)
    - .planning/phases/03-what-you-actually-played/03-RESEARCH.md "Pattern 5" and "Common Pitfalls" 1, 2, 5
  </read_first>
  <behavior>
    Write test/align.test.cjs and test/aggregate.test.cjs first (RED), then implement (GREEN). Header pattern: require('../src/score-model.js'), require('../src/align.js'), const Align = globalThis.Align; no jsdom.
    - For each of the twelve fixture files: Align.alignPass(f.scoreModel, f.pass, f.clicks, f.tunables || undefined) returns attempt, originIndex, bpm and wholeReason equal to f.expected, the sorted keys of result.notes deep-equal the sorted keys of f.expected.notes (review 03-01 MEDIUM round 3: no verdict escapes comparison), every f.expected.notes[id] status/playedPitch/reason equal, entryLag equal when f.expected.entryLag is present, and extras (gap + pitch, in seq order) deep-equal to f.expected.extras; one test per fixture named after its rule
    - rung1-late-first-note: C's deviationMs is 200; rung1-correction: E's deviationMs is 100; rung1-whole-beat-late: every note's deviationMs is 500
    - Worked costs from the contract via PassResult.totalCost (integer thousandths): rung1-wrong-e 3000; rung1-correction 3680; rung1-shifted-start 15000; the correction clone with E at 2300 (events still in time order) gives 5040, E played and extra F4 at before:2; the crossover with E at 2600 is built as its own chronological pass — 60@1000 seq 0, 62@1500 seq 1, 65@1900 seq 2, 65@2500 seq 3, 64@2600 seq 4, 67@3000 seq 5, never the correction with only E's timestamp moved (review 03-01 HIGH round 3) — and gives 6680, E wrong playedPitch 65, F played, extras exactly [{ gap: 'before:4', pitch: 64 }]
    - Decreasing timestamps (review 03-01 HIGH round 3): Align.tokenizePlayed on seq-ordered notes 64@2000 then 65@1940 gives two tokens and 64@2000 then 65@1960 gives one token with onsetTime 2000 (absolute window, first note-on in seq order); alignPass on rung 1 with 60@1000 62@1500 64@2000 65@1940 67@3000 (seq 0-4) gives all five played, F deviationMs −560, totalCost 3808; with F at 1960 it gives E played, F4 (m1-s1-v1-b3_1-p65) missed, extras exactly [{ gap: 'within:2', pitch: 65 }], totalCost 7000; in both, JSON.stringify(pass) is identical before and after and pass.notes keeps its seq order
    - Per-event deviation: a hand-built two-note slot (a synthetic 1-bar model with C4 and E4 at onset 0) played C4@1000 and E4@1030 gives deviationMs 0 and 30 (not 0 and 0)
    - Grace boundary: rung1-not-reached cloned with endTimeStamp 2750 and G unplayed (C D E F on the clicks) makes G missed; endTimeStamp 2749 makes G not-reached
    - Origin: rung1-origin-after-start with startTimeStamp 3499 resolves originIndex 5; 3501 resolves 10; Align.resolveOrigin(clicks, 99999) returns null
    - Align.expectedTime(clicks, 0, ScoreModel.rational(5, 2)) equals 2250; onset (0,1) returns 1000; an onset beyond the recorded clicks returns null
    - Align.tokenizePlayed: two note-ons 50 ms apart are one token, 51 ms apart are two; a re-struck pitch 10 ms later stays in the same token as a duplicate, and rung1-clean cloned with an extra 60@1010 gives C played (playedSeq of the first C) and one extra C4 at within:0 (D-07, D-09, review 03-03 HIGH)
    - Inputs are never mutated (JSON snapshot before/after); every PassResult survives JSON.parse(JSON.stringify()) deep-equal; Align.ANALYSIS_VERSION === 1; Align.REASONS deep-equals ['not-reached', 'restarted', 'ambiguous', 'tempo-changed']; DEFAULT_TUNABLES.weights.lateEntry === 2, DEFAULT_TUNABLES.lateEntryProbeTokens === 3, DEFAULT_TUNABLES.reachClock === 'origin' (the one test that changes if the user flips the reach clock); Align.resolveTunables({ weights: { lateEntry: 13 } }) keeps weights.substitution 3 and chordWindowMs 50 (partial tunables deep-merge)
    - Aggregate (hand-built PassResult objects): foldSession(model, []) → tempoGroups [] and viewForTempoGroup gives every note untested; a zero-note result lands in nonAttempts (D-14); bpm 120, 120, 100 give two groups ordered [120, 100] with attempts 2 and 1; bpm 120 and 121 give two groups; wholeReason 'tempo-changed' lands in tempoChanged and in no group; four results (wrong F4, wrong F4, missed, not-reached) give wrong 2, missed 1, assessed 3, unassessed {not-reached 1}, wrongPitches {65: 2}; a before:2 extra in two of three assessed passes gives passesWithExtra 2, totalExtras 2, assessedPasses 3, unassessedPasses 0
    - Gap invariant (review 03-01 HIGH round 1, MEDIUM round 2): a hand-built messy result — slot 2 unassessed ambiguous but reached, ambiguousPlayed E4 at gap before:2 and E4 at before:3, four extras at before:3 (69, 71, 69, 71) — gives before:3 assessedPasses 1, passesWithExtra 1, totalExtras 4, lowerBoundPasses 1; before:2 assessedPasses 0, unassessedPasses 1 (no certain extra, ambiguous note beside it); within:2 unassessedPasses 1; before:4 assessedPasses 1, lowerBoundPasses 0; a clean result folded with it keeps before:2 at assessedPasses 1 of 2 passes; a no-origin result with two extras at before:0 gives before:0 assessedPasses 0, unassessedPasses 1, passesWithExtra 0; a restarted result counts unassessedPasses 1 for every gap; and folding every fixture's PassResult together never yields lowerBoundPasses > passesWithExtra or passesWithExtra > assessedPasses for any gap
    - colourFor: wrong 2 / missed 1 → 'wrong'; wrong 1 / missed 1 → 'wrong' (tie); wrong 0 / missed 2 → 'missed'; assessed 3 no mistakes → 'clean'; assessed 0 → 'untested' (D-18)
    - describeNote session view reproduces the contract sentences exactly: 'E4, bar 1 beat 3: wrong in 4 of 10 assessed passes (played F4 three times, D#4 once); missed in 1 of 10; 2 passes unassessed (1 restarted, 1 not reached)', 'G4, bar 1 beat 5: missed in 1 of 2 assessed passes', 'C4, bar 1 beat 1: played correctly in all 3 assessed passes', 'F4, bar 1 beat 4: played correctly in all 4 assessed passes; 1 pass unassessed (1 not reached)', 'E4, bar 1 beat 3: not assessed in any pass (3 not reached)', 'E4, bar 1 beat 3: no passes yet'; pass view 'E4, bar 1 beat 3: wrong, played F4', ': missed', ': unassessed (not reached)', ': unassessed (ambiguous)', ': unassessed (tempo changed)'; describeGap 'Extra notes between D4 and E4: in 2 of 3 assessed passes (F4 twice)', with one unassessed pass '...; 1 pass unassessed', the messy fold 'Extra notes between E4 and F4: in 1 of 1 assessed passes (at least A4 twice, B4 twice); 1 pass with ambiguous notes nearby', pass view 'Extra notes between D4 and E4: F4' and the messy pass view 'Extra notes between E4 and F4: at least A4, B4, A4, B4 (ambiguous notes nearby)', both called with the pass source { kind: 'pass', result } holding the whole hand-built PassResult (review 03-04 HIGH round 3); the same messy PassResult with its ambiguousPlayed emptied and slot 2 relabelled played gives 'Extra notes between E4 and F4: A4, B4, A4, B4' (the qualifier comes from the result's ambiguity data, not from the extras); Aggregate.gapAssessment on the messy result gives before:3 { baseAssessable: true, ambiguityAdjacent: true, extras of pitches 69 71 69 71 }, before:2 { baseAssessable: true, ambiguityAdjacent: true, extras [] } and before:4 { ambiguityAdjacent: false }; a fractional onset prints 'beat 2.5'; headingFor gives 'This session at 120 BPM - 1 pass' and '... - 3 passes'
    - viewForTempoGroup gives noteKinds for every model note and glyphs { 'before:2': { count: 2, text: '+2' } }; viewForPass maps played → clean, wrong → wrong, missed → missed, unassessed → untested and glyph text '+'
  </behavior>
  <action>
Implement, in this order, keeping every module a classic script with the one-global wrapper and the module.exports tail (PATTERNS "Shared Patterns"):

1. `src/align.js` → `globalThis.Align`. Header comment: pure; plain data in and out; never mutates inputs. Exports `ANALYSIS_VERSION` (1), `REASONS`, `DEFAULT_TUNABLES` (the contract's table, weights nested, `weights.lateEntry` 2, `lateEntryProbeTokens` 3, and the policy switch `reachClock: 'origin'` with a comment naming it the one place the pending user decision on D-11 reach is applied), `resolveTunables(partial)` (null/undefined → the defaults; otherwise a deep merge of the partial object over DEFAULT_TUNABLES, weights merged key by key; alignPass and isFinal call it first), `resolveOrigin(clicks, passStartTimeStamp)` (first click with accent true and pageTime ≥ start; none → null), `expectedTime(clicks, originIndex, onsetRational)` (whole = floor(num/den), frac from the integers; before = clicks[originIndex + whole], after = the next click; null when a needed click is missing; before.pageTime when frac is 0, linear interpolation otherwise), `localBeatMs(clicks, index)` (the gap from click index to the next recorded click, the previous gap when there is no next click), `buildSlots(model)` (absolute onset = ScoreModel.addRationals(measure.start, note.onset); a new slot whenever ScoreModel.compareRationals says the onset differs; slot = { index, onset, noteIds, pitches sorted MIDI, notes }), `tokenizePlayed(notes, chordWindowMs)` (seq order, never re-sorted by timeStamp; a note joins the open token when Math.abs(timeStamp − token.onsetTime) ≤ window — the absolute difference, so a later-seq note stamped more than the window before the open token starts its own token (review 03-01 HIGH round 3) — a pitch already present joining as a duplicate; token.onsetTime is the first note-on in seq order; token = { onsetTime, notes, pitches (unique, sorted), duplicates }; comment the decreasing-timestamp rule and that the raw notes are never copied out of order or modified), `pairCost(slot, token, expected, beatMs, tunables)` (integer thousandths: equal pitches paired one-to-one as a multiset → played; remaining slot and token pitches paired by the minimum total semitone distance assignment → substitution × 1000 each; leftover slot pitches deletion × 1000 each; leftover token pitches and duplicates insertion × 1000 each; timing Math.round(perBeat × 1000 × |token.onsetTime − expected| / beatMs); returns { cost, pairs, missed, extraNotes, timingCost }; leave a comment naming the seam where plan 03-08 reports equal-distance alternative assignments as ambiguous), `alignAtLag(model, pass, clicks, lag, tunables)` (the full in-reading computation: expected times from clicks at originIndex + lag for timing cost and gap keys; reach = the reach-clock time is not null and ≤ end + markGraceBeats × localBeatMs(clicks, b) with b the last click at or before the end (the origin index when none), where the reach-clock time is the lag-0 expected time when `tunables.reachClock === 'origin'` and this reading's time when `'reading'` — identical at lag 0, so this plan cannot tell them apart and 03-03 tests both; forward DP table F over reached slots × tokens with back-pointers preferring pair > delete > insert, and the backward table B kept for plan 03-03's co-optimal detection — comment the seam; returns labels, extras with gap keys from this reading's expected times, and cost), `alignPass(model, pass, clicks, tunables = DEFAULT_TUNABLES)` and `analyzeSession(model, passes, clicks, tunables)`. Inside alignPass: attempt = pass.notes.length > 0 (false → the empty-shaped result with originIndex/bpm resolved); end = pass.endTimeStamp ?? last note timeStamp; originIndex via resolveOrigin (null → every note unassessed not-reached, every note an extra at before:0, bpm from the last click before the start or null, entryLag 0); bpm = clicks[originIndex].bpm; call alignAtLag with lag 0 only and leave a comment naming the seam where plan 03-03 evaluates lags 1..H; entryLag 0; totalCost = the reading's cost; played notes get playedSeq and deviationMs = that note's own timeStamp − the lag-0 expected time (never the token onset); wrong notes get playedPitch and playedSeq; deleted slot notes missed; inserted tokens' notes extras at `before:k`; leftover pitches of paired tokens extras at `within:k`; unreached slots unassessed not-reached; wholeReason null and ambiguousPlayed [] (entries, once 03-03 fills them, carry `{ seq, pitch, timeStamp, gap }` with gap computed exactly like an extra's). Every onset operation through ScoreModel.compareRationals / addRationals, never `.beats` for comparison. No reference to any browser global (the DOM, Web MIDI, Web Audio, the window object) anywhere in the file, comments included.

2. `src/aggregate.js` → `globalThis.Aggregate`. `foldSession(model, passResults)` (attempt false → nonAttempts; wholeReason tempo-changed → tempoChanged; bpm null → ungrouped; the rest grouped by bpm in first-appearance order; NoteCounts initialised to zeros for every model note; GapCounts `{ passesWithExtra, totalExtras, assessedPasses, unassessedPasses, lowerBoundPasses, pitches }` per gap key using the contract's rule — base-assessable when wholeReason is null and slot k−1 (slot 0 for k = 0) is reached for before:k, or slot k is assessed for within:k; ambiguity-adjacent when an ambiguousPlayed entry has that gap or a bracketing slot is unassessed ambiguous; not base-assessable → unassessedPasses; certain extras present → assessedPasses, passesWithExtra, totalExtras, pitches, plus lowerBoundPasses when ambiguity-adjacent; no certain extra and ambiguity-adjacent → unassessedPasses; otherwise assessedPasses; gap keys enumerated as before:0..before:S and within:0..within:S−1 for every slot), `colourFor`, `viewForTempoGroup(model, aggregate, bpm)`, `viewForPass(model, passResult)`, `describeNote(model, noteId, source, { noteName })` with source `{ kind: 'session', counts }` (counts null → 'no passes yet') or `{ kind: 'pass', result }`, `gapAssessment(result, gapKey)` (review 03-04 HIGH round 3: the single implementation of the contract's per-pass gap rule — returns `{ baseAssessable, extras, ambiguityAdjacent }` with `extras` the result's confirmed extras at that gap in seq order and `ambiguityAdjacent` from `result.ambiguousPlayed` gap keys and the bracketing slots' ambiguous labels; foldSession calls it for every gap of every attempt), `describeGap(model, gapKey, source, { noteName })` with source `{ kind: 'session', gap: GapCounts }` or `{ kind: 'pass', result }` — the whole PassResult, never a pre-filtered extras list — whose pass branch lists `gapAssessment(result, gapKey).extras` and adds the `at least … (ambiguous notes nearby)` form when `ambiguityAdjacent` is true, `headingFor(aggregate, view, { pendingOrdinals })`, `wordCount(n)`. Sentences, pluralization and slot naming exactly per the contract. Pure: no browser global; noteName passed in.

3. `src/paint.js` → `globalThis.Paint`: `COLOURS` ({ wrong: '#d00000', missed: '#0050d0', clean: '#000000', untested: '#b0b0b0', extra: '#d00000' }), `paintNotes(svgMap, noteKinds)` (fill on every child of each mapped element; skip missing ids), `resetAll(svgMap)`, `clearExtras(overlayEl)` (replaceChildren), `paintExtras(overlayEl, svgMap, model, glyphs, onGlyphClick)` (clears and returns in this plan; plan 03-04 positions glyphs). No top-level access to the document. Reads svgMap only.

4. `src/score-renderer.js`: at the end of `renderAndMap()`, dispatch `new CustomEvent('piece-rendered', { detail: { renderCount: S.renderCount } })` on the document. Nothing else changes.

5. `index.html`: wrap `#notation` in `<div id="scoreWrap">` (position relative) with sibling `<div id="markOverlay">` (absolute, inset 0, pointer-events none; children pointer-events auto); above the score `<section id="analysis">` with `<button type="button" id="analysisHeading">No session</button>` and `<select id="tempoGroup" hidden></select>`; below the score `<p id="detail">Click a coloured notehead or a + to see what happened there.</p>`; CSS for `.extraGlyph` (absolute, red, bold, cursor pointer) and `#passList li.selected` (bold). Add `src/align.js`, `src/aggregate.js`, `src/paint.js` script tags after `src/storage.js` and before `src/capture-app.js`. `scripts/check-run-path.cjs`: same three paths at the same position in EXPECTED_LOCAL_SCRIPTS; add `id="analysisHeading"`, `id="tempoGroup"`, `id="markOverlay"`, `id="detail"` to requiredMarkers.

6. `src/capture-app.js`: add `A` next to `C` — `{ session: null, results: [], resultCache: new Map(), aggregate: null, view: { kind: 'session', bpm: null, ordinal: null }, pendingOrdinals: [], detail: null, loadGen: 0 }` where a session is `{ id, pieceId, model, live, passes, clicks, bpmAtStart }`. Functions: `completedPasses()` (live: PassSegmenter.segment(C.events, { sessionStartTimeStamp: C.session.startedPerf, includeEmptyTrailing: true }) without its last element; otherwise A.session.passes); `analyzeCurrent()` (A.results = Align.analyzeSession(A.session.model, completedPasses(), A.session.clicks); A.aggregate = Aggregate.foldSession(A.session.model, A.results); pick A.view.bpm as the first group's bpm when unset or absent, else bpmAtStart; renderAnalysis()); `renderAnalysis()` (no session, no model or no svgMap → Paint.resetAll when a map exists, clearExtras, heading `No session`; otherwise build the PaintView, Paint.resetAll then Paint.paintNotes on ScoreRenderer.state.svgMap, Paint.clearExtras/paintExtras on `$('markOverlay')`, heading from Aggregate.headingFor, #tempoGroup options `${bpm} BPM (${attempts} passes)` hidden when groups ≤ 1, then `refreshDetail()`); `refreshDetail()` (A.detail null → the default text; a note → describeNote for the current view; a gap → describeGap when the view has that glyph, else the default text); `loadLatestSession(pieceId, model)` (sessions via Storage.listSessionsForPiece sorted by id, take the last; events, passes via PassSegmenter.segment(events, { sessionStartTimeStamp: s.startedPerf, sessionEndTimeStamp: s.endedPerf, includeEmptyTrailing: false }), clicks via Storage.readClicks; A.session = { id, pieceId, model, live: false, passes, clicks, bpmAtStart }; analyzeCurrent(); none → A.session = null, A.detail = null, renderAnalysis()). Hooks: in `mark()` right after `renderPassList()` → analyzeCurrent(); in `start()`, right after its guards and before its first await, `C.sessionModel = ScoreRenderer.state.model`, and `timeSignatureFor(bar)` reads `C.sessionModel.measures` instead of the measures of the renderer's current model (review 03-02 HIGH round 3: a click callback must never read the model on screen, which a failed load sets to null); after `renderPassList()` → A.session = { id, pieceId: C.pieceId, model: C.sessionModel, live: true, passes: null, clicks: C.clicks, bpmAtStart: bpm }, A.view = { kind: 'session', bpm, ordinal: null }, A.detail = null, analyzeCurrent(); in `endSession()` after the final pass list → A.session = { ...A.session, live: false, passes: finalPasses, clicks: C.clicks.slice() } and analyzeCurrent(); at the end of `restore()` inside the try → `if (C.pieceId && ScoreRenderer.state.model) await loadLatestSession(C.pieceId, ScoreRenderer.state.model)`; in the `piece-loaded` listener after `C.pieceId = id` → `if (!C.session) await loadLatestSession(id, ev.detail.model)`; the `piece-unloaded` listener is rewritten in Task 2 step 3 (for this task: renderAnalysis()). Listeners: `document.addEventListener('piece-rendered', () => renderAnalysis())`; a click listener on `$('notation')` walking up to the nearest `vf-notehead` element, finding its noteId by scanning ScoreRenderer.state.svgMap, setting A.detail = { type: 'note', noteId } and calling refreshDetail() — ignore clicks on no mapped notehead. Expose `analysis: A`, `analyzeCurrent`, `loadLatestSession` on the returned CaptureApp object. Pass-list clicks, glyph clicks and the tempoGroup change handler are plan 03-04.

7. Run the engine tests until green, then `node scripts/check-run-path.cjs`, then `node scripts/check-paint.cjs`: this task is done when it prints `OK paint: wrong E4 painted red on the notehead child after a mark` (it may still stop with exit 1 at the horizon step, which Task 2 closes). If a weight or window must change to satisfy a fixture, change DEFAULT_TUNABLES and record the fixture name and value in the docs/analysis-rules.md tunables table (that file is otherwise untouched by this plan).
  </action>
  <verify>
    <automated>node --test test/align.test.cjs test/aggregate.test.cjs test/align-fixtures.test.cjs</automated>
    <fails_when>non-zero exit (node --test exits 1 when any test fails or a file cannot load), or the summary shows a non-zero fail count or a pass count of 0 — the default spec reporter prints these as "ℹ fail N" and "ℹ pass N", never "# fail"</fails_when>
    <automated>node scripts/check-run-path.cjs</automated>
    <fails_when>non-zero exit, any line starting "FAIL:", or the summary line not ending in "0 failed"</fails_when>
    <automated>node scripts/check-paint.cjs | grep -c 'OK paint: wrong E4 painted red on the notehead child after a mark'</automated>
    <fails_when>prints 0 (the tracer's one path did not paint E4 red and explain it), or the harness exited 2 (environment problem) before reaching the paint step</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test --test-reporter=tap test/align.test.cjs` exits 0 and its summary shows `# fail 0` and `# pass N` with N at least 20; `node --test --test-reporter=tap test/aggregate.test.cjs` exits 0 and shows `# fail 0` and `# pass N` with N at least 15 (TAP selected explicitly — the default reporter prints `ℹ fail 0`, review 03-05 MEDIUM round 3)
    - `grep -c 'Math.abs' src/align.js` prints at least 1 and `grep -c '1940' test/align.test.cjs` prints at least 1 and `grep -c '2600' test/align.test.cjs` prints at least 1 (absolute chord window, decreasing-timestamp and chronological crossover tests — review 03-01 HIGH round 3)
    - `grep -c 'function gapAssessment' src/aggregate.js` prints 1 and `grep -c 'gapAssessment(' src/aggregate.js` prints at least 3 (definition, foldSession, describeGap — review 03-04 HIGH round 3)
    - `node scripts/check-paint.cjs` prints `OK paint: wrong E4 painted red on the notehead child after a mark`; `git diff --quiet -- scripts/check-paint.cjs` exits 0 (the harness is not edited)
    - `grep -vE '^\s*(/\*|\*|//)' src/align.js | grep -cE 'document\.[a-zA-Z]|navigator\.[a-zA-Z]|new AudioContext|\bwindow\.[a-zA-Z]|requestMIDIAccess'` prints 0; the same command over src/aggregate.js prints 0 (ANLZ-03)
    - `grep -c 'ANALYSIS_VERSION' src/align.js` prints at least 1; `grep -c "'not-reached', 'restarted', 'ambiguous', 'tempo-changed'" src/align.js` prints 1; `grep -c 'lateEntry' src/align.js` prints at least 1; `grep -c 'function alignAtLag' src/align.js` prints 1
    - `grep -c 'ScoreModel.compareRationals' src/align.js` prints at least 1 and `grep -c 'ScoreModel.addRationals' src/align.js` prints at least 1; `grep -c '\.beats *[<>=]' src/align.js` prints 0
    - `grep -c 'unassessedPasses' src/aggregate.js` prints at least 1; `grep -c 'lowerBoundPasses' src/aggregate.js` prints at least 1; `grep -c 'at least ' src/aggregate.js` prints at least 1
    - `grep -c "reachClock" src/align.js` prints at least 2 (the default and the one reach computation that reads it); `grep -c 'function resolveTunables' src/align.js` prints 1
    - `grep -c "piece-rendered" src/score-renderer.js` prints 1 and `grep -c "piece-rendered" src/capture-app.js` prints at least 1
    - `grep -c 'Align.analyzeSession' src/capture-app.js` prints at least 1; `grep -c 'analyzeCurrent()' src/capture-app.js` prints at least 4
    - `grep -c "setAttribute('fill'" src/paint.js` prints at least 1 and `grep -c 'children' src/paint.js` prints at least 1
    - `node scripts/check-run-path.cjs` prints `ok: local script tags in dependency order` and `ok: index.html contains id="detail"`; `npm test` exits 0
  </acceptance_criteria>
  <done>The single happy path works end to end and is committed: a marked C D F F G pass paints E4 red on the real notation and clicking it shows the contract sentence; all twelve fixtures and the worked costs are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the path trustworthy live — judge a pass only once the click timeline covers it (stable fields), bind each session to its own score across piece switches and same-file reopens, reuse final results, regenerate the detail panel</name>
  <precondition>Task 1 is committed and node scripts/check-paint.cjs prints its first OK line</precondition>
  <files>src/align.js, src/capture-app.js, test/align.test.cjs</files>
  <read_first>
    - docs/analysis-rules.md sections "When a pass is judged (finalization)" and the D-11 / D-20 finalization example
    - src/metronome.js lines 10-12 and 65-69 (SCHEDULE_AHEAD_S, LOOKAHEAD_MS, tick) — why a click needed by a just-marked pass may not be recorded yet
    - src/score-renderer.js loadPiece() lines 34-65 (S.model is replaced and renderAndMap runs before piece-loaded is dispatched)
    - src/capture-app.js as left by Task 1 (A, analyzeCurrent, renderAnalysis, refreshDetail, loadLatestSession, onClick 208-220, the piece-loaded listener 600-624)
    - scripts/check-paint.cjs groups 4, 6b, 7, 8, 8b and 8c (horizon pass, same-file reopen, piece switch, reload comparison, Start during a delayed restoration, unreadable file during capture)
    - src/capture-app.js start() lines 439-508 (the awaits at resume, getSetting and createSession before C.session is set), timeSignatureFor lines 203-206 and the piece-unloaded listener lines 626-628; src/score-renderer.js loadPiece's catch block lines 51-63 (model, svgMap and notation cleared, then piece-unloaded dispatched synchronously)
    - src/capture-app.js lines 600-624 (the piece-loaded listener: it hashes the file and ends a live session only when the hash differs from C.pieceId, so a same-hash reload keeps the session) and src/score-renderer.js lines 34-50 (loadPiece always builds a fresh S.model and renders before dispatching piece-loaded)
  </read_first>
  <behavior>
    - Align.isFinal(pass, clicks, sessionEnded, tunables): sessionEnded true → true; endTimeStamp null → false; no accented click at or after pass.startTimeStamp → false; otherwise true exactly when the last click's pageTime ≥ end + (markGraceBeats + 1) × localBeatMs(clicks, b), where b is the index of the last click at or before the end (the origin index when none) — never the last click's own gap, which a truncated timeline would distort. Boundary on the rung-1 timeline: rung1-not-reached's pass (end 2200) with clicks through 2500 is false (needs 2950), through 3000 is true; a timeline 1000, 1500, 2000, 2500 plus a last click at 2949 is false and at 2950 is true
    - The review's case, corrected against the reach formula (review 03-02 HIGH round 2): rung 1 C D E F on the clicks, G unplayed, end 2750, grace 250. Clicks through 2500 (what is recorded live at the mark: G's click at 3000 does not exist yet) → isFinal false and alignPass gives G not-reached — the wrong verdict finalization exists to withhold. Clicks through 3000 → isFinal false and alignPass gives G missed (G's expected time 3000 exists and 3000 ≤ 3000). Clicks through 3500 → isFinal true and G missed; all fifteen clicks → G missed
    - Align.stableFields(result) returns a deep copy of the PassResult with the expectedTime key removed from every slot whose reached is false and every other field untouched; it never mutates its input
    - Final-prefix property (review 03-02 HIGH round 2): for every fixture file and every k from 1 to clicks.length, when isFinal(f.pass, clicks.slice(0, k), false, f.tunables) is true, stableFields(alignPass on that prefix) deep-equals stableFields(alignPass on all clicks), and every reached slot's expectedTime is equal in both; each of the twelve rung-1 fixtures has at least one final prefix
    - The tail case the review named: a pass start 3600, one note 60 at 3700, end 3800 on the rung-1 timeline is not final while the prefix lacks the accented click at 6000; on the prefix ending at 6000 it is final, its slots[1].expectedTime is null while the full timeline gives 6500, the whole results are not deep-equal, and their stableFields are
  </behavior>
  <action>
1. `src/align.js`: add and export `isFinal(pass, clicks, sessionEnded, tunables)` and `stableFields(result)` exactly per the behavior block and the contract section "When a pass is judged". Add the behavior tests to test/align.test.cjs first; they must fail before the functions exist. Do not make the property pass by nulling expectedTime inside alignPass; the reported expected times stay what the recorded clicks give.

2. `src/capture-app.js` — finalization (review 03-02 HIGH): in `analyzeCurrent()`, sessionEnded = !A.session.live; split completedPasses() into final passes (Align.isFinal) and pending ones; A.pendingOrdinals = the pending ordinals in order; analyse and fold only the final passes. Result reuse (review 03-03 MEDIUM round 2): for each final pass, take `A.resultCache.get(ordinal)` when present, otherwise `Align.alignPass(A.session.model, pass, A.session.clicks)` and store it; A.results = the final results in ordinal order; `A.resultCache` is replaced by a new Map whenever A.session is assigned a different session id, whenever the model is rebound (step 3) and when A.session becomes null. `endSession()` re-analyses with live false; passes already cached stay cached (their stableFields cannot change) and only the newly final ones are aligned. In `onClick(click)`, after pushing the record, `if (A.session && A.session.live && A.pendingOrdinals.length > 0) analyzeCurrent()` so a pending pass is judged on the first click that covers it. Pass the pending ordinals to Aggregate.headingFor so the heading reads `... - finishing pass N` while one is pending. `endSession()` already re-analyses with live false, which finalizes every remaining pass against the clicks actually recorded, exactly what a reload computes.

3. `src/capture-app.js` — one score per session (review 03-02 HIGH): `analyzeCurrent()` uses only `A.session.model` (never ScoreRenderer.state.model for an existing session). `renderAnalysis()` treats `A.session.model !== ScoreRenderer.state.model` like no session for painting: Paint.resetAll on the current map, Paint.clearExtras, heading `No session`, A.detail = null, default detail text — so the piece-rendered event fired inside loadPiece() for a new piece never paints the old session's marks. `loadLatestSession(pieceId, model)` takes `const gen = ++A.loadGen` at its start and returns without touching A after any await when `gen !== A.loadGen`; the `piece-loaded` listener increments A.loadGen at its very start (before awaiting the hash) and `piece-unloaded` increments it too, so an in-flight load for a previous piece can never overwrite the current one. `endSession('piece-change')` keeps the ended session's own model, so its analysis is against the score it was recorded on even though the screen already shows the new piece.

   Same-file reopen (review 03-02 HIGH round 2, verified: loadPiece always creates a new model object and the listener keeps a live session when the hash is unchanged): in the `piece-loaded` listener, capture `const gen = ++A.loadGen` at its start; right after `const id = await Storage.hashBytes(bytes)`, `if (gen !== A.loadGen) return`; then, when `A.session && A.session.pieceId === id`, rebind `A.session = { ...A.session, model: ev.detail.model }`, reset `A.resultCache` and call analyzeCurrent() — a same-hash file yields the same structural note ids (Phase 1 D-09), so the rebound analysis equals the old one and painting resumes on the new svgMap. The existing `if (C.session && id !== C.pieceId) await endSession('piece-change')` and `if (!C.session) await loadLatestSession(id, ev.detail.model)` paths are unchanged. Keep the identity guard in renderAnalysis: it is what hides the old paint during the brief window between piece-rendered and the hash check.

   Session ownership across Start and End (review 03-02 HIGH round 3, verified: start() awaits `C.audio.resume()`, `Storage.getSetting` and `Storage.createSession` before `C.session` exists, and nothing invalidated a loadLatestSession already past its first await): `start()` executes `A.loadGen++` right after its guards, before its first await; `endSession()` executes `A.loadGen++` before its first await. `loadLatestSession` re-checks after every await `if (gen !== A.loadGen || C.session) return;` — a live session always owns A — and assigns `A.session` only after that check passes; when it does assign, it sets `A.detail = null` if the session id changed. Group 8b of the harness drives exactly this race.

   Failed piece load during capture (review 03-02 HIGH round 3, verified: loadPiece's catch sets `S.model = null` and dispatches `piece-unloaded` synchronously; the old listener only cleared `C.pieceId` while the metronome kept running): rewrite the `piece-unloaded` listener so its synchronous prefix is `A.loadGen++; const ending = C.session ? endSession('piece-unloaded') : null; C.pieceId = null;` — endSession's own synchronous prefix stops the metronome and resample timer before any await, and timeSignatureFor already reads `C.sessionModel` (Task 1), so no click callback can touch the null model — then `await ending` inside a try that toasts errors, then `renderAnalysis()`. The ended session keeps `A.session` = its own model, final passes and clicks with `live: false`, so it stays retained in memory (nothing is painted while no score is on screen: the identity guard sees `ScoreRenderer.state.model === null`, heading `No session`); its raw events, passes and clicks were flushed to storage by endSession, so reopening the same piece takes the existing `if (!C.session) await loadLatestSession(id, ev.detail.model)` path and paints it again, and opening any other piece shows that piece's latest session instead. `endReason` `'piece-unloaded'` is a new free-text value next to `'stop'`, `'piece-change'` and `'reopen'` (src/storage.js stores it as given). Group 8c of the harness drives an unreadable file during capture.

4. `src/capture-app.js` — detail regeneration (review 03-04 MEDIUM): confirm renderAnalysis() ends with refreshDetail() on every path (mark, finalizing click, endSession, load, resize via piece-rendered) and that a session change (start, loadLatestSession to a different session id, no session) sets A.detail = null.

5. Run `node --test test/align.test.cjs`, then `node scripts/check-paint.cjs` until it prints all nine OK lines unchanged (including `OK paint: reopening the same file during capture keeps painting the live session`, `OK paint: starting a session during a delayed restoration keeps the live session on screen` and `OK paint: an unreadable file during capture ends the session cleanly and its passes return with the piece`), then `node scripts/check-svg-map.cjs` and `node scripts/check-capture-roundtrip.cjs` to prove the new DOM and hooks did not disturb Phase 1 and 2 behaviour.
  </action>
  <verify>
    <automated>node --test test/align.test.cjs test/aggregate.test.cjs test/align-fixtures.test.cjs</automated>
    <fails_when>non-zero exit (node --test exits 1 when any test fails or a file cannot load), or the summary shows a non-zero fail count or a pass count of 0 — the default spec reporter prints these as "ℹ fail N" and "ℹ pass N", never "# fail"</fails_when>
    <automated>node scripts/check-paint.cjs</automated>
    <fails_when>non-zero exit; any line starting "FAIL paint:"; fewer than nine lines starting "OK paint:"</fails_when>
    <automated>node scripts/check-svg-map.cjs</automated>
    <fails_when>non-zero exit; any "FAIL fixtures/..." line; fewer than six "OK fixtures/" lines</fails_when>
    <automated>node scripts/check-capture-roundtrip.cjs</automated>
    <fails_when>non-zero exit; output lacks either OK line ("OK capture round-trip: 8 raw events restored unmodified" prefix, "OK capture round-trip: pass split 3/2/1/1 restored after reload mid-session"); any line starting "FAIL capture round-trip"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function isFinal' src/align.js` prints 1; `grep -c 'function stableFields' src/align.js` prints 1; `grep -c 'Align.isFinal' src/capture-app.js` prints at least 1
    - `grep -c 'stableFields' test/align.test.cjs` prints at least 2 (the property and the tail case)
    - `grep -c 'pendingOrdinals' src/capture-app.js` prints at least 3 (state, analyzeCurrent, onClick)
    - `grep -c 'resultCache' src/capture-app.js` prints at least 3 (state, read, reset)
    - `grep -c 'loadGen' src/capture-app.js` prints at least 5 (state, capture in loadLatestSession, comparison, listener capture, listener comparison)
    - `grep -c 'A.session.model' src/capture-app.js` prints at least 2 (analysis and the render guard) and `grep -c 'model: ev.detail.model' src/capture-app.js` prints at least 1 (the same-hash rebind)
    - `grep -c 'refreshDetail()' src/capture-app.js` prints at least 2
    - `node --test --test-reporter=tap test/align.test.cjs` exits 0 and its summary shows `# fail 0` and a `# pass N` at least 5 more than after Task 1
    - Session ownership (review 03-02 HIGH round 3): `grep -cE '\+\+A\.loadGen|A\.loadGen\+\+' src/capture-app.js` prints at least 5 (loadLatestSession, piece-loaded, piece-unloaded, start, endSession) and `grep -c 'C.sessionModel' src/capture-app.js` prints at least 3; `grep -c 'ScoreRenderer.state.model.measures' src/capture-app.js` prints 0; `grep -c "endSession('piece-unloaded')" src/capture-app.js` prints 1
    - `node scripts/check-paint.cjs` exits 0 and prints the nine `OK paint:` lines from 03-01 Task 3; `git diff --quiet -- scripts/check-paint.cjs` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>A pass marked before its covering click exists is shown as finishing and then judged once, identically to a reload; switching pieces never paints or analyses one piece's session against another, and reopening the same file during capture keeps painting; Start during a restoration keeps the live session, and an unreadable file during capture ends it cleanly with its passes kept; final results are reused instead of re-aligned on every click; the detail panel always describes what is on screen; Phase 1 and 2 checks still pass.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `src/align.js` — `globalThis.Align`: `ANALYSIS_VERSION`, `REASONS`, `DEFAULT_TUNABLES` (with `weights.lateEntry`, `lateEntryProbeTokens` and the policy switch `reachClock: 'origin'`), `resolveTunables`, `resolveOrigin`, `expectedTime`, `localBeatMs`, `buildSlots`, `tokenizePlayed` (duplicates), `pairCost`, `alignAtLag`, `alignPass` (PassResult with `entryLag`, `totalCost`, per-event `deviationMs`), `analyzeSession`, `isFinal`, `stableFields`
- `src/aggregate.js` — `globalThis.Aggregate`: `foldSession` (GapCounts with `unassessedPasses` and `lowerBoundPasses`), `gapAssessment(result, gapKey)` → `{ baseAssessable, extras, ambiguityAdjacent }` (shared by the fold and the pass-view sentence), `colourFor`, `viewForTempoGroup`, `viewForPass`, `describeNote`, `describeGap`, `headingFor`, `wordCount`
- `src/paint.js` — `globalThis.Paint`: `COLOURS`, `paintNotes`, `resetAll`, `clearExtras`, `paintExtras` (signature; positioning in 03-04)
- `src/capture-app.js` — analysis state `A` (`session` with `pieceId` and `model`, `results`, `resultCache`, `aggregate`, `view`, `pendingOrdinals`, `detail`, `loadGen`), `completedPasses`, `analyzeCurrent`, `renderAnalysis`, `refreshDetail`, `loadLatestSession(pieceId, model)`, the same-hash model rebind in the piece-loaded listener, `A.loadGen` advanced by `start()` and `endSession()`, the `piece-unloaded` listener that ends capture with `endReason 'piece-unloaded'` and retains the ended session, `C.sessionModel` read by `timeSignatureFor`, the onClick finalization hook, the notation click delegation, `CaptureApp.analysis` / `CaptureApp.analyzeCurrent` / `CaptureApp.loadLatestSession`
- `src/score-renderer.js` — the `piece-rendered` CustomEvent (detail.renderCount)
- `index.html` — `#analysis` (`#analysisHeading`, `#tempoGroup`), `#scoreWrap` + `#markOverlay`, `#detail`, three new script tags; `scripts/check-run-path.cjs` — thirteen-script order and four new markers
- `test/align.test.cjs`, `test/aggregate.test.cjs`

## Flagged assumptions (not silently dropped)

- ANLZ-03 (unclassified probe): closed operationally by the plain-node tests plus the comment-stripped negative grep above; remains flagged as manually classified.
- The DP's deterministic back-pointer preference (pair > delete > insert) and the in-chord minimum-distance choice are placeholders for ordering only; genuine ties become `ambiguous` in 03-03 (alignment and lag ties) and 03-08 (in-chord ties, rung 4) — no fixture in this plan has a tie.
- Reach clock (review 03-01 HIGH round 2, user decision pending): `reachClock` exists from this plan with the default `'origin'` (D-11 as written); both clocks agree at lag 0, which is all this plan runs. The user decides at the rung-1 piano gate (03-05); see the 03-01 flagged assumptions.
- Only the in-time reading (lag 0) runs in this plan; rung1-whole-beat-late passes here because five notes separate in time alone (17 vs 19). The length-independent late-entry reading lands in 03-03 with its own fixtures.
- Research A1 (glyph positioning) is deferred to 03-04; `paintExtras` exists as a signature so no capture-app call site changes later.
- Finalization delays a pass's marks until the metronome has recorded the covering click. That is more than a beat and a half after the mark, and more when the mark came before the pass's bar-1 click. No fixed bound is promised. Accepted for honesty (review 03-02 HIGH; wording corrected per review 03-05 MEDIUM round 3); the piano checklists tell the user to watch the `(finishing)` label.
- A failed piece load ends the live session (review 03-02 HIGH round 3). Continuing capture without a score would record passes that could never be analysed against anything on screen, and the user's next action is to reopen a readable file anyway. The attempts are not lost: they are stored and repaint when their piece is reopened.

<verification>
- Engine tests green under plain node; check-run-path green with thirteen scripts; check-paint prints nine OK lines; check-svg-map and check-capture-roundtrip still green
</verification>

<success_criteria>
- ROADMAP Phase 3 success criteria 1 (fixture tests with no DOM), 2 (single-pass classification on rung 1, no false marks on the clean pass) and 3 (marks on the noteheads, not a list) hold on rung 1 at the desk, with the live result equal to the replayed one
- Nothing in Phase 1 or 2 regressed
</success_criteria>

<output>
Create `.planning/phases/03-what-you-actually-played/03-02-SUMMARY.md` when done, quoting the test counts, the nine OK paint lines, and any tunable that changed with the fixture that forced it.
</output>

## Review Dispositions Ledger

### Round 1 — af0835b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-02: live and replayed results disagree because clicks are recorded only ~100 ms ahead; harness waits for ten clicks and hides it | HIGH | Plan 03-02, Task 2: verified in src/metronome.js (SCHEDULE_AHEAD_S 0.1, LOOKAHEAD_MS 25); `Align.isFinal` plus `A.pendingOrdinals` and an onClick re-analysis judge a pass only once covered; final-prefix property test over every fixture; the 03-01 harness marks at the horizon edge and compares reload fills with live fills |
| 03-02: piece switching analyses the old session against the new model and exposes old paint to the new map | HIGH | Plan 03-02, Task 2: verified in src/score-renderer.js loadPiece() and the piece-loaded listener; sessions carry their own model and pieceId, the render guard resets paint on a model mismatch, a load generation counter discards stale async loads; harness step 7 switches to rung 3 (shared note id) and back |
| 03-02: per-note deviations use the chord token's first onset | MEDIUM | Plan 03-02, Task 1: deviationMs from each matched event's own timeStamp; a two-note slot played 30 ms apart must report 0 and 30 |
| 03-01: extra-note denominators vs confident extras in unassessed gaps | HIGH | Plan 03-02, Task 1: GapCounts implements the contract's assessability rule with `unassessedPasses`; ambiguous-neighbour, no-origin and restarted folds plus an all-fixture invariant test |
| 03-01: worked cost numbers inconsistent | MEDIUM | Plan 03-02, Task 1: PassResult.totalCost asserted for each reconciled worked example (3000, 3680, 15000, 5040, 6680) |
| 03-03: re-strike inside a chord fragments tokens | HIGH | Plan 03-02, Task 1: tokenizer keeps a re-struck pitch in the open token as a duplicate extra within the slot (rung-1 test here; chord fixture in Plan 03-06) |
| 03-04: detail text goes stale | MEDIUM | Plan 03-02, Tasks 1-2: `A.detail` remembered target and `refreshDetail()` on every repaint; harness asserts regenerated sentences after passes 2 and 3 (views extended in Plan 03-04) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-03: in-chord equidistant ties resolved by pitch order | Not deferred from the phase — addressed in Plan 03-06 with the rung-4 chord work; this plan only leaves a commented seam and has no chord fixture |
| 03-01: late entry on long passages | Not deferred from the phase — the lag readings are implemented in Plan 03-03, Task 2; this plan builds `alignAtLag` with lag 0 as the seam |

### Round 2 — 58daf59

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-02: final-prefix deep equality is false — tail slot times are null in the prefix and filled in later (start 3600 / end 3800) (L55@58daf59) | HIGH | Plan 03-02, Task 2: verified (D's expectedTime is null on the prefix ending at 6000 and 6500 on the full timeline). The property now compares `Align.stableFields` (expectedTime removed from unreached slots only) and also asserts every reached slot's expectedTime is equal. The tail case is a named test showing the whole results differ while their stable fields match. Must_haves truth 4. The contract's reasoning is in Plan 03-01, Task 1, section 5. Supersedes Round 1's "deep-equals alignPass" wording |
| 03-02: clicks through 3000, end 2750, grace 250 expects G not-reached, but the formula gives missed (L56@58daf59) | HIGH | Plan 03-02, Task 2: verified (3000 ≤ 2750 + 250). The test now expects G missed with clicks through 3000. A separate case with clicks through 2500 (G's click absent, as live at the mark) expects not-reached, and clicks through 3500 expect final and missed. Must_haves truth 5 |
| 03-02: reopening the same file during capture permanently suppresses painting (L57@58daf59) | HIGH | Plan 03-02, Task 2 step 3: verified in src/score-renderer.js loadPiece (always a new model) and src/capture-app.js piece-loaded (the session continues on the same hash). The listener rebinds `A.session.model` to the new model when the verified hash equals `A.session.pieceId`, resets the cache and re-analyses, with the load-generation check after the hash await. The identity guard stays for the pre-hash window. Harness group 6b (Plan 03-01) proves it. Must_haves truth 7 |
| 03-03: no performance budget; every pending-click callback re-analyses completed passes on the metronome thread (L79@58daf59) | MEDIUM | Plan 03-02, Task 2 step 2: `A.resultCache` aligns only newly final passes; this is safe because a final pass's stableFields never change. The measured budget itself is in Plan 03-03, Task 2 (`scripts/check-align-perf.cjs`) |
| 03-01: extra-note lower bound presented as exact (L33@58daf59) | MEDIUM | Plan 03-02, Task 1: `GapCounts.lowerBoundPasses`, the ambiguity-adjacent rule, the "at least" sentence forms, and folds of a hand-built messy result asserting before:2 unassessed and before:3 lower-bound |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-01: lagged reach vs D-11 (L32@58daf59) | Deferred — awaiting user decision (conflicts with D-11); see the Plan 03-01 Round 2 ledger. This plan only adds the `reachClock` switch with the D-11 default; both values behave identically at lag 0 |

### Round 3 — 2098322

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-02: Start does not invalidate an outstanding history load; a delayed `loadLatestSession` can overwrite the new live session (L56@2098322) | HIGH | Plan 03-02, Task 2 step 3: verified in src/capture-app.js start() (awaits before `C.session` is set) and the Round 2 generation scheme (only piece events and loads advanced it). `start()` and `endSession()` now advance `A.loadGen` before their first await. `loadLatestSession` re-checks generation and `C.session` after every await and applies nothing while a live session exists. Harness group 8b (Plan 03-01, Task 3) holds a restoration in flight across `CaptureApp.start()` and asserts the live session stays on screen. Acceptance greps count the increments |
| 03-02: a failed piece load during capture leaves the metronome running against a null model; the unload hook clears analysis state without ending capture (L57@2098322) | HIGH | Plan 03-02, Tasks 1-2: verified in src/score-renderer.js loadPiece's catch (model nulled, `piece-unloaded` dispatched synchronously) and src/capture-app.js timeSignatureFor (read `ScoreRenderer.state.model.measures`). The unload listener calls `endSession('piece-unloaded')` in its synchronous prefix, and `timeSignatureFor` reads `C.sessionModel` captured at Start. The ended session is retained in `A.session` and stored, so its marks return when the piece is reopened. Harness group 8c loads invalid MusicXML during capture: session ended, clicks stop, no page error, stored endReason, marks restored on reopen |
| 03-01: the 2600 ms crossover clone merges E and F into one token (7360, not 6680); decreasing timestamps unspecified (L35@2098322) | HIGH | Plan 03-02, Task 1: `tokenizePlayed` uses the absolute window and seq order. The crossover test builds the chronological sequence (6680, extra E4 at before:4). New decreasing-timestamp tests: F at 1940 → own token, all played, deviationMs −560, 3808; F at 1960 → joins E, extra at within:2, F missed, 7000. The raw pass is unchanged. Numbers checked with a reference implementation (see Plan 03-01 Round 3) |
| 03-01: fixture comparison iterates only supplied expectations (L36@2098322) | MEDIUM | Plan 03-02, Task 1: the fixture loop asserts that result note keys equal expected note keys exactly before comparing each verdict |
| 03-04: single-pass `describeGap` receives only filtered extras and cannot know about ambiguous neighbours (L98@2098322) | HIGH | Plan 03-02, Task 1: verified (Round 2's 03-04 wiring passed `result.extras.filter`). `Aggregate.gapAssessment(result, gapKey)` is the one gap rule; `describeGap`'s pass source is the whole PassResult. Tests: the messy pass-view sentence from a full result, the same result without ambiguity data losing the qualifier, and `gapAssessment` outputs for before:2, before:3 and before:4. The capture-app wiring and the browser check are in Plans 03-04 and 03-05 |
| 03-05: `# fail 0` cannot match the default reporter (L120@2098322) | MEDIUM | Plan 03-02, Tasks 1-2: `<fails_when>` names the exit status and `ℹ` summary; count checks run with `--test-reporter=tap`; `npm test` is judged by exit status |
| 03-05: "up to about a beat and a half" understates finalization (L119@2098322) | MEDIUM | Plan 03-02 flagged assumptions: no bound stated |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-01: lagged reach vs D-11 (carried) | Still awaiting the user's decision at Plan 03-05; unchanged here |
