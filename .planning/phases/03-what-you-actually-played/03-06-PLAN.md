---
phase: 03-what-you-actually-played
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - src/align.js
  - test/align.test.cjs
  - docs/analysis-rules.md
  - test/align-ladder.test.cjs
  - fixtures/README.md
autonomous: false
requirements: [ANLZ-01, ANLZ-03, AGGR-03]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The reach decision the user recorded at the rung-1 gate is applied before any rung-2 work (review 03-01 HIGH round 2): 'Reach decision: origin' leaves reachClock 'origin' and marks the contract paragraph and tunables row as the user's decision; 'Reach decision: reading' sets DEFAULT_TUNABLES.reachClock to 'reading', flips the one default test and the contract's default sentence, and leaves rung1-late-abandoned-origin.json and rung1-late-abandoned-reading.json unchanged; a missing decision stops this plan"
    - "test/align-ladder.test.cjs parses rungs 1 and 2 through the real OSMD parser and, against an oracle computed only from the model's own rationals (never Align.buildSlots or Align.expectedTime), shows for each: a perfect performance all played with no extras; one raised pitch marks exactly that notehead wrong; omitting the last onset group with the end fixed one beat after it marks those notes missed, not not-reached; the whole performance one beat late reads all played with entryLag 1; and a stray note-on at the origin before that late performance is an extra at before:0 while every score note is still played with entryLag 1 (ROADMAP success criterion 1, review 03-05 HIGH and MEDIUM round 1, review 03-03 HIGH round 2)"
    - "Rung 2 is checked at the piano before rung 3 is built (review 03-06 HIGH round 2, ROADMAP 'check the current rung before expanding'): plan 03-07 depends on this plan's approval"
    - "The user played rung 2 (left hand C3 D3 E3 F3 G3) at the FP-60X: a clean pass showed no marks, C3 D3 F3 F3 G3 showed only E3 red with 'E3, bar 1 beat 3: wrong in 1 of 2 assessed passes (played F3 once)', and one clean rung-1 regression pass showed no marks (ROADMAP success criterion 4 on rung 2; earlier rungs remain regression checks)"
    - "npm test (exit 0), node scripts/check-run-path.cjs --fixtures, node scripts/check-svg-map.cjs, node scripts/check-capture-roundtrip.cjs, node scripts/check-paint.cjs (thirteen OK lines), node scripts/check-align-perf.cjs and node scripts/check-live-perf.cjs are green, run fresh before the checkpoint"
    - "A changed reach default is observed at the piano before it carries later rungs (review 03-06 MEDIUM round 3): when the recorded decision is 'reading', the rung-2 checkpoint replays the rung-1 late-and-abandoned bar (C D E one beat late, mark right after E) and 03-06-SUMMARY.md records what F4 actually showed as a 'Reach recheck:' line; expected F4 grey 'F4, bar 1 beat 4: unassessed (not reached)'; with 'origin' the line reads 'Reach recheck: not needed (origin unchanged)'"
    - "The canonical decision record is handed off explicitly (review 03-06 LOW round 3): 03-06-SUMMARY.md ends with 'CONTEXT reconciliation: required - amend D-11 in 03-CONTEXT.md to include the literal reach: reading' or 'CONTEXT reconciliation: none (origin)'; the orchestrator, never the planner or executor, makes the amendment, and plan 03-07 refuses to start rung-3 work until 03-CONTEXT.md agrees with the recorded decision"
    - "Any false mark on rung 2 is recorded verbatim and routes to /gsd-plan-phase 3 --gaps before rung 3 (VRFY-01)"
  artifacts:
    - path: "test/align-ladder.test.cjs"
      provides: "OSMD-parsed ladder regression with an independent oracle; rungs 1-2 here, extended one rung per plan"
      contains: "02-left-hand.xml"
    - path: "fixtures/README.md"
      provides: "Phase 3 rung-2 at-the-piano checklist"
      contains: "Phase 3 - rung 2 at the piano"
    - path: "docs/analysis-rules.md"
      provides: "The reach-clock paragraph and tunables row carrying the user's recorded decision"
      contains: "reachClock"
  key_links:
    - from: "test/align-ladder.test.cjs"
      to: "test/osmd-node-env.cjs"
      via: "installOsmdNodeEnv / loadSheet / readFixture, the Phase 1 pattern for parsing the ladder under node:test"
      pattern: "installOsmdNodeEnv"
    - from: ".planning/phases/03-what-you-actually-played/03-05-SUMMARY.md"
      to: "src/align.js DEFAULT_TUNABLES.reachClock"
      via: "the 'Reach decision:' line recorded from the user's words decides the one switch"
      pattern: "Reach decision"
  prohibitions:
    - requirement_id: ANLZ-03
      category: transparency
      statement: "MUST NOT generate expected times for a ladder performance with Align.buildSlots or Align.expectedTime; the test's oracle is independent of the code under test (review 03-05 MEDIUM round 1)"
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT set reachClock to anything other than the user's recorded choice, and MUST NOT edit 03-CONTEXT.md; a D-11 wording amendment is the orchestrator's, from the user's words (review 03-01 HIGH round 2)"
    - requirement_id: ANLZ-01
      category: values
      statement: "MUST NOT count rung 2 done on automated output alone; the checkpoint is blocking-human (VRFY-01)"
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT start rung-3 work (both-hands fixture, rung-3 ladder cases) in this plan (ROADMAP rung order, review 03-06 HIGH round 2)"
---

## Phase Goal

**As a** pianist drilling right-hand C D E F G alone at the FP-60X, **I want to** see wrong, missed and extra notes painted on the notation after each pass, with honest counts across the passes at one tempo, **so that** I can tell a recurring mistake from a one-off slip and trust what the score shows me.

<objective>
Rung 2, and only rung 2, after the user approved rung 1 (03-05). The steps:

1. Apply the reach decision the user recorded at the rung-1 gate to its one switch, so no later rung builds on an undecided rule.
2. Create the ladder regression that runs each rung through the real OSMD parser against an oracle independent of the engine, for rungs 1 and 2.
3. Write the rung-2 piano checklist and stop for the user at the FP-60X.

Rung 3 (03-07), rung 4 (03-08) and rung 5 (03-09) follow the same build-then-check pattern, each gated on the previous rung's approval.

Round 2 (58daf59): this plan exists because the review found that rungs 3, 4 and 5 were still all built before the next piano check (review 03-06 HIGH). The former 03-06 (chords, both hands, the whole ladder) and 03-07 (rungs 2-5 at the piano) are split into one plan per rung.

Purpose: ROADMAP success criterion 4 on rung 2 and criterion 1 on rungs 1-2 through the real parser, following "fix false marks at the current rung before expanding".

Output: the reach switch set from the user's words, test/align-ladder.test.cjs (rungs 1-2), the rung-2 checklist, gates green, and the user's response in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-what-you-actually-played/03-CONTEXT.md
@.planning/phases/03-what-you-actually-played/03-03-SUMMARY.md
@.planning/phases/03-what-you-actually-played/03-05-SUMMARY.md
@docs/analysis-rules.md
@src/align.js
@test/align.test.cjs
@test/score-model.test.cjs
@test/osmd-node-env.cjs
@fixtures/README.md

<interfaces>
<!-- From 03-02/03-03 (src/align.js) and Phase 1 (test/osmd-node-env.cjs). -->
- Align.alignPass(model, pass, clicks, tunables?) -> PassResult { attempt, originIndex, entryLag, totalCost, wholeReason, slots: [{ index, noteIds, expectedTime, reached }], notes: { noteId: { status, playedPitch?, deviationMs?, reason? } }, extras: [{ seq, pitch, timeStamp, gap }], ambiguousPlayed }
- Align.DEFAULT_TUNABLES.reachClock ('origin' until the user's recorded decision says otherwise); lateEntryProbeTokens 3
- test/osmd-node-env.cjs: installOsmdNodeEnv() -> env with restore(); loadSheet(xmlText) -> OSMD Sheet; readFixture('fixtures/01-right-hand.musicxml') -> text. ScoreModel.extract(sheet) -> model (pattern: test/score-model.test.cjs lines 1-12)
- Rung 2 ids (test/score-model.test.cjs EXPECTED_RUNG_2): C3 D3 E3 F3 G3 quarter notes in the bass staff of one 5/4 bar (MIDI 48 50 52 53 55)
- 03-05-SUMMARY.md carries a line `Reach decision: origin`, `Reach decision: reading` or `Reach decision: not given`
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "rungs 1 and 2, parsed by the real OSMD parser, align against an independent oracle" — plus the user's reach decision applied to its one switch first</name>
  <precondition>03-05-SUMMARY.md records "approved" for both rung-1 checkpoints and a line `Reach decision: origin` or `Reach decision: reading` (ROADMAP: no harder rung before rung 1 is approved at the piano; review 03-01 HIGH round 2: no rung built on an undecided reach rule)</precondition>
  <reversibility rating="costly">If the user chose 'reading', this flips which unplayed notes of a late abandoned pass count as missed in every later rung and in Phase 4; it is a one-switch change plus one default test, but every at-the-piano observation after it assumes the chosen rule.</reversibility>
  <files>src/align.js, test/align.test.cjs, docs/analysis-rules.md, test/align-ladder.test.cjs</files>
  <read_first>
    - .planning/phases/03-what-you-actually-played/03-05-SUMMARY.md (the `Reach decision:` line and the user's step-9 words, quoted verbatim)
    - docs/analysis-rules.md the reach-clock paragraph (D-02 late-and-abandoned) and the `reachClock` tunables row
    - src/align.js DEFAULT_TUNABLES and the one reach computation; test/align.test.cjs the DEFAULT_TUNABLES.reachClock assertion and the "no tunables gives the -origin labels" test
    - test/score-model.test.cjs lines 1-12 and EXPECTED_RUNG_2 (how the env is installed, sheets loaded, models extracted, env restored in after())
    - test/osmd-node-env.cjs (installOsmdNodeEnv, loadSheet, readFixture signatures)
    - fixtures/README.md rungs 1 and 2
  </read_first>
  <behavior>
    - For fixtures/01-right-hand.musicxml and fixtures/02-left-hand.xml at 120 BPM from page time 1000, five cases each:
    - (a) the perfect performance yields attempt true, originIndex 0, entryLag 0, wholeReason null, every model note played, extras [] and ambiguousPlayed []
    - (b) raising the pitch of the first note of the second onset group by one semitone yields exactly that noteId wrong with that playedPitch and every other note played
    - (c) omitting every note of the last onset group, with the end fixed at the oracle time of that group plus one beat, yields those notes missed (not not-reached) and everything else played
    - (d) the whole perfect performance shifted by one beat (500 ms) yields every note played, entryLag 1, and every deviationMs within 0..20 of 500
    - (e) one extra note-on, pitch = the first note's pitch + 24, at the origin click's page time, followed by the performance of (d) yields every note played, entryLag 1, and extras exactly one entry with gap before:0 and that pitch (review 03-03 HIGH round 2 on real parsed rungs; the stray note is part of the raw pass, never filtered)
    - After the reach decision step: Align.DEFAULT_TUNABLES.reachClock equals the recorded choice, and alignPass on rung1-late-abandoned-origin.json's pass and clicks with no tunables gives the labels of the fixture named after that choice
  </behavior>
  <action>
1. Apply the reach decision (review 03-01 HIGH round 2). Read the `Reach decision:` line of 03-05-SUMMARY.md.
   - `origin`: leave src/align.js unchanged. In docs/analysis-rules.md, replace every occurrence of "user decision pending" (the Late-and-abandoned worked example, the reach-clock paragraph and the tunables row) with "user decision: origin, recorded at the rung-1 piano gate (03-05-SUMMARY.md)", followed by the user's step-9 words quoted verbatim.
   - `reading`: set DEFAULT_TUNABLES.reachClock to `'reading'` in src/align.js and update its comment. In test/align.test.cjs, change the DEFAULT_TUNABLES.reachClock assertion to `'reading'` and change the "no tunables" abandoned-pass test to expect the rung1-late-abandoned-reading.json labels. In docs/analysis-rules.md, make `'reading'` the default in the reach-clock paragraph, the tunables row and the Late-and-abandoned worked example (keeping both outcomes described), and replace every occurrence of "user decision pending" with "user decision: reading, recorded at the rung-1 piano gate (03-05-SUMMARY.md)", followed by the user's words.
   - Either way:
     - Do not edit the two explicit-tunables fixtures or 03-CONTEXT.md. Write two lines into 03-06-SUMMARY.md now, so the hand-off exists even if the plan stops at the checkpoint (review 03-06 LOW round 3):
       - `CONTEXT reconciliation: required - amend D-11 in 03-CONTEXT.md to include the literal reach: reading` when the choice was 'reading', or `CONTEXT reconciliation: none (origin)`. The orchestrator makes the amendment from the user's words; plan 03-07's precondition checks it.
       - `Reach recheck: pending (Task 3 step 4)` when the choice was 'reading', or `Reach recheck: not needed (origin unchanged)`. Task 3 replaces the pending value with what the user saw (review 03-06 MEDIUM round 3).
     - Run `node --test test/align.test.cjs test/align-fixtures.test.cjs test/aggregate.test.cjs` and `node scripts/check-paint.cjs` before continuing.
   - If the line is `not given` or absent, stop and report; do not guess.

2. Create `test/align-ladder.test.cjs` (node:test; jsdom only to let OSMD parse, exactly as test/score-model.test.cjs does; alignment calls receive plain JSON). None of these helpers call the engine's slot-building or expected-time helpers, and none mention them in comments (review 03-05 MEDIUM round 1):
   - `clicksFor(model, bpm, pageTime0)` walks model.measures in order, emitting timeSignature.beats clicks per measure (assert beatType === 4 for every rung, else fail loudly) with bar = measure.number, beat 1..beats, accent = beat === 1, bpm, and pageTime advancing by 60000 / bpm; then it emits one extra bar.
   - `oracleTime(model, note, pageTime0, bpm)` = pageTime0 + (measure.start.num / measure.start.den + note.onset.num / note.onset.den) × 60000 / bpm, computed from the model's own rationals for the note's measure (valid because the synthetic clicks are evenly spaced).
   - `onsetGroups(model)` groups notes by that absolute quarter-beat value, in model order.
   - `perfectPerformance(model, pageTime0, bpm, shiftMs = 0)` emits one note-on per note at oracleTime + shiftMs + 5 × (index within its onset group), velocity 80, seq in time order.
   - `withStrayAtOrigin(notes, pitch, pageTime0)` prepends one note-on at pageTime0 and renumbers seq.
   - `passFor(notes, endTimeStamp, pageTime0)` gives { ordinal: 1, startTimeStamp: pageTime0 − 100, endTimeStamp, notes, noteCount }. The end is passed in explicitly: for every case it is fixed at the oracle time of the last onset group + one beat (+ shiftMs for the late cases), never derived from the last played note (review 03-05 HIGH round 1).

   Write cases (a)-(e) from the behavior block for rungs 1 and 2 in a loop over a `RUNGS` array, so later plans append rungs 3, 4 and 5 by adding entries plus their own extra cases. Print nothing but TAP; restore the env in after().
  </action>
  <verify>
    <automated>node --test test/align-ladder.test.cjs test/align.test.cjs test/align-fixtures.test.cjs</automated>
    <fails_when>non-zero exit (node --test exits 1 when any test fails or a file cannot load), or the summary shows a non-zero fail count or a pass count of 0 — the default spec reporter prints these as "ℹ fail N" and "ℹ pass N", never "# fail"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '02-left-hand.xml' test/align-ladder.test.cjs` prints at least 1 and `grep -c '01-right-hand.musicxml' test/align-ladder.test.cjs` prints at least 1; `grep -c '03-both-hands' test/align-ladder.test.cjs` prints 0 (no rung-3 work yet)
    - `grep -c 'installOsmdNodeEnv' test/align-ladder.test.cjs` prints at least 1 and `grep -c 'Align.alignPass' test/align-ladder.test.cjs` prints at least 1
    - `grep -vE '^\s*(/\*|\*|//)' test/align-ladder.test.cjs | grep -cE 'Align\.(buildSlots|expectedTime)'` prints 0 (the oracle is independent; comment lines are excluded from the count)
    - `grep -c 'withStrayAtOrigin' test/align-ladder.test.cjs` prints at least 2
    - `node --test --test-reporter=tap test/align-ladder.test.cjs` exits 0 and its summary shows `# fail 0` and `# pass N` with N at least 10 (TAP selected explicitly, review 03-05 MEDIUM round 3)
    - `grep -cE '^CONTEXT reconciliation: (required|none)' .planning/phases/03-what-you-actually-played/03-06-SUMMARY.md` prints 1 and `grep -cE '^Reach recheck: (pending|not needed)' .planning/phases/03-what-you-actually-played/03-06-SUMMARY.md` prints 1 (review 03-06 LOW and MEDIUM round 3)
    - `node -e "const s=require('fs').readFileSync('.planning/phases/03-what-you-actually-played/03-05-SUMMARY.md','utf8');const m=s.match(/Reach decision: (origin|reading)/);if(!m)process.exit(1);globalThis.ScoreModel=require('./src/score-model.js');const A=require('./src/align.js');process.exit(A.DEFAULT_TUNABLES.reachClock===m[1]?0:1)"` exits 0 (the switch equals the user's recorded choice)
    - `grep -c 'user decision pending' docs/analysis-rules.md` prints 0 and `grep -c 'recorded at the rung-1 piano gate' docs/analysis-rules.md` prints at least 1
    - `git diff --quiet -- test/fixtures/align/rung1-late-abandoned-origin.json test/fixtures/align/rung1-late-abandoned-reading.json .planning/phases/03-what-you-actually-played/03-CONTEXT.md` exits 0
  </acceptance_criteria>
  <done>The reach rule is the user's, applied in one place, and rungs 1 and 2 parsed by the real parser align cleanly, mark a single injected wrong or missing note exactly, and read a one-beat-late performance as late even after a stray opening note — all against an oracle that shares no code with the engine.</done>
</task>

<task type="auto">
  <name>Task 2: Rung-2 gate — every automated check fresh and the rung-2 piano checklist</name>
  <precondition>C:\Program Files\Google\Chrome\Application\chrome.exe exists (or CHROME_EXE is set) and the machine has internet access for the pinned CDN bundles</precondition>
  <files>fixtures/README.md</files>
  <read_first>
    - fixtures/README.md section "Phase 3 - rung 1 at the piano" (legend, ready rule, origin rule — reuse the wording; append after it, do not rewrite)
    - docs/analysis-rules.md "Detail-panel sentences"
    - .planning/phases/03-what-you-actually-played/03-05-SUMMARY.md (anything the user reported on rung 1 that the rung-2 wording should reflect, and the reach decision)
  </read_first>
  <action>
1. Append `## Phase 3 - rung 2 at the piano` to `fixtures/README.md`, plain prose and short lists, no JSON. Include:
   - a two-line reminder of the legend, the finishing delay and the ready rule (pointing to the rung-1 section);
   - one sentence stating the reach rule the user chose;
   - Scenario D exactly as written in Task 3, each observation with its view and sentence — include step 4 (the reach recheck) only when the recorded decision is 'reading', and omit it with 'origin';
   - the rule that a false mark stops the ladder here.

2. Run the gates fresh, in this order, and fix anything red before committing:
   1. `npm test`
   2. `node scripts/check-run-path.cjs --fixtures`
   3. `node scripts/check-svg-map.cjs`
   4. `node scripts/check-capture-roundtrip.cjs`
   5. `node scripts/check-paint.cjs`
   6. `node scripts/check-align-perf.cjs`
   7. `node scripts/check-live-perf.cjs`

   Commit fixtures/README.md.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit (npm test propagates node --test's exit 1 on any failing test), or a summary line "ℹ fail N" with N greater than 0</fails_when>
    <automated>node scripts/check-run-path.cjs --fixtures</automated>
    <fails_when>non-zero exit, any line starting "FAIL:", or the summary line not ending in "0 failed"</fails_when>
    <automated>node scripts/check-svg-map.cjs</automated>
    <fails_when>non-zero exit; any "FAIL fixtures/..." line; fewer than six "OK fixtures/" lines</fails_when>
    <automated>node scripts/check-capture-roundtrip.cjs</automated>
    <fails_when>non-zero exit; output lacks either OK line ("OK capture round-trip: 8 raw events restored unmodified" prefix, "OK capture round-trip: pass split 3/2/1/1 restored after reload mid-session"); any line starting "FAIL capture round-trip"</fails_when>
    <automated>node scripts/check-paint.cjs</automated>
    <fails_when>non-zero exit; any line starting "FAIL paint:"; fewer than thirteen lines starting "OK paint:"</fails_when>
    <automated>node scripts/check-align-perf.cjs</automated>
    <fails_when>non-zero exit, a line starting "FAIL align perf:", or no line starting "OK align perf:"</fails_when>
    <automated>node scripts/check-live-perf.cjs</automated>
    <fails_when>non-zero exit; a line starting "FAIL live perf:"; no line starting "OK live perf:"</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven commands above exit 0 with their OK lines (thirteen `OK paint:` lines, six `OK fixtures/` lines, both round-trip lines, `OK align perf:`, `OK live perf:`; `npm test` judged by its exit status)
    - When 03-05-SUMMARY.md records `Reach decision: reading`, `grep -c 'Only if you chose "reach: reading"' fixtures/README.md` prints 1 (the rung-2 recheck step is in the checklist); with `origin` the same command prints 0
    - `grep -c 'Phase 3 - rung 2 at the piano' fixtures/README.md` prints 1; `grep -c 'E3, bar 1 beat 3: wrong in 1 of 2 assessed passes (played F3 once)' fixtures/README.md` prints 1; `grep -c 'Phase 3 - rung 3 at the piano' fixtures/README.md` prints 0
    - `git ls-files --error-unmatch fixtures/README.md` exits 0
  </acceptance_criteria>
  <done>All automated gates are green with rungs 1-2 in the ladder regression, and the checklist tells the user exactly what to play on rung 2 and what to expect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: At the FP-60X — rung 2 (left hand), clean and wrong, plus a rung-1 regression pass</name>
  <what-built>The same engine and display on the left-hand exercise, with the reach rule you chose at rung 1. The ladder regression already runs rungs 1 and 2 through the real parser against an independent oracle; this is the hardware check before anything for both hands is built.</what-built>
  <how-to-verify>
fixtures/README.md open at "Phase 3 - rung 2 at the piano". Same ready rule as rung 1: read a pass only after `(finishing)` clears, and before the next pass press the mark once more and start on the next accented click.

Scenario D:
1. Open fixtures/02-left-hand.xml, set 80 BPM (Enter), press Start. Play C3 D3 E3 F3 G3 cleanly on the clicks, mark. Session view: no coloured noteheads; click C3 -> `C3, bar 1 beat 1: played correctly in all 1 assessed passes`.
2. Play C3 D3 F3 F3 G3 (F3 instead of E3), mark. Session view: only E3 red; click E3 -> `E3, bar 1 beat 3: wrong in 1 of 2 assessed passes (played F3 once)`.
3. Regression: open fixtures/01-right-hand.musicxml (the rung-2 session ends), press Start, play C D E F G cleanly, mark -> the pass view of the pass you just played shows no coloured noteheads.
4. Only if you chose "reach: reading" at rung 1 — the recheck of the rule you picked (review 03-06 MEDIUM round 3). Still on rung 1, press the mark (ready rule), let the accented click pass, play C on beat 2's click, D on beat 3's, E on beat 4's, and press the mark immediately after E. Pass view of the pass you just played: C4 D4 E4 black; F4 and G4 grey; click F4 -> `F4, bar 1 beat 4: unassessed (not reached)`. This is the first time the app runs your chosen rule at the piano: report exactly what F4 showed.
  </how-to-verify>
  <resume-signal>Type "approved" only if the clean rung-2 pass showed no mark, the wrong pass showed only E3 red with the quoted sentence, the rung-1 regression pass showed no mark, and — if you chose "reach: reading" — F4 was grey with the step-4 sentence. Include the step-2 sentence as shown and, for step 4, what F4 showed. Otherwise describe the step, what you played and what the app showed (for example "step 2: E3 and F3 both turned red"). A false mark goes to /gsd-plan-phase 3 --gaps and rung 3 is not built until it is fixed.</resume-signal>
  <read_first>
    - fixtures/README.md section "Phase 3 - rung 2 at the piano" (written by Task 2)
    - docs/analysis-rules.md D-04 and "Detail-panel sentences" (to judge a reported mismatch against the written rule)
  </read_first>
  <acceptance_criteria>
    - The user's response is recorded verbatim in 03-06-SUMMARY.md and is either "approved" with the step-2 sentence, or a list of mismatches naming the step, what was played and what was shown
    - With `Reach decision: reading`, the `Reach recheck: pending (Task 3 step 4)` line in 03-06-SUMMARY.md is replaced by `Reach recheck: F4 showed <the user's words>`; a blue F4, or any other mark on C4-E4, is a mismatch that goes to gap closure. With `origin` the line stays `Reach recheck: not needed (origin unchanged)`
    - A mismatch response leads to /gsd-plan-phase 3 --gaps before plan 03-07 starts
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `src/align.js`: `DEFAULT_TUNABLES.reachClock` set to the user's recorded choice (unchanged when the choice is 'origin').
- `test/align.test.cjs`: the default-clock assertions follow the choice.
- `docs/analysis-rules.md`: the reach-clock paragraph and tunables row carry "user decision: …, recorded at the rung-1 piano gate", with the user's words.
- `test/align-ladder.test.cjs`: the ladder regression through OSMD, with helpers `clicksFor`, `oracleTime`, `onsetGroups`, `perfectPerformance`, `withStrayAtOrigin`, `passFor` and a `RUNGS` array (rungs 1-2; five cases each: perfect, wrong pitch, omission with fixed end, one beat late, stray opening note then late).
- `fixtures/README.md`: "Phase 3 - rung 2 at the piano" (Scenario D, with the step-4 reach recheck only after a "reading" decision).
- `03-06-SUMMARY.md`: the applied reach decision, the ladder test count, the fresh gate output, the user's rung-2 response verbatim, and the hand-off lines `CONTEXT reconciliation: required - … | none (origin)` and `Reach recheck: F4 showed … | not needed (origin unchanged)`.

## Flagged assumptions (not silently dropped)

- The ladder oracle assumes evenly spaced synthetic clicks and x/4 time signatures (asserted); it does not exercise the real scheduler (Phase 2 and the paint harness do).
- Rung 2 introduces no new interpretation rule, so its piano check is clean + wrong + a rung-1 regression pass; the rules exercised at rung 1 are not repeated per rung.
- D-11's wording in 03-CONTEXT.md is not edited by the planner or executor. If the user chose 'reading', the orchestrator amends it from the user's recorded words, using the literal `reach: reading`, which plan 03-07's precondition checks before rung 3 (review 03-06 LOW round 3).
- The reach recheck (review 03-06 MEDIUM round 3) happens at the rung-2 session because that is the next time the user is at the piano; it uses rung 1 because the late-and-abandoned bar the user judged was on rung 1.

<verification>
- `node --test test/align-ladder.test.cjs test/align.test.cjs test/align-fixtures.test.cjs` exits 0; the seven gate commands green
- The blocking-human checkpoint answered "approved" with the sentence quoted, or its issues recorded as gaps for `/gsd-plan-phase 3 --gaps`
</verification>

<success_criteria>
- ROADMAP success criterion 4 holds on rung 2; criterion 1 holds on rungs 1-2 through the real parser against an independent oracle
- No rung is built on an undecided reach rule; rung 3 is not started before rung 2 is approved
</success_criteria>

<output>
Create `.planning/phases/03-what-you-actually-played/03-06-SUMMARY.md` when done. Record:
- the reach decision applied and exactly what changed (or "no change");
- the ladder test count;
- the fresh gate output;
- the checkpoint response verbatim;
- the `CONTEXT reconciliation:` and `Reach recheck:` lines.
</output>

## Review Dispositions Ledger

### Round 1 — af0835b

<!-- Carried verbatim from the former 03-06 (chords, both hands, ladder). That work now lives in 03-07 (rung 3), 03-08 (rung 4) and 03-09 (rung 5); see Round 2 below. -->

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-03: internal chord ties resolved by lower pitch (a guess the outer DP cannot see) | HIGH | Plan 03-06, Task 2: pairCost enumerates minimum-distance assignments and reports differing noteheads as ambiguous; `rung4-chord-equidistant.json`; the reviewer's specific C E G / C F example was checked and is not a tie (F4 is one semitone from E4), which a test pins as a unique pairing |
| 03-03: a re-strike inside a chord fragments tokens and causes false misses; add a chord + re-strike fixture and order permutations | HIGH | Plan 03-06, Task 1: verified against the old tokenizer rule; `rung4-chord-restrike.json` plus two note-order permutations (the duplicate-in-token rule itself lands in Plan 03-02) |
| 03-05: rung-4 omission test expects missed but the helper's end (last note + 400 ms) makes it not-reached | HIGH | Plan 03-06, Task 3: verified (end ≈ 1665 < 2000); passFor takes an explicit end fixed one beat after the last onset group |
| 03-05: performance generator reuses Align.buildSlots / Align.expectedTime | MEDIUM | Plan 03-06, Task 3: independent `oracleTime` from model rationals, a negative grep forbidding the Align helpers in the ladder test, and hand-computed rung-5 anchor times |
| 03-01: late entry untested on multi-note slots and longer passages | HIGH | Plan 03-06, Task 3 case (d): every rung including rung 4 chords and 41-note rung 5 played one beat late must read all played with entryLag 1 |
| 03-06: first piano gate after harder rungs | HIGH | Plan 03-06 now depends on Plan 03-05 (rung-1 approval) and carries a precondition on it |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none for this plan) | — |

### Round 2 — 58daf59

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-06: rung-by-rung gating only partly fixed — rungs 3, 4 and 5 are all built before the next piano checkpoint; interleave per-rung implementation and checkpoints (L138@58daf59) | HIGH | Verified against ROADMAP "Execution within Phases 3–5" ("fix false marks at the current rung before expanding"). The former 03-06/03-07 are split into one build-then-check plan per rung: 03-06 rung 2 (this plan: ladder rungs 1-2, rung-2 checkpoint), 03-07 rung 3 (both-hands fixture, ladder rung 3, checkpoint), 03-08 rung 4 (chords, in-chord ambiguity, ladder rung 4, checkpoint), 03-09 rung 5 (ladder rung 5, interpolation anchors, checkpoint, phase close). Each depends on the previous plan's approval and has a prohibition against starting the next rung. Supersedes this file's Round 1 rows: their chord work is carried to Plan 03-08, the both-hands work to Plan 03-07, and the rung-5 anchors to Plan 03-09, where each row is re-ledgered |
| 03-03: one stray opening note disables the late-entry repair (L78@58daf59) | HIGH | Plan 03-06, Task 1 case (e): a stray note-on at the origin before a one-beat-late performance, on every OSMD-parsed rung (rungs 1-2 here, extended per rung). Fixtures and the engine fix are in Plan 03-03, Task 2 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-01: lagged reach changes locked decision D-11 (L32@58daf59) | Deferred — awaiting user decision (conflicts with D-11); the decision is taken at Plan 03-05, Task 2 step 9. This plan applies the recorded choice to the single `reachClock` switch in Task 1 and stops if none was recorded. It never chooses for the user and never edits 03-CONTEXT.md |

### Round 3 — 2098322

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-06: choosing `reading` flips the default, but the later hardware checklist only exercises clean and wrong pitches; the user saw only the `origin` behaviour (L140@2098322) | MEDIUM | Plan 03-06, Tasks 1-3: verified (Scenario D had no late-and-abandoned bar). When the recorded decision is `reading`, Scenario D gains step 4, which replays the rung-1 late-and-abandoned bar under the new default. The expected F4 is grey `unassessed (not reached)`, and 03-06-SUMMARY.md records `Reach recheck: F4 showed …`; a blue F4 is a mismatch routed to gap closure. With `origin` nothing changed, so the step is omitted and the line says so |
| 03-06: the canonical CONTEXT amendment is left to the orchestrator with no completion gate (L141@2098322) | LOW | Plan 03-06, Task 1 writes the hand-off line `CONTEXT reconciliation: required - amend D-11 … reach: reading` (or `none (origin)`) into 03-06-SUMMARY.md; Plan 03-07, Task 1 has a precondition and an acceptance command that stop rung-3 work until 03-CONTEXT.md contains `reach: reading` whenever the decision was `reading`. The orchestrator makes the amendment; the planner and executor still never edit 03-CONTEXT.md |
| 03-05: `# fail 0` cannot match the default reporter (L120@2098322) | MEDIUM | Plan 03-06, Tasks 1-2: `<fails_when>` names the exit status and `ℹ` summary; the ladder count uses `--test-reporter=tap`; `npm test` is judged by exit status |
| 03-03: live callback budget missing from the gates (L77@2098322) | MEDIUM | Plan 03-06, Task 2: `node scripts/check-live-perf.cjs` is the seventh gate command |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-01: lagged reach vs D-11 (carried) | The user's decision is still taken at Plan 03-05; this plan only applies it, rechecks a `reading` choice at the piano and hands off the CONTEXT amendment |
