---
phase: 03-what-you-actually-played
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/analysis-rules.md
  - test/fixtures/align/rung1-clean.json
  - test/fixtures/align/rung1-late-first-note.json
  - test/fixtures/align/rung1-missing-first-note.json
  - test/fixtures/align/rung1-wrong-e.json
  - test/fixtures/align/rung1-octave-slip.json
  - test/fixtures/align/rung1-correction.json
  - test/fixtures/align/rung1-shifted-start.json
  - test/fixtures/align/rung1-not-reached.json
  - test/fixtures/align/rung1-early-last-note.json
  - test/fixtures/align/rung1-origin-after-start.json
  - test/fixtures/align/rung1-double-mark.json
  - test/fixtures/align/rung1-whole-beat-late.json
  - test/align-fixtures.test.cjs
  - scripts/check-paint.cjs
  - package.json
autonomous: true
requirements: [ANLZ-01, ANLZ-03, AGGR-01, AGGR-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/analysis-rules.md exists and states every rule D-01 to D-21 from 03-CONTEXT.md in plain prose, each paired with the fixture file name that exercises it, with a worked example on rung 1 (C4 D4 E4 F4 G4, 5/4, 120 BPM, clicks 500 ms apart from page time 1000) for: missing first note, late entry, repeated pitches, an extra note, an interrupted (abandoned) pass, a restart, ambiguous alignment, and a tempo change (ROADMAP Phase 3 success criterion 6, D-21)"
    - "Every numeric worked example in the contract is arithmetically consistent with its own definitions: the gap rule (k = slots whose expected time is at or before the note) gives the gap key printed next to each extra, and each cost comparison is recomputed from the tunables table (review 03-01 MEDIUM: correction 3.68 vs 6.68, crossover extras F4 at 1900 -> before:2)"
    - "The late-entry rule holds for any passage length, any lateness, and with a stray opening note or a missing opening note (D-02, review 03-01 HIGH round 1, review 03-03 HIGH round 2): the origin never moves and deviationMs is always measured from it; pitch correspondence is also evaluated at every whole-click lag h from 0 to the lag bound H, where H is the largest (nearest click index minus origin index) over the first lateEntryProbeTokens (3) played tokens, and a lag above 0 pays one constant lateEntry cost; the contract shows with numbers twelve notes one beat late (2 vs 40.8/40), the same with an extra C6 on the origin (5 vs 39, which a bound taken from the first event alone would lose), and with the opening C4 missing (6 vs 41)"
    - "A pass is judged only once it is final (review 03-02 HIGH): final means the session has ended, or the recorded click timeline contains an accented click at or after the pass start and a click at or after pass end + (markGraceBeats + 1) local beats; once final, appending clicks never changes Align.stableFields(result) — the PassResult with expectedTime removed from every slot whose reached is false — so the live verdicts equal the replayed ones; an unreached slot's expectedTime may change from null to a number and nothing reads it (review 03-02 HIGH round 2)"
    - "Extra-note denominators are consistent and never overstated (review 03-01 HIGH round 1, review 03-01 MEDIUM round 2): a gap is base-assessable in a pass when the pass has no whole-pass reason and slot k-1 (slot 0 for k = 0) is reached for before:k, or slot k is assessed for within:k; a base-assessable gap is ambiguity-adjacent when an ambiguousPlayed note carries that gap key or a bracketing slot (k-1 or k for before:k, k for within:k) is unassessed ambiguous; with at least one confirmed extra it counts in assessedPasses and passesWithExtra and, if ambiguity-adjacent, in lowerBoundPasses (the sentence then says 'at least'); with no confirmed extra and ambiguity-adjacent it counts in unassessedPasses; lowerBoundPasses <= passesWithExtra <= assessedPasses always holds"
    - "FLAGGED ASSUMPTION — reach clock, awaiting the user's decision (review 03-01 HIGH round 2, conflicts with the round-1 plan, not with D-11): under a late reading, whether a score note was reached is decided by the single policy switch tunables.reachClock; the executable default is 'origin' (D-11 as written: reach from the fixed-origin expected times, so C D E played one beat late and abandoned gives F missed, G not-reached, 6 vs 13); the alternative 'reading' (reach from the late reading's times: F and G not-reached, 2 vs 13) is documented beside it and pinned by its own fixture; the rung-1 piano gate (03-05) presents this exact scenario and records the user's choice before rung 2; flipping touches DEFAULT_TUNABLES.reachClock, the contract's reach-clock paragraph and the one test asserting the default"
    - "Tempo membership (review 03-05 HIGH): a pass is tempo-changed only when a click with pageTime strictly before the pass end, at or after its origin, carries a bpm different from the origin click; a click at or after the end (even one whose label already carries a new BPM) is interpolation support, never evidence of a change"
    - "Chord rules (review 03-03 HIGH): a re-struck pitch within chordWindowMs stays in the open token as a duplicate and is an extra within that slot, so C E C G inside the window is all three chord notes played plus one extra C; inside a paired chord the wrong/missed split is the minimum total semitone distance assignment, and when two minimal assignments label different noteheads those noteheads are unassessed ambiguous, never resolved by pitch order"
    - "Twelve plain-JSON rung-1 fixtures under test/fixtures/align/ carry a score model, a fifteen-click timeline, a pass and hand-derived expected labels; test/align-fixtures.test.cjs validates their shape against the contract under node:test with no alignment code present, including exact expectation coverage — for an attempt the keys of expected.notes are exactly the model's note ids, for a non-attempt expected.notes is empty (review 03-01 MEDIUM round 3) — and npm test stays green (ANLZ-03, D-21)"
    - "Boundary contract (ANLZ-01 boundary probe, explicit): a slot whose expected time equals pass end + grace exactly is reached, one millisecond later is not-reached; two note-ons exactly chordWindowMs apart are one token, one millisecond more are two; a one-note pass whose note is exactly half a beat after the origin gives lag bound 0 (ties to the earlier click), one millisecond more gives 1; a click exactly at the pass end is not tempo evidence, one millisecond earlier is; isFinal is false with the last click one millisecond before end + 1.5 beats and true exactly at it"
    - "Adjacency contract (ANLZ-01 adjacency probe, explicit): a re-struck pitch inside the chord window joins the open token as a duplicate (D-09 extra within:k); an extra played exactly at a slot's expected time is attached to the gap after that slot (before:k+1); an expected time exactly at the mark counts as reached"
    - "Empty-input contract (ANLZ-01 empty probe, explicit): a zero-note pass yields attempt:false with no note labels (D-14); a pass with no accented click at or after its start yields originIndex null, every score note unassessed not-reached, every played note an extra at before:0, and gap before:0 unassessed; a one-slot score aligns without special cases"
    - "Ordering contract (ANLZ-01 ordering probe, explicit): played notes are consumed in seq order never timestamp order; slots follow the score model's canonical note order; extras are listed by seq; a cost tie that would change any assignment (across alignments, lags, or in-chord pairings) is reported ambiguous rather than broken by order"
    - "Decreasing timestamps (review 03-01 HIGH round 3): tokens are formed in seq order and a note-on joins the open token only when the absolute difference between its timeStamp and the token's first note-on is at most chordWindowMs, so a note-on whose timestamp is earlier than the open token's by more than the window opens its own token instead of merging; raw records are never reordered or modified; every worked example whose events are in time order lists them with seq in that same order — the crossover with E at 2600 is the chronological sequence C 1000, D 1500, F 1900, F 2500, E 2600, G 3000 (E wrong played F4, extra E4 at before:4, 6.68 vs 14.08), never a clone of the correction with only E's timestamp changed"
    - "Restart over optimal alternatives (review 03-03 MEDIUM round 3): a pass is restarted only when every optimal alternative — every co-optimal alignment in every minimum-cost reading — inserts at least one token of the second prefix run; when the alternatives disagree the pass is not restarted and the disagreeing slots and tokens are ambiguous by the tie rule; rung1-restart-tie.json (perBeat 1, lateEntry 1: C D E then C D E F G from 2500, lag 0 and lag 2 both cost 15) pins it"
    - "Precision contract (ANLZ-01 precision probe, explicit): onsets are compared with ScoreModel.compareRationals never floats; expected times are page-time milliseconds from recorded clicks with linear interpolation between two real clicks for fractional onsets; alignment costs are integers in thousandths of a weight unit (the timing term rounded once), so equal-cost alternatives compare exactly with no epsilon; deviationMs is each matched event's own timeStamp minus its expected time, rounded only for display"
    - "scripts/check-paint.cjs exists, launches real headless Chrome with the fake FP-60X and drives: pass 1 C D F F G marked as soon as its clicks exist; pass 2 C D E F marked 300 ms after F's click before G's click is recorded (pending, then G missed); pass 3 clean; a resize; reopening the same file while capture is live (painting continues, same session); a mid-session switch to rung 3 and back; a reload whose fills equal the live fills; Start pressed while a restoration is delayed in flight (the live session stays on screen, never overwritten by the restored one); an unreadable MusicXML file loaded during live capture (capture ends with endReason piece-unloaded, the metronome stops, no page error, and the session's marks come back when rung 1 is reopened). Each group prints its OK paint line as soon as its own assertions pass and the first failing group prints one FAIL paint line and exits 1, so an earlier OK line is visible even when a later group fails (review 03-01 MEDIUM round 2). Before the tracer lands it exits 1 with a FAIL paint line, never 2 (AGGR-03 slice, RESEARCH Pitfall 1, review 03-02 HIGH x2 round 1, review 03-02 HIGH round 2, review 03-02 HIGH x2 round 3)"
  artifacts:
    - path: "docs/analysis-rules.md"
      provides: "The interpretation contract every fixture cites (D-21)"
      contains: "D-21"
    - path: "test/fixtures/align/rung1-wrong-e.json"
      provides: "Hand-derived expected labels for the tracer's one path"
      contains: "\"status\": \"wrong\""
    - path: "test/align-fixtures.test.cjs"
      provides: "Shape validation of every fixture against the contract, runnable with no align.js"
      contains: "fixtures/align"
    - path: "scripts/check-paint.cjs"
      provides: "Headless-Chrome end-to-end proof of painting, finalization, detail regeneration, resize, same-file reopen, piece switching, live-equals-replay, Start during a delayed restoration and an unreadable file during capture, with per-group OK lines printed immediately"
      contains: "OK paint"
  key_links:
    - from: "test/fixtures/align/*.json"
      to: "docs/analysis-rules.md"
      via: "each fixture's rule field names the D-number whose section names the fixture file"
      pattern: "\"rule\": \"D-"
    - from: "scripts/check-paint.cjs"
      to: "src/capture-app.js"
      via: "CaptureApp.start(), CaptureApp.mark('spacebar', ts), CaptureApp.state.clicks page times, CaptureApp.analysis.pendingOrdinals, #analysisHeading, #detail"
      pattern: "CaptureApp\\.mark"
  prohibitions:
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT derive a fixture's expected labels by running any alignment code; every expected block is hand-derived from docs/analysis-rules.md before src/align.js exists (D-21, Phase 1 pattern 'never paste extractor output into a test expectation')"
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT write a rule into docs/analysis-rules.md that moves the pass origin, changes expected times or deviationMs, or otherwise makes playing appear on time; the late-entry reading only chooses which played note is compared with which score note (D-02, user's words)"
    - requirement_id: ANLZ-01
      category: values
      statement: "MUST NOT define any velocity floor, bounce suppression or note filter; every non-marker note-on in a pass is a played note (D-09)"
---

## Phase Goal

**As a** pianist drilling right-hand C D E F G alone at the FP-60X, **I want to** see wrong, missed and extra notes painted on the notation after each pass, with honest counts across the passes at one tempo, **so that** I can tell a recurring mistake from a one-off slip and trust what the score shows me.

<objective>
Write the interpretation contract before any alignment code exists (D-21 is a locked decision, so this plan leads with the contract rather than the tracer; the tracer is plan 03-02 and is the very next thing built): `docs/analysis-rules.md` states every rule D-01 to D-21 with a worked rung-1 example and the fixture that pins it, fixes the result shapes and the tunables table, and twelve hand-derived JSON fixtures plus a shape-validation test make the contract executable. The same plan writes the headless-Chrome harness `scripts/check-paint.cjs` that proves the tracer end to end at the real DOM, including the live-versus-replay and piece-switch cases the cross-AI review found: it is red now (exit 1, not 2) and goes green when 03-02 lands.

This revision incorporates the Round 1 cross-AI review (see the Review Dispositions Ledger at the end): the late-entry rule is made length-independent, pass finalization against the scheduler horizon is defined, gap denominators are made consistent, tempo membership excludes clicks at or after the end, chord re-strikes and equidistant in-chord pairings are defined, and every worked number is reconciled.

Round 2 (58daf59) adds: the reach rule under a late reading is isolated behind one named switch, `reachClock`, whose default `'origin'` is D-11 as written, while the user's decision is pending (the rung-1 piano gate asks it); the lag bound comes from the first three played tokens, so one stray opening note cannot disable the late reading; the finalization guarantee is stated over `stableFields` instead of the whole PassResult; extra-note counts next to an ambiguous region are either marked as a minimum ("at least") or left unassessed, never presented as exact; the harness prints each OK line as soon as its group passes and gains a same-file reopen group.

Round 3 (2098322) adds: the crossover example with E at 2600 is its own chronological event sequence (the old clone put E before the F at 2500 in seq order and made them one token, 7.36 instead of 6.68), and the tokenizer rule states how a decreasing timestamp is handled without touching raw records; restart detection is defined over every optimal alternative, with a tie fixture; the fixture shape test requires an expectation for every model note; the harness gains two lifecycle groups (Start during a delayed restoration, an unreadable file during live capture), nine OK lines after 03-02.

Purpose: ROADMAP Phase 3 requires "Document these rules with examples before coding"; the STATE.md blocker says cost weights are tunable data derived from fixture failures, which needs the fixtures to exist first; the E2E harness written before the code is the executable spec for the DOM ids and strings the tracer must produce.

Output: docs/analysis-rules.md, test/fixtures/align/*.json (12 files), test/align-fixtures.test.cjs, scripts/check-paint.cjs, package.json `check:paint` script.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-what-you-actually-played/03-CONTEXT.md
@.planning/phases/03-what-you-actually-played/03-RESEARCH.md
@.planning/phases/03-what-you-actually-played/03-PATTERNS.md
@.planning/phases/03-what-you-actually-played/03-REVIEWS.md
@src/score-model.js
@src/pass-segmenter.js
@src/metronome.js
@src/capture-app.js
@src/score-renderer.js
@scripts/check-capture-roundtrip.cjs
@test/score-model.test.cjs
@test/pass-segmenter.test.cjs

<interfaces>
<!-- Existing shapes the contract and fixtures must match exactly. Extracted from the codebase. -->

From src/score-model.js (rung 1 model, verbatim from test/score-model.test.cjs):
- ModelNote: { id, measure, staff, voice, onset: {num, den, beats}, duration: {num, den, beats}, pitch: {name, midi}, tiedNoteCount }
- id = `m${measure}-s${staff}-v${voice}-b${onset.num}_${onset.den}-p${midi}` via ScoreModel.deriveNoteId({measure, staff, voice, onset, midi})
- Rung 1 ids: m1-s1-v1-b0_1-p60 (C4), m1-s1-v1-b1_1-p62 (D4), m1-s1-v1-b2_1-p64 (E4), m1-s1-v1-b3_1-p65 (F4), m1-s1-v1-b4_1-p67 (G4); measures: [{ number: 1, start: {num:0,den:1,beats:0}, length: {num:5,den:1,beats:5}, timeSignature: {beats:5, beatType:4} }]; schemaVersion 1
- ScoreModel.addRationals(a, b), ScoreModel.compareRationals(a, b) — the only onset arithmetic allowed

From src/pass-segmenter.js (PassSegmenter.segment output, the engine's input):
- pass: { ordinal, startSeq, startTimeStamp, endSeq, endTimeStamp (null for an open trailing pass), noteCount, eventCount, notes }
- pass.notes: non-marker note-on records only: { seq, type: 'noteon', note (MIDI), velocity, timeStamp, marker: false, ... }

From src/metronome.js (verified for the review): SCHEDULE_AHEAD_S = 0.1 and LOOKAHEAD_MS = 25, so a click is recorded only about 100-125 ms before it sounds; advance() pushes each click with bpm = the cursor bpm at emission and then adds 60/bpm, and setBpm() changes only the cursor, so the first click emitted after a change already carries the new bpm label while its own time was computed from the old interval — a label describes the interval that follows its click.

From src/metronome.js + src/capture-app.js onClick (click record as stored by Storage.appendClick):
- click: { id, sessionId, audioTime, pageTime, bar, beat, bpm, accent } with accent === (beat === 1); pageTime shares performance.now()'s origin with every MIDI timeStamp

From src/score-renderer.js (verified for the review): loadPiece() assigns S.model and calls renderAndMap() before dispatching piece-loaded, and capture-app's piece-loaded listener is what ends a live session on a piece change — so anything run from a render hook during a switch already sees the new model.

From src/capture-app.js (surface the harness drives):
- CaptureApp.start() -> Promise<sessionId>; CaptureApp.stop(); CaptureApp.flush(); CaptureApp.mark(source, timeStamp); CaptureApp.state.clicks (live click records with pageTime); CaptureApp.state.session; CaptureApp.state.restored
- DOM ids today: #bpm, #startStop, #passList (li text 'Pass N - K notes'), #status (dataset.check / capture / restore / renderCount)

From src/score-renderer.js:
- ScoreRenderer.state.svgMap: Map<noteId, container element>; the fill must be set on each container's children (RESEARCH Pattern 5); ScoreRenderer.state.renderCount increments on every render (resize re-renders); ScoreRenderer.loadPiece(file)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the interpretation contract docs/analysis-rules.md — every rule, reconciled worked rung-1 examples, late-entry and finalization rules, result shapes, tunables, fixture table</name>
  <reversibility rating="costly">The PassResult, SessionAggregate, PaintView and fixture JSON shapes, the late-entry reading and the finalization rule fixed here are consumed by every fixture, both engine test files, paint.js, capture-app.js and Phase 4/5; changing them later rewrites every fixture and every consumer.</reversibility>
  <files>docs/analysis-rules.md</files>
  <read_first>
    - .planning/phases/03-what-you-actually-played/03-CONTEXT.md (the twenty-one decisions, the Specific Ideas scenario list, and the user's own words on the origin)
    - .planning/phases/03-what-you-actually-played/03-REVIEWS.md (every finding this contract now answers; the ledger at the end of this plan says how)
    - .planning/phases/03-what-you-actually-played/03-RESEARCH.md sections "Pattern 1" to "Pattern 5", "Common Pitfalls", "Open Questions"
    - .planning/ROADMAP.md sections "Interpretation contract" and "Pass origin and timing evidence"
    - test/score-model.test.cjs lines 1-30 (the exact rung 1 model) and fixtures/README.md (rung 4 chords, rung 5 eighth notes)
    - src/metronome.js lines 10-83 (SCHEDULE_AHEAD_S, advance() bpm labelling, setBpm) and src/pass-segmenter.js header comment
  </read_first>
  <action>
Create `docs/analysis-rules.md` (per D-21). Plain prose and tables, no implementation code, no fenced code blocks. Sections in this order.

1. **Purpose and reading guide** (one paragraph): raw events, passes and clicks are the source of truth and never modified; alignment is derived data recomputed on load and after each mark (D-20); every rule names the fixture that pins it; the app says "unassessed" with a reason rather than guessing.

2. **The setting used by every worked example**: rung 1 (`fixtures/01-right-hand.musicxml`), ids `m1-s1-v1-b0_1-p60` … `m1-s1-v1-b4_1-p67`; a synthetic 120 BPM click timeline, clicks 500 ms apart from page time 1000 (bar 1 beat 1 at 1000 … bar 2 beat 1 at 3500, fifteen clicks to 8000); a pass starting at 900 and, unless stated, ending at 3400. Expected times C 1000, D 1500, E 2000, F 2500, G 3000; local beat 500 ms; mark grace 250 ms.

3. **One section per decision, D-01 to D-21**, each with the rule in the user's terms, a worked example with the numbers above, the exact expected labels, and `Fixture: test/fixtures/align/<name>.json` (tagged `(plan 03-03)`, `(plan 03-07)` or `(plan 03-08)` for fixtures a later plan writes) or `Test: test/aggregate.test.cjs`. Pairings — D-01 rung1-clean.json, rung1-origin-after-start.json; D-02 rung1-late-first-note.json, rung1-missing-first-note.json, rung1-whole-beat-late.json, and (03-03) long12-one-beat-late.json, long12-late-opening-extra.json, long12-late-missing-first.json, rung1-three-beats-late.json, rung1-late-wrong-e.json, rung1-late-abandoned-origin.json, rung1-late-abandoned-reading.json; D-03 prose; D-04 rung1-wrong-e.json, rung1-octave-slip.json; D-05 rung1-correction.json; D-06 (03-03) repeated-e-unique.json, repeated-e-tie.json; D-07 (03-08) rung4-chord-mistakes.json, rung4-chord-restrike.json, rung4-chord-equidistant.json and (03-07) rung3-left-hand-wrong.json; D-08 rung1-shifted-start.json; D-09 (03-03) rung1-restart-near-miss.json and (03-08) rung4-chord-restrike.json; D-10 rung1-correction.json; D-11 rung1-not-reached.json, rung1-early-last-note.json, and (03-03) rung1-late-abandoned-origin.json, rung1-late-abandoned-reading.json; D-12 (03-03) rung1-restart.json, rung1-restart-near-miss.json, rung1-two-runs.json, rung1-restart-tie.json; D-13 (03-03) rung1-messy-fumble.json; D-14 rung1-double-mark.json and test/aggregate.test.cjs; D-15 vocabulary table; D-16 (03-03) rung1-tempo-changed.json, rung1-tempo-change-after-end.json and test/aggregate.test.cjs; D-17 to D-19 test/aggregate.test.cjs plus scripts/check-paint.cjs; D-20 prose (`Align.ANALYSIS_VERSION = 1`, nothing persisted, plus the finalization rule of section 5); D-21 this document.

   Worked examples with these exact outcomes (every gap key follows the gap rule in section 7, every cost follows section 4 — re-check each number before committing, per review 03-01 MEDIUM):
   - Late first note (D-02): C at 1200, D E F G on their clicks → all played, C deviationMs +200 (recorded, not judged). Lag bound 2 (probe tokens C, D, E are nearest clicks 0, 1, 2); every late reading costs more than 1.36, so entryLag 0.
   - Skipped first note (D-02): D E F G on their clicks → C missed, D E F G played; the origin stays at 1000. The lag bound is 3 (probe tokens D, E, F), but the one-click-late reading costs 4 substitutions + G deleted + lateEntry = 18 against 4 in time and later lags cost more, so the in-time reading stands.
   - Wrong E (D-04): C D F F G → E wrong played F4. Octave slip C D C5 F G → E wrong played C5 (wrong note 3 vs missed-plus-extra 7).
   - Correction (D-05, D-10): C 1000, D 1500, F 1900, E 2100, F 2500, G 3000 → all five played, one extra F4 at gap before:2 (two slots at or before 1900), E deviationMs +100. Costs: E with E (timing 0.68) plus inserted F (3) against E with F (3 + 0.68) plus inserted E (3): **3.68 vs 6.68**.
   - Crossover ("roughly a slot's width"): with F at 1900 and E at 2300 (events C 1000, D 1500, F 1900, E 2300, F 2500, G 3000, seq 0-5) → E played (2.04 + 3), extra F4 at before:2, **5.04 vs 6.68**. With F at 1900 and E at 2600 the late E now comes after the F at 2500, so the example is its own chronological sequence, seq numbered in time order: C 1000 (seq 0), D 1500 (1), F 1900 (2), F 2500 (3), E 2600 (4), G 3000 (5) — written out as **C D F F E G**, never the correction's events with only E's timestamp moved (review 03-01 HIGH round 3: that clone leaves E before F in seq order, and the tokenizer would then read E 2600 and F 2500 as one chord). Result: E wrong played F4 (3 + 0.68), F played, extra E4 at before:4 (four slots at or before 2600), **6.68 vs 14.08** — crediting E as played would pair E with the E at 2600 (4.08), leave both Fs as extras (3 + 3) and F's slot missed (4), because an F slot cannot pair with an F played before the E it follows. Every later reading costs more (lag 1: 22.36, lag 2: 27.8).
   - Shifted start (D-08): D E F G A on the five clicks → C wrong (D4), D wrong (E4), E wrong (F4), F wrong (G4), G wrong (A4); no extras; **15 vs 20.6** (five substitutions against C deleted + four matches a beat early + A inserted). The lag bound is 2 (probe tokens on clicks 0, 1, 2), but the one-click-late reading costs **21 vs 15** (D inserted, four substitutions, G deleted, lateEntry) and the two-click reading more, so the in-time reading stands.
   - Late entry (D-02, the length-independent rule, review 03-01 HIGH): a whole pass one beat late — C 1500 … G 3500, mark 3900 → all five played, each deviationMs +500, entryLag 1. Show why the lag reading exists: in time alone, five notes still separate (17 vs 19), but twelve quarter notes one beat late cost **40.8 vs 40** (twelve late matches against one deletion, eleven substitutions and one insertion) and rung 1 three beats late costs 51 against 27 (a three-slot shift) — both would lose pitch correspondence. The late reading costs lateEntry (2) in every such case. Late with a wrong note: C 1500, D 2000, F 2500, F 3000, G 3500 → E wrong played F4, others played, entryLag 1, **5 vs 16**.
   - Late entry with a stray opening note (review 03-03 HIGH round 2): an extra C6 at 1000 on the origin, then the twelve quarter notes one beat late (1500 … 7000), mark 7400 → all twelve played, extra C6 at before:0, entryLag 1, **5 vs 39** (C6 inserted + lateEntry, against twelve in-slot substitutions + the last note inserted; the intended in-time correspondence would cost 43.8). The probe tokens C6, C4, D4 give lag bound 2; a bound taken from the first event alone would be 0 and lose this reading. The raw C6 is never filtered: it is an extra (D-09). Accepted limit: three or more stray tokens before a late entry fall back to the in-time reading; lateEntryProbeTokens is tunable.
   - Late entry with the opening note missing (review 03-03 HIGH round 2): the twelve-note passage one beat late with C4 omitted (D4 at 2000 … G5 at 7000), mark 7400 → C4 missed, the other eleven played, entryLag 1, **6 vs 41** (C4 deleted + lateEntry, against two deletions, ten substitutions and one insertion in time).
   - Late and abandoned — the reach clock (review 03-01 HIGH round 2, user decision pending): C 1500, D 2000, E 2500, mark 2700. Both rules agree on pitch correspondence (lag 1: C D E played) and differ only on F. **reachClock 'origin' (the default, D-11 as written):** reach is judged from the fixed-origin expected times C 1000 … G 3000 against 2700 + 250 = 2950, so F (2500) was reached with no matching note → F missed, G not-reached, **6 vs 13** (F deleted + lateEntry, against C deleted and three substitutions in time). **reachClock 'reading' (the alternative):** reach is judged from the late reading's times, F 3000 and G 3500 are after 2950 → F and G not-reached, **2 vs 13**. Both fixtures exist with explicit tunables so neither changes when the default flips. Mark this paragraph "the user chooses at the rung-1 piano checkpoint (03-05); rung 5 checks a complete late performance".
   - Abandoned pass (D-11): C D E on the clicks, mark 2200 → F and G not-reached (2500 > 2450). Early last note: C D E F on the clicks, G at 2900, mark 2950 → all played. Boundary: with no G played, mark 2750 → G missed (3000 ≤ 3000); mark 2749 → G not-reached.
   - Finalization (D-11, D-20, review 03-02 HIGH): the same C D E F with no G and a mark at 2750, live. The metronome records a click only about 100 ms before it sounds, so at the moment of the mark G's click (3000) does not exist yet; the pass is pending — not painted, not counted — until a click at or after 2750 + 750 = 3500 is recorded, then it is judged once: G missed. A reload computes the same result from the stored clicks.
   - Restart (D-12): C D E on the clicks, silence, then C D E F G from 4000, mark 6400 → whole pass restarted, every note reason restarted, no extras; listed, one unassessed pass per note. Near miss: C at 1000 and C again at 1150 (outside the 50 ms window), then D E F G → all played plus an extra C4 at before:1. Two complete runs without a mark are a restart too. The restart verdict never depends on which optimal alignment a traceback happens to pick (review 03-03 MEDIUM round 3): rung1-restart itself has several co-optimal alignments at cost 23, every one inserting part of the second run, so it is restarted. Tied alternatives that disagree: with the fixture tunables perBeat 1 and lateEntry 1, C D E at 1000/1500/2000 then C D E F G at 2500 … 4500, mark 4900 → lag 0 (first run matched, second run's C D E extras) and lag 2 (first run extras, second run matched one beat late, 14 + 1) both cost **15**; one inserts the second run and the other does not, so the pass is not restarted, every slot and token is assigned differently across the alternatives, and all five notes are unassessed ambiguous with all eight played notes ambiguous played notes (gap keys from lag 0, the smallest tied lag), no extras. Fixture: test/fixtures/align/rung1-restart-tie.json (plan 03-03).
   - Messy pass (D-13): C D on the clicks, E 1900 and E 2100, A4 2200, B4 2280, A4 2360, B4 2440, F 2500, G 3000 → C D F G played; E unassessed ambiguous; the two Es are ambiguous played notes (E 1900 at gap before:2, E 2100 at before:3), not extras; four confirmed extras at before:3. Gap before:3 has confirmed extras, so the pass counts as a pass with extras there, but the count is a minimum because one of the two Es may also be an extra (review 03-01 MEDIUM round 2): `Extra notes between E4 and F4: in 1 of 1 assessed passes (at least A4 twice, B4 twice); 1 pass with ambiguous notes nearby`. Gap before:2 has no confirmed extra and an ambiguous E beside it, so it is unassessed in this pass; within:2 is unassessed because slot E is.
   - Repeated pitches (D-06): three quarter-note E4s (3/4, slots 1000/1500/2000): E 1000 and E 2000 → played, missed, played; E 1000 and E 1750 → all three ambiguous (5.7 vs 5.7).
   - Chord (D-07, rung 4 = `fixtures/04-chords.musicxml`, 4/4, half-note chords C4 E4 G4 + C3 at beat 1 and F4 A4 C5 + F3 at beat 3): C3 1000, C4 1005, Eb4 1010, G4 1020, B4 1030, F3 2000, F4 2010, C5 2020 → C3 C4 G4 played, E4 wrong played MIDI 63, extra B4 at within:0; F3 F4 C5 played, A4 missed. Re-strike inside a chord (review 03-03 HIGH): C3 1000, C4 1005, E4 1010, C4 1020, G4 1030 → all four played, one extra C4 at within:0 (the later C4 stays in the token as a duplicate). Equidistant pairing (review 03-03 HIGH): C3 1000, D4 1005, G4 1010 → C3 and G4 played; C4 and E4 unassessed ambiguous because D4 is two semitones from both; D4 is an ambiguous played note. Note that the reviewer's C E G played C F example is not a tie (F4 is one semitone from E4, two from G4): E4 wrong played F4, G4 missed.
   - Tempo change (D-16): clicks 1000, 1500, 2000 labelled 120, then from 2500 labelled 100 (next clicks 3100, 3700, …), pass 900..3400 → tempo-changed (the click at 2500 lies before the end with bpm 100). After the end (review 03-05 HIGH): clicks 1000 … 3000 labelled 120, the click at 3500 labelled 100 (it was computed from the old interval; its label describes the interval after it), pass 900..3400 → not tempo-changed, all played, bpm 120.
   - Double mark (D-14): zero notes → not an attempt; excluded from every count; listed.
   - No origin: a pass starting after the last accented click → originIndex null, every note not-reached, every played note an extra at before:0 listed in the pass view but not counted (gap before:0 is unassessed); bpm from the last click before the start, or ungrouped when none.

4. **Slots, tokens, readings and the cost model** (prose + one table). A *slot* is a distinct absolute onset (measure start plus onset via ScoreModel.compareRationals) holding every score note at that onset across both staves, in canonical order. Expected time = the recorded click at (origin index + reading lag + whole quarter beats), linearly interpolated to the next recorded click for a fractional onset; null when a needed click is absent. *Tokens*: note-ons in seq order; a note-on joins the open token when the absolute difference between its timeStamp and the open token's first note-on is at most chordWindowMs — a pitch already in the token joins as a duplicate; the token's onset is its first note-on in seq order. *Decreasing timestamps* (review 03-01 HIGH round 3): seq order is kept even when a later seq carries an earlier timeStamp (the segmenter already orders by seq, src/pass-segmenter.js); such a note-on is never moved or re-stamped, it joins the open token only within the window measured in either direction, otherwise it opens its own token with its own onset, and its timing cost, deviationMs and gap key use its own timeStamp. Example: C 1000, D 1500, E 2000, then F with timeStamp 1940 (seq 3), G 3000 → F is its own token (60 ms from E's onset) and pairs with F's slot: all played, F deviationMs −560; with F at 1960 instead it joins E's token: E played, F4 an extra at within:2, F missed. The raw pass is byte-identical before and after alignment. Global DP over reached slots × tokens: pair, delete a slot (notes missed), insert a token (notes extras). Pair cost = pitch cost + timing cost: equal pitches pair one-to-one as a multiset (played); remaining slot and token pitches are paired by the assignment with the minimum total semitone distance, each pair a wrong note at `substitution`; leftover slot pitches missed at `deletion` each; leftover token pitches (duplicates included) extras within the slot at `insertion` each; when more than one minimum-distance assignment labels different noteheads, those noteheads are unassessed ambiguous and their played pitches ambiguous played notes (the pair cost is the same either way). Timing cost = `perBeat` × |token first onset − expected| / local beat ms, linear, no threshold. All costs integer thousandths. deviationMs of a played note = its own matched event's timeStamp − the fixed-origin expected time (never the token's first onset, review 03-02 MEDIUM). **Readings (D-02 late entry):** lag bound H = the largest value, over the first `lateEntryProbeTokens` tokens, of (the index of the click nearest that token's onset among clicks at or after the origin, ties to the earlier click) minus the origin index, at least 0 — several tokens, so one stray opening note cannot pin H to 0 (review 03-03 HIGH round 2). Every lag h from 0 to H is a reading; a reading's pitch correspondence (timing cost) and gap keys use the click lag h later; its total cost is its DP cost plus `lateEntry` when h > 0. **Reach clock (the one switch for the pending user decision, review 03-01 HIGH round 2):** a slot is reached when its expected time is not null and is at or before end + markGraceBeats × localBeatMs(b), where b is the last click at or before the end (the origin index when none) and localBeatMs(i) is the gap from click i to the next recorded click (the previous gap when none); with `reachClock` `'origin'` (default, D-11 as written) the expected time used is the lag-0 time in every reading, so all readings share one reach set; with `'reading'` it is the reading's lagged time. The two agree at lag 0. No other rule reads this switch. The cheapest reading is chosen; entryLag = its h. deviationMs and the reported origin always use lag 0 — the origin never moves. **Ties:** two optimal alignments (within a reading or across equally cheap readings) that assign a slot or token differently make it ambiguous, extended over a run of consecutive identical-pitch-set slots (D-06); label-identical optimal readings report the smaller lag; equally cheap readings with different labels report entryLag = the smallest tied lag, and that lag's times give the gap keys of the ambiguous played notes. **Restart rule:** a second run of tokens matching the pitch sets (unique pitches) of the first restartPrefixSlots slots, starting after the first run ends, where every optimal alternative (every co-optimal alignment of every minimum-cost reading) inserts at least one token of that run → restarted; when some optimal alternative pairs every token of the run, the pass is not restarted, and the tokens and slots on which the alternatives disagree are ambiguous under the tie rule (review 03-03 MEDIUM round 3). **Tempo rule:** clicks at or after the origin and strictly before the pass end must share the origin click's bpm, else tempo-changed. Precedence: tempo-changed > restarted > per-slot not-reached / ambiguous.

5. **When a pass is judged (finalization)**: a completed pass is final when the session has ended, or when the recorded timeline holds an accented click at or after the pass start and its last click is at or after pass end + (markGraceBeats + 1) × localBeatMs(b) (b as in the reach rule). Only final passes are painted and counted; a pending pass is listed as finishing. Once final, appending clicks never changes `stableFields(result)` = the PassResult with `expectedTime` removed from every slot whose `reached` is false (review 03-02 HIGH round 2). Why that is the exact stable set: the clicks are a contiguous prefix whose last click lies after end + grace, so the origin, tempo membership, the lag bound, every reach decision, every gap key, every cost and every label are computed from clicks the prefix already holds; only a slot beyond the covered timeline (never reached) can gain an expected time later, e.g. a pass 3600..3800 whose origin is the accented click at 6000 reports D's expectedTime null on the prefix ending at 6000 and 6500 on the full timeline. Nothing in aggregation, painting or the sentences reads an unreached slot's expectedTime. Property pinned in test/align.test.cjs by 03-02.

6. **Tunables table** — `chordWindowMs 50`, `markGraceBeats 0.5`, `restartPrefixSlots 3`, `lateEntryProbeTokens 3`, `weights.substitution 3`, `weights.deletion 4`, `weights.insertion 3`, `weights.perBeat 3.4`, `weights.lateEntry 2` — columns "why this value" and "changed from fixture failure" (empty now). A separate row, `reachClock 'origin'`, is marked "policy switch — user decision pending (D-11); changed only by the user's recorded choice, never by a fixture failure". A fixture's `tunables` is null (defaults) or a partial object deep-merged over the defaults. The inequalities any future weights must keep, stated for passages of any length: substitution < perBeat (shifted pitches on the clicks stay wrong at every length, since n × substitution < deletion + insertion + (n − 1) × perBeat); substitution < deletion + insertion (a wrong note beats missed-plus-extra); 0 < lateEntry < deletion + insertion − substitution (a late entry with one wrong note still reads late on two or more notes, and an in-time reading that explains the pass as well stays the default). A value changes only when a named fixture fails, recorded here with the fixture name.

7. **Shapes** (field lists, no code): fixture `{ name, rule, description, scoreModel, clicks, pass, tunables (null = defaults), expected: { attempt, originIndex, bpm, wholeReason, entryLag?, notes: { noteId: { status, playedPitch?, reason? } }, extras: [{ gap, pitch }] } }`; `PassResult { analysisVersion, passOrdinal, attempt, originIndex, originTime, bpm, endTime, entryLag, totalCost, wholeReason, slots: [{ index, noteIds, expectedTime, reached }], notes: { noteId: { status: played|wrong|missed|unassessed, playedPitch?, playedSeq?, deviationMs?, reason? } }, extras: [{ seq, pitch, timeStamp, gap }], ambiguousPlayed: [{ seq, pitch, timeStamp, gap }] }` (an ambiguous played note carries the gap key it would have as an extra, so the aggregate can tell which gaps are ambiguity-adjacent); gap keys `before:k` (k = number of slots whose expected time in the chosen reading is at or before the note's time) and `within:k`; `stableFields(result)` = the PassResult with `expectedTime` removed from unreached slots (section 5); `SessionAggregate { analysisVersion, attempts, tempoGroups: [{ bpm, passOrdinals, attempts, notes: { noteId: NoteCounts }, gaps: { gapKey: GapCounts } }], nonAttempts, tempoChanged, ungrouped }`; `NoteCounts { played, wrong, missed, assessed, unassessed: { not-reached, restarted, ambiguous }, unassessedTotal, wrongPitches }`; `GapCounts { passesWithExtra, totalExtras, assessedPasses, unassessedPasses, lowerBoundPasses, pitches }` with the gap rule from the must_haves (review 03-01 HIGH round 1 and MEDIUM round 2): base-assessable when the pass has no whole-pass reason and, for before:k, slot k−1 (slot 0 for k = 0) is reached, or, for within:k, slot k is assessed; ambiguity-adjacent when an ambiguousPlayed entry carries that gap key or a bracketing slot (k−1 or k for before:k; k for within:k) is unassessed ambiguous; not base-assessable → unassessedPasses; base-assessable with at least one confirmed extra → assessedPasses, passesWithExtra, totalExtras and pitches from the confirmed extras, plus lowerBoundPasses when ambiguity-adjacent; base-assessable with no confirmed extra and ambiguity-adjacent → unassessedPasses; otherwise assessedPasses; invariants lowerBoundPasses ≤ passesWithExtra ≤ assessedPasses; reaching a gap alone never makes "no extra there" a confident verdict when an ambiguous note could belong to it; `PaintView { kind: session|pass, label, bpm, noteKinds, glyphs: { gapKey: { count, text } } }` with the D-18 colour rule (larger of wrong/missed, tie → wrong; assessed 0 → untested; no mistakes → clean; pass view played → clean, unassessed → untested).

8. **Detail-panel sentences** (D-19): aggregate with mistakes `E4, bar 1 beat 3: wrong in 4 of 10 assessed passes (played F4 three times, D#4 once); missed in 1 of 10; 2 passes unassessed (1 restarted, 1 not reached)` — the first mistake segment says `N of M assessed passes`, later ones `N of M`; zero segments omitted (so a note only ever missed reads `G4, bar 1 beat 5: missed in 1 of 2 assessed passes`); wrong pitches by count descending then MIDI ascending; once / twice / N times; unassessed `1 pass unassessed` / `N passes unassessed`, reasons in order not reached, restarted, ambiguous; without mistakes `C4, bar 1 beat 1: played correctly in all 3 assessed passes` plus `; 1 pass unassessed (1 not reached)` when non-zero; zero assessed `E4, bar 1 beat 3: not assessed in any pass (3 not reached)`; no passes `E4, bar 1 beat 3: no passes yet`; single pass `: wrong, played F4` / `: played` / `: missed` / `: unassessed (not reached)`, with the reason words `not reached`, `restarted`, `ambiguous`, `tempo changed`; gap aggregate `Extra notes between D4 and E4: in 6 of 10 assessed passes (F4 five times, E4 once)` plus `; 2 passes unassessed` when non-zero, with `Extra notes before C4`, `Extra notes after G4`, `Extra notes played with E4` (a multi-note slot is `the chord at bar 1 beat 1`); when lowerBoundPasses > 0 the pitch list starts with `at least ` and the sentence ends with `; 1 pass with ambiguous notes nearby` / `; N passes with ambiguous notes nearby` after any unassessed segment (review 03-01 MEDIUM round 2: `Extra notes between E4 and F4: in 1 of 1 assessed passes (at least A4 twice, B4 twice); 1 pass with ambiguous notes nearby`); gap single pass `Extra notes between D4 and E4: F4`, several extras comma-separated in seq order, and `Extra notes between E4 and F4: at least A4, B4, A4, B4 (ambiguous notes nearby)` when that gap is ambiguity-adjacent in the pass. Beat = onset.beats + 1, fractional as `beat 2.5`. Played pitches spelled by MidiCapture.noteName (sharps: Eb4 prints D#4). Heading `This session at 120 BPM - 3 passes` (`1 pass`), with ` - finishing pass N` while a pass is pending; tempo option `120 BPM (4 passes)`; default detail text `Click a coloured notehead or a + to see what happened there.` The panel remembers the last clicked note or gap and rewrites its sentence from the current view on every repaint; a remembered gap with no glyph in the view, or a session or piece change, returns it to the default text (review 03-04 MEDIUM).

9. **Colours, glyphs and views** (D-17, D-18): wrong `#d00000`, missed `#0050d0`, clean `#000000`, untested `#b0b0b0`; extra glyph red `+N` (session) / `+` (pass). Glyph x for before:k between slots on one system = midpoint of the previous slot's right edge and the next slot's left edge; when the next slot's left edge is left of the previous slot's right edge (a system break) the glyph sits 12 px right of the previous slot on its line (review 03-04 MEDIUM); before:0 12 px left of slot 0; after the last slot 12 px right of it; within:k the centre of slot k; y 18 px above the highest notehead used. Two views; BPM selector only with more than one tempo group; repaint after every final pass, on load, after a resize, through the current svgMap; a session is painted only on the score it was recorded against.

Do not include timing verdicts beyond "deviationMs is recorded" (Phase 4), intensity or bar shading (Phase 5), or persisting results (deferred).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/analysis-rules.md','utf8');const missing=[];for(let i=1;i<=21;i++){const id='D-'+String(i).padStart(2,'0');if(!s.includes(id))missing.push(id);}for(const f of ['rung1-clean.json','rung1-late-first-note.json','rung1-missing-first-note.json','rung1-wrong-e.json','rung1-octave-slip.json','rung1-correction.json','rung1-shifted-start.json','rung1-not-reached.json','rung1-early-last-note.json','rung1-origin-after-start.json','rung1-double-mark.json','rung1-whole-beat-late.json','long12-one-beat-late.json','long12-late-opening-extra.json','long12-late-missing-first.json','rung1-three-beats-late.json','rung1-late-wrong-e.json','rung1-late-abandoned-origin.json','rung1-late-abandoned-reading.json','repeated-e-unique.json','repeated-e-tie.json','rung1-messy-fumble.json','rung1-restart.json','rung1-restart-near-miss.json','rung1-two-runs.json','rung1-restart-tie.json','rung1-tempo-changed.json','rung1-tempo-change-after-end.json','rung1-no-origin.json','rung4-chord-mistakes.json','rung4-chord-restrike.json','rung4-chord-equidistant.json','rung3-left-hand-wrong.json','not-reached','restarted','ambiguous','tempo-changed','before:2','within:0','before:4','perBeat','lateEntry','entryLag','unassessedPasses','chordWindowMs','markGraceBeats','restartPrefixSlots','ANALYSIS_VERSION','PaintView','SessionAggregate','PassResult','3.68 vs 6.68','5.04 vs 6.68','6.68 vs 14.08','C D F F E G','absolute difference','every optimal alternative','15 vs 20.6','21 vs 15','40.8 vs 40','5 vs 16','5 vs 39','6 vs 41','6 vs 13','2 vs 13','reachClock','lateEntryProbeTokens','stableFields','lowerBoundPasses','at least A4 twice, B4 twice','ambiguous notes nearby','tempo changed','finishing','played correctly in all','no passes yet','#d00000','#0050d0','#b0b0b0']){if(!s.includes(f))missing.push(f);}if(missing.length){console.log('MISSING: '+missing.join(', '));process.exit(1);}console.log('contract complete')"</automated>
    <fails_when>non-zero exit, or a line starting "MISSING:" instead of "contract complete"</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/analysis-rules.md` exists and `git ls-files --error-unmatch docs/analysis-rules.md` exits 0 after the commit
    - The verify command prints `contract complete` (every D-number, every fixture name for this and later plans, the reason words, gap keys, tunable names including lateEntry, shape names and the reconciled cost strings are present)
    - `grep -c 'the user chooses at the rung-1 piano checkpoint' docs/analysis-rules.md` prints at least 1 and `grep -c 'user decision pending' docs/analysis-rules.md` prints at least 1 (the reach clock is flagged as the user's open decision, review 03-01 HIGH round 2)
    - `grep -c "reachClock 'origin'" docs/analysis-rules.md` prints at least 1 (the executable default is D-11 as written)
    - `grep -c 'before:3' docs/analysis-rules.md` prints at least 1 and the crossover paragraph places F at 1900: `grep -c 'F at 1900' docs/analysis-rules.md` prints at least 1 (review 03-01 MEDIUM)
    - The 2600 ms crossover is chronological (review 03-01 HIGH round 3): `grep -c 'C D F F E G' docs/analysis-rules.md` prints at least 1, `grep -c '6.68 vs 14.08' docs/analysis-rules.md` prints at least 1, and `grep -c '7.08 vs 6.68' docs/analysis-rules.md` prints 0; the decreasing-timestamp rule is written: `grep -c 'Decreasing timestamps' docs/analysis-rules.md` prints at least 1 and `grep -c 'F deviationMs −560' docs/analysis-rules.md` prints at least 1
    - Restart over optimal alternatives (review 03-03 MEDIUM round 3): `grep -c 'every optimal alternative' docs/analysis-rules.md` prints at least 1 (the restart rule) and `grep -c 'rung1-restart-tie.json' docs/analysis-rules.md` prints at least 1
    - `grep -c 'passesWithExtra' docs/analysis-rules.md` prints at least 2 (field and invariant)
    - `grep -c 'strictly before the pass end' docs/analysis-rules.md` prints at least 1 (tempo membership)
    - `grep -c 'thousandths' docs/analysis-rules.md` prints at least 1; `grep -c 'changed from fixture failure' docs/analysis-rules.md` prints at least 1
    - No fenced code block: `grep -c '^```' docs/analysis-rules.md` prints 0
  </acceptance_criteria>
  <done>The contract reads as a complete rulebook a pianist could check marks against, every number in it is consistent with its own rules, and the late-entry, finalization, gap-denominator, tempo-membership and chord rules the review asked for are fixed before any code.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-derive the twelve rung-1 fixtures under test/fixtures/align/ and pin their shape with test/align-fixtures.test.cjs</name>
  <files>test/fixtures/align/rung1-clean.json, test/fixtures/align/rung1-late-first-note.json, test/fixtures/align/rung1-missing-first-note.json, test/fixtures/align/rung1-wrong-e.json, test/fixtures/align/rung1-octave-slip.json, test/fixtures/align/rung1-correction.json, test/fixtures/align/rung1-shifted-start.json, test/fixtures/align/rung1-not-reached.json, test/fixtures/align/rung1-early-last-note.json, test/fixtures/align/rung1-origin-after-start.json, test/fixtures/align/rung1-double-mark.json, test/fixtures/align/rung1-whole-beat-late.json, test/align-fixtures.test.cjs</files>
  <read_first>
    - docs/analysis-rules.md (Task 1 — the shapes section and each worked example; the expected blocks below must agree with it)
    - test/score-model.test.cjs lines 1-30 (copy the rung 1 model object exactly: ids, onsets, durations, pitches, measures)
    - test/pass-segmenter.test.cjs (the `ev()` helper style and the require pattern; the fixtures test mirrors its header)
    - src/score-model.js (ScoreModel.deriveNoteId, used by the fixtures test to re-derive every note id)
  </read_first>
  <action>
Write twelve plain-JSON fixture files, each self-contained: `name`, `rule` (the D-number), `description` (one sentence), `scoreModel` (the rung 1 model verbatim: schemaVersion 1, title "Rung 1 - Right hand C D E F G", the one measure, the five notes with exact rationals and ids), `clicks` (fifteen records i = 0..14: id i+1, sessionId 1, audioTime 0.1 + 0.5·i, pageTime 1000 + 500·i, bar 1 + floor(i/5), beat (i mod 5) + 1, bpm 120, accent = beat === 1 — fifteen so every fixture's timeline covers its pass end plus one and a half beats and is therefore final under the contract's finalization rule), `pass` ({ ordinal 1, startSeq 0, startTimeStamp 900, endSeq = last seq, endTimeStamp 3400, noteCount, eventCount 0, notes }), `tunables` null, and `expected`. Each played note is `{ seq, type: "noteon", note, velocity: 80, timeStamp, marker: false }` with seq counting from 0 in time order. Derive every expected block by hand from the contract — never from a program. Do not put `entryLag` in these twelve expected blocks (03-02 implements only the in-time reading; 03-03 adds entryLag assertions on its own fixtures). The twelve files:

- `rung1-clean.json` (D-01): 60@1000 62@1500 64@2000 65@2500 67@3000 → attempt true, originIndex 0, bpm 120, wholeReason null, all five played, extras [].
- `rung1-late-first-note.json` (D-02): 60@1200, rest as clean → all played, extras [].
- `rung1-missing-first-note.json` (D-02): 62@1500 64@2000 65@2500 67@3000 → C missed, D E F G played, extras [].
- `rung1-wrong-e.json` (D-04): 60@1000 62@1500 65@2000 65@2500 67@3000 → E `{ status: "wrong", playedPitch: 65 }`, others played, extras [].
- `rung1-octave-slip.json` (D-04): 60 62 72@2000 65 67 on the clicks → E wrong playedPitch 72, others played, extras [].
- `rung1-correction.json` (D-05, D-10): 60@1000 62@1500 65@1900 64@2100 65@2500 67@3000 → all played; extras `[{ "gap": "before:2", "pitch": 65 }]`.
- `rung1-shifted-start.json` (D-08): 62@1000 64@1500 65@2000 67@2500 69@3000 → C wrong 62, D wrong 64, E wrong 65, F wrong 67, G wrong 69; extras [].
- `rung1-not-reached.json` (D-11): 60@1000 62@1500 64@2000, endTimeStamp 2200 → C D E played; F and G `{ "status": "unassessed", "reason": "not-reached" }`; extras [].
- `rung1-early-last-note.json` (D-11 grace): 60 62 64 65 on the clicks, 67@2900, endTimeStamp 2950 → all played, extras [].
- `rung1-origin-after-start.json` (D-01, RESEARCH Pitfall 2): startTimeStamp 3200, endTimeStamp 5900, notes 60@3500 62@4000 64@4500 65@5000 67@5500 → originIndex 5, all played, extras [].
- `rung1-double-mark.json` (D-14): notes [], noteCount 0, endTimeStamp 1300 → `expected: { attempt: false, originIndex: 0, bpm: 120, wholeReason: null, notes: {}, extras: [] }`.
- `rung1-whole-beat-late.json` (D-02 mirror of D-08): 60@1500 62@2000 64@2500 65@3000 67@3500, endTimeStamp 3900 → all five played, extras [].

Then write `test/align-fixtures.test.cjs` (node:test, no jsdom, no align.js): read every `*.json` in `test/fixtures/align/` (fail if fewer than 12), and for each assert — it parses; `scoreModel.schemaVersion === 1`; every note id equals `ScoreModel.deriveNoteId({ measure, staff, voice, onset, midi: pitch.midi })` and ids are unique; `clicks` are non-empty, sorted by pageTime ascending, each has integer bpm and `accent === (beat === 1)`; `pass.notes` are all `type === 'noteon'` with `marker === false`, sorted by seq ascending, and `pass.noteCount === pass.notes.length`; `pass.startTimeStamp` is a finite number; `expected.attempt === (pass.notes.length > 0)`; exact expectation coverage (review 03-01 MEDIUM round 3) — when `expected.attempt` is true the sorted keys of `expected.notes` deep-equal the sorted model note ids (none missing, none extra, so no verdict can escape verification by being left out), and when it is false `expected.notes` deep-equals `{}`; every status is one of played / wrong / missed / unassessed; `reason` is present exactly when status is unassessed and is one of not-reached / restarted / ambiguous / tempo-changed; `playedPitch` is present exactly when status is wrong; `expected.entryLag`, when present, is a non-negative integer; every `expected.extras[i].gap` matches `/^(before|within):\d+$/`; `rule` matches `/^D-\d\d$/`; when `expected.originIndex` is not null and `pass.endTimeStamp` is a number, the last click's pageTime is at least `pass.endTimeStamp + 1.5 × (clicks[1].pageTime − clicks[0].pageTime)` (the fixture is final); `tunables` is null or a plain object whose top-level keys are among `chordWindowMs`, `markGraceBeats`, `restartPrefixSlots`, `lateEntryProbeTokens`, `reachClock`, `weights`, with `reachClock`, when present, equal to `'origin'` or `'reading'` (partial tunables are deep-merged over the defaults by the engine; later plans' fixtures use this); and the whole file survives `JSON.parse(JSON.stringify(x))` deep-equal. Name each check in plain English.
  </action>
  <verify>
    <automated>node --test test/align-fixtures.test.cjs</automated>
    <fails_when>non-zero exit (node --test exits 1 when any test fails or a file cannot load), or the summary shows a non-zero fail count or a pass count of 0 — the default spec reporter prints these as "ℹ fail N" and "ℹ pass N", never "# fail"</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit (npm test propagates node --test's exit 1 on any failing test), or a summary line "ℹ fail N" with N greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls test/fixtures/align/*.json | wc -l` prints 12
    - `node -e "const f=require('./test/fixtures/align/rung1-clean.json');process.exit(f.clicks.length===15&&f.clicks[14].pageTime===8000&&f.clicks[10].accent===true?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-whole-beat-late.json');process.exit(Object.values(f.expected.notes).every(n=>n.status==='played')&&f.pass.notes[0].timeStamp===1500&&!('entryLag' in f.expected)?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-wrong-e.json');process.exit(f.expected.notes['m1-s1-v1-b2_1-p64'].status==='wrong'&&f.expected.notes['m1-s1-v1-b2_1-p64'].playedPitch===65?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-shifted-start.json');process.exit(Object.values(f.expected.notes).every(n=>n.status==='wrong')&&f.expected.extras.length===0?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-not-reached.json');process.exit(f.expected.notes['m1-s1-v1-b3_1-p65'].reason==='not-reached'&&f.expected.notes['m1-s1-v1-b4_1-p67'].reason==='not-reached'&&f.pass.endTimeStamp===2200?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-origin-after-start.json');process.exit(f.expected.originIndex===5&&f.pass.startTimeStamp===3200?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-correction.json');process.exit(JSON.stringify(f.expected.extras)===JSON.stringify([{gap:'before:2',pitch:65}])&&f.pass.notes[2].timeStamp===1900?0:1)"` exits 0
    - Exact coverage is enforced: `node -e "const fs=require('fs');let bad=0;for(const n of fs.readdirSync('test/fixtures/align')){const f=JSON.parse(fs.readFileSync('test/fixtures/align/'+n,'utf8'));const k=Object.keys(f.expected.notes).sort().join();const m=f.expected.attempt?f.scoreModel.notes.map(x=>x.id).sort().join():'';if(k!==m){console.log('coverage mismatch: '+n);bad++;}}process.exit(bad?1:0)"` exits 0, and `grep -c 'none missing' test/align-fixtures.test.cjs` prints at least 1 (the check is named in plain English in the shape test)
    - Reporter-independent test result (review 03-05 MEDIUM round 3 — the default reporter prints `ℹ fail 0`, not `# fail 0`): `node --test --test-reporter=tap test/align-fixtures.test.cjs` exits 0 and its summary shows `# fail 0` and `# pass N` with N at least 1; `npm test` exits 0
  </acceptance_criteria>
  <done>Twelve contract-derived fixtures with final-covering click timelines are committed, each validated for shape by a green node:test that needs no alignment code, and the full suite is still green.</done>
</task>

<task type="auto">
  <name>Task 3: Write the headless-Chrome paint harness scripts/check-paint.cjs (red until the tracer lands) and register npm run check:paint</name>
  <precondition>C:\Program Files\Google\Chrome\Application\chrome.exe exists (or CHROME_EXE is set) and the machine has internet access for the pinned OSMD and idb CDN bundles</precondition>
  <files>scripts/check-paint.cjs, package.json</files>
  <read_first>
    - scripts/check-capture-roundtrip.cjs (copy its Chrome launch, DevTools WebSocket `send`/`evaluate`, `pollCapture`, `pollClicks`, `pollRestore`, and `FAKE_MIDI_INIT_SCRIPT` verbatim — same harness, new scenario)
    - scripts/check-svg-map.cjs lines 85-99 and 155-158 (polling `#status` dataset.renderCount after a resize)
    - docs/analysis-rules.md sections "When a pass is judged", "Detail-panel sentences" and "Colours, glyphs and views" (the exact strings and hex values asserted below)
    - src/capture-app.js `mark()`, `start()`, `onClick()`, the piece-loaded listener; src/score-renderer.js `loadPiece()` and `devLoadFixture()` (the XHR-to-File pattern reused for the piece switch)
  </read_first>
  <action>
Create `scripts/check-paint.cjs` following the roundtrip script's structure and exit codes (0 pass, 1 assertion failed, 2 environment problem). The scenario is split into ordered groups; each group collects its own failures and, the moment its last assertion passes, prints its one `OK paint: <what>` line (exact texts in step 10) before the next group starts; the first group with any failure prints one `FAIL paint: ` line joining that group's failures with ` | `, closes Chrome and exits 1 without running later groups (review 03-01 MEDIUM round 2 — so 03-02's tracer can see its first OK line even while a later group still fails). A missing `CaptureApp.analysis` object or any other app-side absence is an assertion failure (exit 1), never exit 2. Every DOM lookup happens inside `evaluate` expressions. Let `fill(id)` mean `ScoreRenderer.state.svgMap.get(id).children[0].getAttribute('fill')` read fresh each time; `waitFinal` mean polling (10 s) until `CaptureApp.analysis.pendingOrdinals.length === 0`; `clickNote(id)` mean dispatching a bubbling `MouseEvent('click')` on the svgMap element for id. Rung-1 ids: C4 `m1-s1-v1-b0_1-p60`, D4 `-b1_1-p62`, E4 `-b2_1-p64`, F4 `-b3_1-p65`, G4 `-b4_1-p67`.

1. Launch headless Chrome with the fake FP-60X init script, navigate to `index.html?fixture=fixtures/01-right-hand.musicxml`, `pollCapture`.
2. Set `#bpm` to 120, `CaptureApp.start()` (awaitPromise, must return a number), assert `CaptureApp.state.audioState === 'running'`.
3. Pass 1 = C D F F G marked as soon as its clicks exist: one awaitPromise evaluate that waits in-page (2 ms interval) until `CaptureApp.state.clicks.length >= 5`, then immediately sends note-on 0x90 for 60, 62, 65, 65, 67 at `clicks[0..4].pageTime` (note-off 0x80 at +100 each) through the fake input and calls `CaptureApp.mark('spacebar', clicks[4].pageTime + 100)`. Assert `clicks[0]` is bar 1 beat 1 accent true. `waitFinal`. Assert fill(E4) `#d00000`, the other four `#000000` (RESEARCH Pitfall 1 — the child, never the container), `#analysisHeading` contains `120 BPM`; `clickNote(E4)` → `#detail` equals `E4, bar 1 beat 3: wrong in 1 of 1 assessed passes (played F4 once)`.
4. Pass 2 = C D E F with G omitted, marked before G's click is recorded (review 03-02 HIGH): one awaitPromise evaluate that waits in-page (2 ms interval) until `clicks.length >= 9`, then in the same callback sends 60, 62, 64, 65 at `clicks[5..8].pageTime` and calls `CaptureApp.mark('spacebar', clicks[8].pageTime + 300)`, returning `{ recorded: clicks.length, pending: [...CaptureApp.analysis.pendingOrdinals], g: fill(G4) }`. Assert recorded is 9, pending includes 2, and g is not `#0050d0` (not judged yet). `waitFinal`. Assert fill(G4) `#0050d0`, fill(E4) `#d00000`, C4 D4 F4 `#000000`; with no new click, `#detail` equals `E4, bar 1 beat 3: wrong in 1 of 2 assessed passes (played F4 once)` (the remembered target regenerated); `clickNote(G4)` → `G4, bar 1 beat 5: missed in 1 of 2 assessed passes`.
5. Pass 3 = clean: wait in-page until `clicks.length >= 15`, send 60 62 64 65 67 at `clicks[10..14].pageTime`, mark at `clicks[14].pageTime + 100`; `waitFinal`. With no new click, `#detail` equals `G4, bar 1 beat 5: missed in 1 of 3 assessed passes`; `clickNote(E4)` → `E4, bar 1 beat 3: wrong in 1 of 3 assessed passes (played F4 once)`; `clickNote(C4)` → `C4, bar 1 beat 1: played correctly in all 3 assessed passes`. Save `snapshot` = the five fills.
6. Resize survival (PITFALLS 13): set `#notation` width to 60%, dispatch a window resize event, poll `#status` dataset.renderCount until `'2'` (10 s), then assert the five fills (read from the rebuilt svgMap) deep-equal `snapshot`.
6b. Reopen the same file while capture is live (review 03-02 HIGH round 2): save `sid = CaptureApp.state.session.id`; XHR-load `fixtures/01-right-hand.musicxml` again as a File (the devLoadFixture pattern) and await `ScoreRenderer.loadPiece(file)`. Poll (10 s) until `#analysisHeading` textContent contains `120 BPM` and `CaptureApp.analysis.session.model === ScoreRenderer.state.model`. Assert `CaptureApp.state.session.id === sid` (same content hash, so the session continued), the five fills deep-equal `snapshot` (painting resumed on the new map instead of staying reset), and `#detail` equals `Click a coloured notehead or a + to see what happened there.` (the model change cleared the remembered target).
7. Piece switch mid-session (review 03-02 HIGH): in-page, register a one-shot `piece-rendered` listener that records every mapped notehead's first-child fill into `window.__switchFills`; XHR-load `fixtures/03-both-hands.musicxml` as a File (the devLoadFixture pattern) and await `ScoreRenderer.loadPiece(file)`. Poll (10 s) until `CaptureApp.state.session === null` and `#analysisHeading` textContent is `No session`. Assert no value in `__switchFills` and no current rung-3 notehead fill is `#d00000`, `#0050d0` or `#b0b0b0` (rung 3 shares the id `m1-s1-v1-b2_1-p64`, so a stale paint would show), and `#detail` equals `Click a coloured notehead or a + to see what happened there.` Then XHR-load `fixtures/01-right-hand.musicxml`, await loadPiece, poll until `CaptureApp.analysis.session` is non-null, `waitFinal`, and assert the five fills deep-equal `snapshot` (the session ended by the switch, replayed against its own score).
8. Reload: navigate to plain `index.html`, `pollRestore`, assert the status line starts with `01-right-hand.musicxml: 1 bars, 5 notes, 5/5 noteheads mapped, map OK`, poll until `CaptureApp.analysis.session` is non-null, `waitFinal`, assert the five fills deep-equal `snapshot` with no interaction (live result equals replay, D-17/D-20), and `clickNote(E4)` gives the step-5 E4 sentence.
8b. Start during a delayed restoration (review 03-02 HIGH round 3 — verified: start() crosses several awaits and nothing invalidated an in-flight loadLatestSession): in one awaitPromise evaluate, save `pid = CaptureApp.state.pieceId` and `orig = Storage.readClicks`; replace `Storage.readClicks` with a wrapper that sets `window.__restoreEntered = true`, waits 1500 ms, then returns `orig(...args)` and sets `window.__restoreDone = true`; call `CaptureApp.loadLatestSession(pid, ScoreRenderer.state.model)` without awaiting it and keep its promise; wait in-page (2 ms interval, 5 s) until `__restoreEntered`; set `#bpm` to 120 and dispatch `change`; `await CaptureApp.start()`; then await the kept promise and restore `Storage.readClicks = orig`. Poll (5 s) until `__restoreDone` and 200 ms more. Assert `CaptureApp.analysis.session.id === CaptureApp.state.session.id`, `CaptureApp.analysis.session.live === true`, `#analysisHeading` contains `- 0 passes`, and no rung-1 notehead fill is `#d00000` or `#0050d0` (the restored session's red E4 and blue G4 must not reappear over the new live session).
8c. An unreadable file during live capture (review 03-02 HIGH round 3 — verified: src/score-renderer.js loadPiece's catch nulls the model and dispatches piece-unloaded, and the running metronome's time-signature callback read the model on screen): in the live session from 8b (start() reset its click list), wait in-page until `CaptureApp.state.clicks.length >= 5`; send 60, 62, 65, 65, 67 at `clicks[0..4].pageTime` — the first bar, so pass 1's origin is click 0; those times may already be in the past, which the fake input accepts because every event carries the timestamp it is given — (note-off +100 each) and `CaptureApp.mark('spacebar', clicks[4].pageTime + 100)`; `waitFinal`; assert fill(E4) `#d00000`; save `sid = CaptureApp.state.session.id`. Set `window.__pageErrors = []` (the init script records every `error` and `unhandledrejection` into it), then `await ScoreRenderer.loadPiece(new File(['this is not MusicXML'], 'broken.musicxml'))`. Poll (5 s) until `CaptureApp.state.session === null`; read `n = CaptureApp.state.clicks.length`, wait 1500 ms, and assert: the click count is still n (the metronome stopped); `window.__pageErrors.length === 0`; `#status` textContent is `No piece loaded`; `#analysisHeading` textContent is `No session`; `CaptureApp.analysis.session.id === sid` with `live === false` (the ended session is retained in memory); and `(await Storage.listSessionsForPiece(CaptureApp.state.db, pid)).find((s) => s.id === sid).endReason === 'piece-unloaded'` (its raw events, passes and clicks are stored). Then XHR-load `fixtures/01-right-hand.musicxml` as a File, await loadPiece, poll (10 s) until `CaptureApp.analysis.session.id === sid` and `CaptureApp.analysis.session.model === ScoreRenderer.state.model`, `waitFinal`, and assert fill(E4) `#d00000` and `#analysisHeading` contains `1 pass` (the attempts recorded before the failed load are back with their piece). Add the error and unhandledrejection collector to the page init script next to the fake MIDI script.
9. Exit 0 after the last group's OK line.
10. The groups and their OK lines, each printed immediately when that group passes, in this order: step 3 `OK paint: wrong E4 painted red on the notehead child after a mark`; step 4 `OK paint: a pass marked before its last click existed is judged once the timeline covers it`; step 5 `OK paint: aggregate and detail sentences over three passes, regenerated after each pass`; step 6 `OK paint: colours survive a resize re-render`; step 6b `OK paint: reopening the same file during capture keeps painting the live session`; step 7 `OK paint: switching pieces never paints one piece's marks on another`; step 8 `OK paint: latest session painted on reload identical to the live result`; step 8b `OK paint: starting a session during a delayed restoration keeps the live session on screen`; step 8c `OK paint: an unreadable file during capture ends the session cleanly and its passes return with the piece` — nine OK lines after 03-02.

Add `"check:paint": "node scripts/check-paint.cjs"` to package.json scripts (others unchanged). Running it now, before plan 03-02, must fail at step 3 with exit code 1 (`CaptureApp.analysis` does not exist), never exit 2 — that proves the Chrome plumbing and click-page-time driving already work.
  </action>
  <verify>
    <automated>node --check scripts/check-paint.cjs && node -e "const p=require('./package.json');process.exit(p.scripts['check:paint']==='node scripts/check-paint.cjs'?0:1)"</automated>
    <fails_when>non-zero exit (syntax error in the script, or the check:paint script entry missing or different)</fails_when>
    <automated>node scripts/check-paint.cjs; test $? -eq 1</automated>
    <fails_when>exit code of the harness is 0 (nothing to paint yet, so a pass is impossible) or 2 (Chrome/env problem), or the output lacks a line starting "FAIL paint:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'OK paint: wrong E4 painted red on the notehead child after a mark' scripts/check-paint.cjs` prints 1; `grep -c 'OK paint: latest session painted on reload identical to the live result' scripts/check-paint.cjs` prints 1; `grep -c "OK paint: switching pieces never paints one piece's marks on another" scripts/check-paint.cjs` prints 1
    - `grep -c 'OK paint: reopening the same file during capture keeps painting the live session' scripts/check-paint.cjs` prints 1 and `grep -c 'state.session.id' scripts/check-paint.cjs` prints at least 1 (review 03-02 HIGH round 2)
    - `grep -v '^\s*//' scripts/check-paint.cjs | grep -c "console.log('OK paint: "` prints at least 9 (one print per group, placed where that group ends rather than collected for the end — review 03-01 MEDIUM round 2)
    - `grep -c 'OK paint: starting a session during a delayed restoration keeps the live session on screen' scripts/check-paint.cjs` prints 1 and `grep -c '__restoreEntered' scripts/check-paint.cjs` prints at least 2 (review 03-02 HIGH round 3, Start during restoration)
    - `grep -c 'OK paint: an unreadable file during capture ends the session cleanly and its passes return with the piece' scripts/check-paint.cjs` prints 1, `grep -c 'broken.musicxml' scripts/check-paint.cjs` prints at least 1, `grep -c "piece-unloaded" scripts/check-paint.cjs` prints at least 1 and `grep -c '__pageErrors' scripts/check-paint.cjs` prints at least 2 (review 03-02 HIGH round 3, failed load during capture)
    - `grep -c "CaptureApp.mark('spacebar'" scripts/check-paint.cjs` prints at least 3 (all marks use recorded click page times)
    - `grep -c 'pendingOrdinals' scripts/check-paint.cjs` prints at least 2 (the pending assertion and the wait)
    - `grep -c 'pageTime + 300' scripts/check-paint.cjs` prints at least 1 (the horizon-edge mark)
    - `grep -c '03-both-hands.musicxml' scripts/check-paint.cjs` prints at least 1 and `grep -c 'piece-rendered' scripts/check-paint.cjs` prints at least 1
    - `grep -c 'children\[0\]' scripts/check-paint.cjs` prints at least 1; `grep -c 'renderCount' scripts/check-paint.cjs` prints at least 1
    - `node scripts/check-paint.cjs` exits with code 1 and prints a line starting `FAIL paint:`
  </acceptance_criteria>
  <done>The E2E harness exists, runs real Chrome end to end, covers the scheduler-horizon and piece-switch cases the review found, and fails only because the analysis layer does not exist yet; plan 03-02 turns it green without changing the harness.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `docs/analysis-rules.md` — the interpretation contract (D-21): rules D-01..D-21, reconciled worked examples, readings and the late-entry rule, finalization rule, cost model, tunables table (including `weights.lateEntry`), shapes (fixture file, PassResult with `entryLag` and `totalCost`, SessionAggregate, NoteCounts, GapCounts with `unassessedPasses`, PaintView), detail-panel sentence templates and regeneration rule, colours and glyph placement rule
- `test/fixtures/align/*.json` — 12 rung-1 fixtures with fifteen-click timelines: rung1-clean, rung1-late-first-note, rung1-missing-first-note, rung1-wrong-e, rung1-octave-slip, rung1-correction, rung1-shifted-start, rung1-not-reached, rung1-early-last-note, rung1-origin-after-start, rung1-double-mark, rung1-whole-beat-late
- `test/align-fixtures.test.cjs` — fixture shape validation (node:test)
- `scripts/check-paint.cjs` — headless-Chrome paint / finalization / detail regeneration / resize / same-file reopen / piece switch / reload / Start during a delayed restoration / unreadable file during capture proof, each OK line printed as its group passes (nine OK lines after 03-02), with a page `error` / `unhandledrejection` collector `window.__pageErrors` in the init script; `package.json` script `check:paint`
- Contract additions from Round 3: the chronological 2600 ms crossover (`C D F F E G`, 6.68 vs 14.08), the decreasing-timestamp tokenizer rule (absolute window, raw records untouched), restart detection over every optimal alternative with the tie example (fixture `rung1-restart-tie.json`, written in 03-03), the entryLag/gap-key rule for equally cheap readings with different labels, and exact expectation coverage in `test/align-fixtures.test.cjs`
- New vocabulary: session `endReason` `'piece-unloaded'` (a failed piece load during capture, implemented in 03-02)
- Vocabulary fixed for later plans: reasons `not-reached`, `restarted`, `ambiguous`, `tempo-changed`; gap keys `before:k`, `within:k`; colours `#d00000`, `#0050d0`, `#000000`, `#b0b0b0`; tunable names `chordWindowMs`, `markGraceBeats`, `restartPrefixSlots`, `lateEntryProbeTokens`, `weights.{substitution,deletion,insertion,perBeat,lateEntry}`; the policy switch `reachClock` (`'origin'` default | `'reading'`); `stableFields`; `GapCounts.lowerBoundPasses`; `ambiguousPlayed[].gap`; app surface `CaptureApp.analysis.pendingOrdinals`, `CaptureApp.analysis.session`

## Flagged assumptions (edge probe, research and review — not silently dropped)

- ANLZ-03 (unclassified probe row): "no DOM, MIDI, or audio dependency" is made checkable in 03-02 by running the engine tests under plain node:test and by a comment-stripped negative grep on src/align.js and src/aggregate.js; stays flagged as manually classified.
- AGGR-03 (unclassified probe row): "click a marked note shows plain-language detail" is pinned by this plan's harness asserting the exact sentences; at the piano the user reads the same panel (03-05 to 03-09). Stays flagged as manually classified.
- Late-entry reading (review 03-01 HIGH, Claude's discretion under D-02/D-08): the in-time cost model cannot keep D-02 for long passages (40.8 vs 40 on twelve notes) and a threshold cannot either (research A3). The contract adds whole-click lag readings bounded by where the first three played tokens actually are (round 2: one stray opening note no longer pins the bound to 0), and deviations always stay measured from the fixed origin. On-click D-08 cases now evaluate a late reading too, which costs more (21 vs 15 on rung 1) — pinned by rung1-shifted-start staying entryLag 0 in 03-03.
- REACH CLOCK — USER DECISION PENDING (review 03-01 HIGH round 2; supersedes the round-1 discretion call that reach follows the lagged times): D-11 says reach is judged from the fixed origin. The round-1 claim that fixed-origin reach turns a late abandoned pass into a cascade was wrong once lag readings exist (the late reading still wins, 6 vs 13; only F changes from not-reached to missed). The plans execute D-11 as written (`reachClock 'origin'`), keep the lagged alternative behind that one switch with its own fixture, and the rung-1 piano gate (03-05 Task 2) shows the user the concrete late-abandoned pass and records their choice before rung 2. Not decided by the planner; 03-CONTEXT.md is not edited here.
- Gap attachment under a late reading (D-10) uses the chosen reading's times, so an extra played between the late D and the late E is shown between D4 and E4 (with fixed-origin times it would be shown one gap later). Claude's discretion, not challenged by review; independent of `reachClock`; the user sees it in 03-05 only if a late pass has extras.
- Accepted consequence of the lag bound: a passage played with transposed pitches starting one beat after the origin could read as a late entry with wrong notes rather than as shifted wrong notes; no ladder rung or scenario plays that. Three or more stray tokens before a late entry fall back to the in-time reading (lateEntryProbeTokens is tunable).
- Finalization (review 03-02 HIGH; wording corrected by review 03-05 MEDIUM round 3): a pass stays "finishing" until the metronome has recorded a click at least a beat and a half after the mark, and the first accented click at or after the pass start. Clicks are recorded only about 100 ms before they sound and land on whole beats, so the wait is usually longer than a beat and a half (the reviewer's example: at 120 BPM a pass ending at 3100 needs a click at or after 3850, the next click is 4000 and is recorded around 3900) and longer again if the pass was marked before its bar-1 click. No fixed bound is promised; the checklists tell the user to watch the label.
- Decreasing timestamps (review 03-01 HIGH round 3): tokens use seq order with an absolute chord window. A note-on stamped earlier than the open token by more than the window starts its own token instead of silently joining a chord. This is Claude's discretion under D-07 ("within a short window ... regardless of order") and the Phase 2 rule that seq, not timestamp, orders events.
- Restart over optimal alternatives (review 03-03 MEDIUM round 3): when optimal alternatives disagree about whether the second run was inserted, the pass is not called restarted; the disagreement is shown as ambiguous notes instead (D-13). Claude's discretion under D-12; the only case found needs non-default weights, which is why the fixture carries tunables.
- Extra counts next to an ambiguous region (review 03-01 MEDIUM round 2, supersedes the round-1 wording): a gap with certain extras counts as a pass with extras, but its count is labelled "at least" and the pass is named in the sentence; a gap with no certain extra next to an ambiguous note is unassessed in that pass rather than counted as extra-free.
- Played-pitch spelling: MidiCapture.noteName spells sharps (D#4), so the user's "Eb4" example prints as D#4 — Claude's discretion (D-19), noted in the contract.

<verification>
- `node --test test/align-fixtures.test.cjs` green; `npm test` green (no red test files committed)
- `node scripts/check-paint.cjs` exits 1 with a `FAIL paint:` line (red harness, env OK)
- `docs/analysis-rules.md` names every fixture and every D-number, and every worked number is reconciled
</verification>

<success_criteria>
- ROADMAP Phase 3 "Interpretation contract" and "Pass origin and timing evidence" paragraphs are satisfied in writing before any alignment code exists (success criterion 6's "rules explain each result"), including a late start on passages of any length
- Every fixture's expected block is traceable to a numbered rule and was derived by hand
- The E2E harness is the executable spec for plan 03-02's DOM ids, sentences, finalization and piece-switch behaviour
</success_criteria>

<output>
Create `.planning/phases/03-what-you-actually-played/03-01-SUMMARY.md` when done, recording the fixture count, the fixtures-test pass count, and the harness's red exit code and FAIL line.
</output>

## Review Dispositions Ledger

### Round 1 — af0835b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-01: cost model fails general late entry (12 notes one beat late 40.8 vs 40; rung 1 three beats late 51 vs 35) | HIGH | Plan 03-01, Task 1: verified (the three-slot shift is even cheaper, 27); contract adds whole-click lag readings bounded by the first played note with a constant `weights.lateEntry` = 2, origin and deviationMs unchanged; inequalities restated for any passage length; must_haves truth 3. Implemented and pinned by fixtures in Plan 03-03, Task 2 |
| 03-01: confident extras in unassessed gaps break passesWithExtra ≤ assessedPasses | HIGH | Plan 03-01, Task 1: verified (messy fixture before:3 next to ambiguous E; no-origin before:0); gap assessability defined from reach and whole-pass reason, extras counted only in assessed gaps, `unassessedPasses` added, invariant stated; must_haves truth 5. Invariant tested in Plan 03-02 |
| 03-01: crossover example F at 2000 labelled before:2 contradicts the ≤ gap rule | MEDIUM | Plan 03-01, Task 1: verified; crossover moved to F at 1900 (before:2 by the rule), every cost recomputed (3.68 vs 6.68, 5.04 vs 6.68, 7.08 vs 6.68) and checked by the verify string list; Plan 03-02 asserts totalCost for each worked example |
| 03-02: scheduler horizon makes live and replayed results disagree; harness hides it by waiting for ten clicks | HIGH | Plan 03-01, Tasks 1 and 3: finalization rule in the contract; harness now marks pass 1 as soon as its clicks exist and pass 2 300 ms after F's click before G's click is recorded, asserts pending then G missed, and compares reload fills with live fills. Implemented in Plan 03-02 |
| 03-02: piece switching analyzes the old session against the new model and exposes old paint to the new map | HIGH | Plan 03-01, Task 3: harness step 7 switches rung 1 → rung 3 (shared note id) mid-session and back, asserting no stale paint at piece-rendered and a correct replay. Implemented in Plan 03-02 |
| 03-02: per-note deviations use the chord token's first onset | MEDIUM | Plan 03-01, Task 1: contract defines deviationMs from each matched event's own timeStamp. Implemented in Plan 03-02 |
| 03-03: in-chord ties resolved by lower pitch | HIGH | Plan 03-01, Task 1: contract uses the minimum total semitone distance assignment and marks noteheads ambiguous when two minimal assignments disagree; notes that the reviewer's C E G / C F example is not itself a tie. Implemented in Plan 03-06 |
| 03-03: a re-strike inside a chord fragments tokens into false misses | HIGH | Plan 03-01, Task 1: tokenizer contract keeps a re-struck pitch in the open token as a duplicate extra within the slot; worked example added. Implemented in Plan 03-02 (tokenizer) and pinned on a chord in Plan 03-06 |
| 03-04: glyph midpoint wrong across a system break | MEDIUM | Plan 03-01, Task 1: placement rule for a system break defined in the contract. Implemented and unit-tested in Plan 03-04 |
| 03-04: detail text goes stale after a view change or new pass | MEDIUM | Plan 03-01, Tasks 1 and 3: regeneration rule in the contract; harness asserts regenerated sentences after passes 2 and 3 without clicking. Implemented in Plan 03-02, extended to views in Plan 03-04 |
| 03-05: tempoChangedBetween counts the first click at or after the pass end | HIGH | Plan 03-01, Task 1: verified against Metronome.advance/setBpm labelling; tempo membership uses clicks strictly before the end; worked after-end example added. Implemented in Plan 03-03, Task 3 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none for this plan) | Every finding touching the contract or the harness is addressed above; findings about other plans are ledgered in those plans |

### Round 2 — 58daf59

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-01: extra-note count next to ambiguous alignment is a lower bound but printed as exact "N of M assessed passes" (L33@58daf59) | MEDIUM | Plan 03-01, Task 1: verified on the messy fixture (two Es for one E slot, so one E may be an extra). Gap assessment now splits reach from confidence: a gap with certain extras counts as a pass with extras, labelled "at least" and counted in `GapCounts.lowerBoundPasses`; a gap with no certain extra next to an ambiguous note is unassessed in that pass. `ambiguousPlayed[].gap` added. Must_haves truth 5 and the sentence templates are updated. Supersedes the Round 1 flagged-assumption wording. Implemented and tested in Plan 03-02; aggregated through the messy pass in Plan 03-03 |
| 03-01 / 03-02: harness prints success lines only at the end, so the 03-02 tracer cannot see its first OK line when a later scenario fails (L34@58daf59) | MEDIUM | Plan 03-01, Task 3: verified. The scenario is split into groups; each group prints its OK line when its assertions pass; the first failing group prints one FAIL line and exits 1. Acceptance counts the per-group prints |
| 03-02: reopening the same file during capture permanently suppresses painting (L57@58daf59) | HIGH | Plan 03-01, Task 3: harness group 6b reopens rung 1 while the session is live and asserts the same session id, restored fills and the default detail text. The fix is in Plan 03-02, Task 2 |
| 03-02: final-prefix deep equality fails because tail slot times fill in later (L55@58daf59) | HIGH | Plan 03-01, Task 1: verified with start 3600 / end 3800 (D's expectedTime is null on the prefix ending at 6000 and 6500 later). The contract states the guarantee over `stableFields` (expectedTime removed from unreached slots) and explains why every other field is already fixed on a covering prefix. Implemented and property-tested in Plan 03-02, Task 2 |
| 03-03: one stray opening note sets the lag bound to 0; twelve late notes then cost 43.8 vs 39 in cascading substitutions (L78@58daf59) | HIGH | Plan 03-01, Task 1: verified (39 = twelve substitutions + one insertion). The lag bound now comes from the first `lateEntryProbeTokens` (3) tokens. Worked examples added: an opening extra (5 vs 39) and a missing opening note (6 vs 41), with no raw event filtered. Recomputed numbers for the in-time cases (skipped first note bound 3, 18 vs 4; shifted start bound 2, 21 vs 15). Fixtures and code in Plan 03-03, Task 2 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-01: lagged reach changes locked decision D-11 (fixed-origin reach) (L32@58daf59) | Deferred — awaiting user decision (conflicts with D-11). Supersedes Round 1's discretion call that reach follows the lagged times. Until the user chooses, the plans execute D-11 as written: `reachClock 'origin'` is the default in the contract, the tunables table and the engine. The lagged alternative `'reading'` sits behind that one switch with its own fixture (`rung1-late-abandoned-reading.json`), and `rung1-late-abandoned-origin.json` pins the default. The Round 1 cascade argument was re-checked and is false once lag readings exist (6 vs 13, only F changes from not-reached to missed). The rung-1 piano gate (Plan 03-05, Task 2, step 9) presents the concrete late-abandoned pass and records the choice; Plan 03-06, Task 1 applies it to the switch before rung 2 is built on it. 03-CONTEXT.md is not edited by the planner |

### Round 3 — 2098322

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-01: the 2600 ms correction example is internally inconsistent — cloning the correction and moving E leaves E@2600 before F@2500 in seq order, the tokenizer merges them, and the substitution path costs 7360, not 6680 (L35@2098322) | HIGH | Plan 03-01, Task 1: verified with a reference implementation of the contract's cost model (the seq-order clone with a signed window gives 7360; the chronological sequence gives 6680 with one optimal path; the cheapest E-credited alternative is 14080; lag 1 22360, lag 2 27800). The crossover is now its own chronological sequence, C D F F E G with seq 0-5, and the cost string is 6.68 vs 14.08. Separately, the tokenizer rule states decreasing-timestamp handling: seq order, an absolute window, raw records never moved or re-stamped, with a worked pair (F at 1940 → own token, all played, deviationMs −560, cost 3.808; F at 1960 → joins E, extra at within:2, F missed, cost 7). Must_haves truth "Decreasing timestamps" and Task 1 acceptance greps. The engine tests (both crossovers built chronologically, both decreasing-timestamp cases, input immutability) are in Plan 03-02, Task 1 |
| 03-01: fixture validation permits missing note expectations; the downstream comparison iterates only supplied keys (L36@2098322) | MEDIUM | Plan 03-01, Task 2: verified. The shape test requires exact key coverage (attempt → keys equal the model note ids; non-attempt → `{}`), plus a standalone acceptance command over every fixture. Plan 03-02, Task 1 compares the PassResult's note keys to the expected keys exactly. It applies to every later plan's fixtures because they share the shape test |
| 03-02: Start does not invalidate an outstanding history load (L56@2098322) | HIGH | Plan 03-01, Task 3 group 8b: a delayed `Storage.readClicks` holds `loadLatestSession` in flight while `CaptureApp.start()` runs; asserts the analysis session is the live one and no restored marks reappear. Fix in Plan 03-02, Task 2 |
| 03-02: a failed piece load during capture leaves capture active against a null model (L57@2098322) | HIGH | Plan 03-01, Task 3 group 8c: invalid MusicXML loaded during live capture. Asserts the session ends with endReason `piece-unloaded`, the click count stops growing, no page error, the heading reads `No session`, the ended session is retained, and its marks return when rung 1 is reopened. Fix in Plan 03-02, Task 2 |
| 03-03: restart detection depends on a chosen traceback (L78@2098322) | MEDIUM | Plan 03-01, Task 1: the contract defines restart over every optimal alternative, with a worked tie. Verified with the reference implementation: `rung1-restart` has 21 co-optimal alignments at 23000, all inserting part of the second run. The tie case (perBeat 1, lateEntry 1, second run from 2500) gives lag 0 and lag 2 at 15000, disagreeing, so not restarted and all ambiguous. It also fixes entryLag and gap keys for equally cheap readings with different labels. Fixture and code in Plan 03-03, Task 3 |
| 03-05: "up to about a beat and a half" understates the finishing delay (L119@2098322) | MEDIUM | Plan 03-01 flagged assumptions: the finalization note no longer states a bound; it explains why the wait exceeds a beat and a half. The user-facing wording is in Plan 03-05, Task 1 |
| 03-05: `# fail 0` acceptance cannot match the default `ℹ fail 0` reporter (L120@2098322) | MEDIUM | Plan 03-01, Task 2: verified locally (node v24.11.1 prints `ℹ fail 0` even when piped). Every `<fails_when>` now names the exit status and the `ℹ` summary, and count checks select `--test-reporter=tap` explicitly. The same fix is applied to every plan's verify and acceptance blocks |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-01: lagged reach vs D-11 (carried from Round 2) | Still awaiting the user's decision at Plan 03-05, Task 2, step 9 (not re-raised by the Round 3 review). Nothing changes in this plan; the Round 3 recheck and CONTEXT-reconciliation gates are in Plans 03-06 and 03-07 |
