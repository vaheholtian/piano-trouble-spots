---
phase: 03-what-you-actually-played
plan: 07
type: execute
wave: 6
depends_on: ["03-06"]
files_modified:
  - test/fixtures/align/rung3-left-hand-wrong.json
  - test/align.test.cjs
  - test/align-ladder.test.cjs
  - src/align.js
  - docs/analysis-rules.md
  - fixtures/README.md
autonomous: false
requirements: [ANLZ-01, ANLZ-03, AGGR-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both hands at the same beat form one slot: on rung 3 a left-hand D3 in place of E3 marks E3 wrong while E4 in the right hand stays played, with no staff-specific code (SCORE-03 staff model, D-07)"
    - "test/align-ladder.test.cjs runs rung 3 (fixtures/03-both-hands.musicxml) through the same five cases as rungs 1-2 — perfect, one raised pitch, last onset group omitted with a fixed end, one beat late, stray opening note then one beat late — against the independent oracle (ROADMAP success criterion 1)"
    - "Rung 3 is checked at the piano before any chord work is built (review 03-06 HIGH round 2): plan 03-08 depends on this plan's approval"
    - "The user played rung 3 at the FP-60X: a clean both-hands pass showed no marks on the ten notes; the right hand correct with D3 instead of E3 in the left showed only the bass E3 red with 'E3, bar 1 beat 3: wrong in 1 of 2 assessed passes (played D3 once)'; one clean rung-2 regression pass showed no marks (ROADMAP success criterion 4 on rung 3)"
    - "All 30 fixtures, the ladder test and the seven automated gates are green, run fresh before the checkpoint; any false mark on rung 3 routes to /gsd-plan-phase 3 --gaps before rung 4 (VRFY-01)"
    - "The two-hand timing tolerance is explicit and tested around its boundary (review 03-07 MEDIUM round 3): a clean rung-3 pass with the second hand 0, 25, 49 or 50 ms after the first, in both arrival orders (left hand first and right hand first), reads all ten noteheads played with no extras; at 51 and 120 ms, in both orders, every note of the later hand reads missed with that note as an extra at before:k+1 (totalCost 35000), because it forms its own token; docs/analysis-rules.md records 'supported two-hand tolerance: 50 ms between the hands, either order' next to chordWindowMs"
    - "Rung-3 work starts only once the canonical decision record agrees with the recorded reach decision (review 03-06 LOW round 3): when 03-05-SUMMARY.md says 'Reach decision: reading', 03-CONTEXT.md must contain the literal 'reach: reading' (the orchestrator's amendment) before Task 1 runs; with 'origin' no amendment is needed"
    - "A two-hand separation wider than the tolerance at the piano is diagnosed from stored timestamps, and any fix is chosen by comparing a wider window with score-aware grouping, both run against the repeated-note, near-miss re-strike, messy fumble and chord re-strike fixtures (review 03-07 MEDIUM round 3); no window is widened silently"
  artifacts:
    - path: "test/fixtures/align/rung3-left-hand-wrong.json"
      provides: "Both hands as one slot, left-hand wrong note"
      contains: "m1-s2-v5-b2_1-p52"
    - path: "test/align-ladder.test.cjs"
      provides: "Ladder regression extended with rung 3"
      contains: "03-both-hands.musicxml"
    - path: "fixtures/README.md"
      provides: "Phase 3 rung-3 at-the-piano checklist"
      contains: "Phase 3 - rung 3 at the piano"
  key_links:
    - from: "test/fixtures/align/rung3-left-hand-wrong.json"
      to: "docs/analysis-rules.md"
      via: "rule D-07; the contract's D-07 section names this fixture (plan 03-07)"
      pattern: "\"rule\": \"D-07\""
  prohibitions:
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT derive the fixture's expected block from the algorithm's output; expectations are hand-derived from the contract before any code change (D-21)"
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT start rung-4 chord work (chord fixtures, in-chord ambiguity, rung-4 ladder cases) in this plan (ROADMAP rung order, review 03-06 HIGH round 2)"
    - requirement_id: ANLZ-01
      category: values
      statement: "MUST NOT count rung 3 done on automated output alone; the checkpoint is blocking-human (VRFY-01)"
---

## Phase Goal

**As a** pianist drilling right-hand C D E F G alone at the FP-60X, **I want to** see wrong, missed and extra notes painted on the notation after each pass, with honest counts across the passes at one tempo, **so that** I can tell a recurring mistake from a one-off slip and trust what the score shows me.

<objective>
Rung 3, and only rung 3, after the user approved rung 2 (03-06). The steps:

1. Pin "both hands at one beat are one slot" with a hand-derived fixture.
2. Add rung 3 to the OSMD-parsed ladder regression.
3. Write the rung-3 checklist.
4. Stop for the user at the FP-60X.

Chords wait for 03-08.

Round 2 (58daf59): this plan exists because rungs 3-5 were built before the next piano check (review 03-06 HIGH). It carries the rung-3 half of the former 03-06 Task 2 and the rung-3 step of the former 03-07 checklist.

Purpose: ROADMAP success criterion 4 on rung 3 and criterion 1 on rung 3 through the real parser.

Round 3 (2098322) adds three things. Before any rung-3 work, a precondition checks that the canonical decision record matches the recorded reach decision. Two-hand offset tests cover both arrival orders around the 50 ms window, with the supported tolerance written into the contract. The checkpoint gets a timestamp diagnosis for a separation beyond it.

Output: one fixture (30 in total), rung-3 ladder cases, two-hand offset tests, the recorded tolerance, the rung-3 checklist, gates green, and the user's response in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-what-you-actually-played/03-CONTEXT.md
@.planning/phases/03-what-you-actually-played/03-06-SUMMARY.md
@docs/analysis-rules.md
@src/align.js
@test/align.test.cjs
@test/align-ladder.test.cjs
@test/score-model.test.cjs
@fixtures/README.md

<interfaces>
- Align.buildSlots(model) -> [{ index, onset, noteIds, pitches, notes }] — a slot holds every score note at one absolute onset across both staves (03-02)
- Rung 3 ids (test/score-model.test.cjs EXPECTED_RUNG_3): m1-s1-v1-b{k}_1-p{60,62,64,65,67} and m1-s2-v5-b{k}_1-p{48,50,52,53,55} for k = 0..4 (5/4, one measure of length 5/1, quarter notes)
- test/align-ladder.test.cjs from 03-06: RUNGS array, clicksFor, oracleTime, onsetGroups, perfectPerformance, withStrayAtOrigin, passFor, cases (a)-(e)
- Fixture shape: docs/analysis-rules.md "Shapes"; fifteen 5/4 clicks at 120 BPM from 1000 as in test/fixtures/align/rung1-clean.json
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a left-hand wrong note on rung 3 marks only the bass notehead" — hand-derived fixture and OSMD-parsed rung 3 through alignPass</name>
  <precondition>03-06-SUMMARY.md records "approved" for the rung-2 checkpoint (ROADMAP: fix false marks at the current rung before expanding), including a `Reach recheck:` line that is not `pending`; and when 03-05-SUMMARY.md records `Reach decision: reading`, .planning/phases/03-what-you-actually-played/03-CONTEXT.md contains the literal `reach: reading` (the orchestrator's D-11 amendment, review 03-06 LOW round 3) — otherwise halt and report that the canonical decision record is not reconciled</precondition>
  <files>test/fixtures/align/rung3-left-hand-wrong.json, test/align.test.cjs, test/align-ladder.test.cjs, src/align.js, docs/analysis-rules.md</files>
  <read_first>
    - docs/analysis-rules.md section D-07 and "Slots, tokens, readings and the cost model" (a slot spans both staves; chordWindowMs)
    - test/score-model.test.cjs EXPECTED_RUNG_3 rows (copy the model exactly: measure, ids, staffs, voices, onsets, durations)
    - src/align.js buildSlots and tokenizePlayed
    - test/align-ladder.test.cjs as left by 03-06 (the RUNGS array and cases)
  </read_first>
  <behavior>
    - rung3-left-hand-wrong.json: rung 3 model (10 notes), fifteen 5/4 clicks at 120 BPM from 1000, both hands on the clicks 5 ms apart (48@1000 60@1005 50@1500 62@1505 50@2000 64@2005 53@2500 65@2505 55@3000 67@3005), pass 900..3400 -> every note played except m1-s2-v5-b2_1-p52 { status: 'wrong', playedPitch: 50 }; extras []
    - Align.buildSlots on the rung-3 model gives five slots of two noteIds each (one treble, one bass)
    - Right hand wrong alone: the same fixture with 64 replaced by 66 and 50 at 2000 restored to 52 gives only m1-s1-v1-b2_1-p64 wrong playedPitch 66
    - Two-hand offsets around the window (review 03-07 MEDIUM round 3; built from the rung-3 model and the fifteen standard clicks, pass 900..3400, seq in time order, the first hand on each click k at 1000 + 500k and the second hand d ms later): for order 'left first' (48@t, 60@t+d; 50, 62; …) and order 'right first' (60@t, 48@t+d; …), and for d in 0, 25, 49, 50 → all ten played, extras [], totalCost 0; for d in 51 and 120 → the first hand's five notes played, the later hand's five notes missed, extras exactly the later hand's five pitches at before:1, before:2, before:3, before:4, before:5 in seq order, totalCost 35000 (reference computation of the contract's cost model: per beat 4000 for the missed note plus 3000 for the extra; pairing the later token instead costs 7347 per beat)
    - Ladder: RUNGS gains fixtures/03-both-hands.musicxml; cases (a)-(e) pass for it
    - Regression: every earlier fixture green; the final-prefix property over stableFields holds for the new fixture
  </behavior>
  <action>
1. Write `test/fixtures/align/rung3-left-hand-wrong.json` by hand from the contract (rule "D-07") and add the behavior tests to test/align.test.cjs; append the rung-3 entry to RUNGS in test/align-ladder.test.cjs. Run.
2. buildSlots from 03-02 should already group both staves by absolute onset; fix `buildSlots` or `tokenizePlayed` in src/align.js only where a case fails, with no staff-specific branch. If chordWindowMs or any weight must change, run the repeated-note and messy fixtures in the same run and record the fixture name and value in the docs/analysis-rules.md tunables table.
3. Record the supported tolerance (review 03-07 MEDIUM round 3). In the docs/analysis-rules.md tunables table, in the chordWindowMs row's "why this value" column, write `supported two-hand tolerance: 50 ms between the hands, either order` and the boundary behaviour (51 ms or more: the later hand reads missed plus an extra), naming the offset tests in test/align.test.cjs. Add the same sentence to the D-07 section. Add the gap-closure rule beside it. If the piano shows separations above 50 ms, the fix is chosen by evaluating two candidates in the same run: a wider chordWindowMs, and score-aware grouping (a note-on joins the open token only while the slot it would pair with still has an unmatched pitch of that note). The candidates are judged against repeated-e-unique, repeated-e-tie, rung1-restart-near-miss (a re-strike 150 ms later), rung1-messy-fumble (note-ons 80 ms apart) and, once they exist, the rung-4 re-strike and rolled-chord cases. It is never a silent window change.
  </action>
  <verify>
    <automated>node --test test/align.test.cjs test/align-fixtures.test.cjs test/align-ladder.test.cjs</automated>
    <fails_when>non-zero exit (node --test exits 1 when any test fails or a file cannot load), or the summary shows a non-zero fail count or a pass count of 0 — the default spec reporter prints these as "ℹ fail N" and "ℹ pass N", never "# fail"</fails_when>
  </verify>
  <acceptance_criteria>
    - Canonical record reconciled (review 03-06 LOW round 3): `node -e "const fs=require('fs');const d='.planning/phases/03-what-you-actually-played/';const s=fs.readFileSync(d+'03-05-SUMMARY.md','utf8');const m=s.match(/Reach decision: (origin|reading)/);if(!m)process.exit(1);if(m[1]==='origin')process.exit(0);process.exit(fs.readFileSync(d+'03-CONTEXT.md','utf8').includes('reach: reading')?0:1)"` exits 0
    - Two-hand tolerance (review 03-07 MEDIUM round 3): `grep -c 'supported two-hand tolerance: 50 ms between the hands, either order' docs/analysis-rules.md` prints at least 1; `grep -c 'right first' test/align.test.cjs` prints at least 1 and `grep -c '51' test/align.test.cjs` prints at least 1
    - `node -e "const f=require('./test/fixtures/align/rung3-left-hand-wrong.json');const e=f.expected;process.exit(e.notes['m1-s2-v5-b2_1-p52'].status==='wrong'&&e.notes['m1-s2-v5-b2_1-p52'].playedPitch===50&&e.notes['m1-s1-v1-b2_1-p64'].status==='played'&&f.scoreModel.notes.length===10?0:1)"` exits 0
    - `ls test/fixtures/align/*.json | wc -l` prints 30
    - `grep -c '03-both-hands.musicxml' test/align-ladder.test.cjs` prints at least 1; `grep -c '04-chords' test/align-ladder.test.cjs` prints 0
    - `grep -vE '^\s*(/\*|\*|//)' src/align.js | grep -cE 'staff *[=!]=='` prints 0 (no staff-specific slot logic)
    - `node --test --test-reporter=tap test/align-ladder.test.cjs` exits 0 and its summary shows `# fail 0` and `# pass N` with N at least 15 (TAP selected explicitly, review 03-05 MEDIUM round 3)
  </acceptance_criteria>
  <done>Both hands at one beat are one slot on rung 3, a left-hand wrong note marks only the bass notehead, and rung 3 parsed by the real parser passes the five ladder cases.</done>
</task>

<task type="auto">
  <name>Task 2: Rung-3 gate — every automated check fresh and the rung-3 piano checklist</name>
  <precondition>C:\Program Files\Google\Chrome\Application\chrome.exe exists (or CHROME_EXE is set) and the machine has internet access for the pinned CDN bundles</precondition>
  <files>fixtures/README.md</files>
  <read_first>
    - fixtures/README.md sections "Phase 3 - rung 1 at the piano" and "Phase 3 - rung 2 at the piano" (append after them, do not rewrite)
    - .planning/phases/03-what-you-actually-played/03-06-SUMMARY.md (anything the user reported on rung 2)
  </read_first>
  <action>
1. Append `## Phase 3 - rung 3 at the piano` to `fixtures/README.md`: a two-line reminder (legend, finishing delay, ready rule), Scenario E exactly as written in Task 3 with views and sentences, a sentence stating the supported tolerance in the user's terms ("the two hands count as together when they land within 50 ms of each other, in either order; more than that and the later hand's note shows as missed with a + beside it"), the timestamp diagnosis for step 1 or 2 (press Stop, open DevTools (F12) → Console, paste `(() => { const p = CaptureApp.analysis.session.passes.filter((x) => x.noteCount > 0).pop(); const t0 = p.notes[0].timeStamp; return 'Pass ' + p.ordinal + ': ' + p.notes.map((n) => n.note + '@+' + Math.round(n.timeStamp - t0)).join(' '); })()` and copy the printed line into the reply, then press Start to continue), and the rule that a false mark stops the ladder here.
2. Run the gates fresh, in this order, and fix anything red before committing:
   1. `npm test`
   2. `node scripts/check-run-path.cjs --fixtures`
   3. `node scripts/check-svg-map.cjs`
   4. `node scripts/check-capture-roundtrip.cjs`
   5. `node scripts/check-paint.cjs`
   6. `node scripts/check-align-perf.cjs`
   7. `node scripts/check-live-perf.cjs`

   Commit fixtures/README.md.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit (npm test propagates node --test's exit 1 on any failing test), or a summary line "ℹ fail N" with N greater than 0</fails_when>
    <automated>node scripts/check-run-path.cjs --fixtures</automated>
    <fails_when>non-zero exit, any line starting "FAIL:", or the summary line not ending in "0 failed"</fails_when>
    <automated>node scripts/check-svg-map.cjs</automated>
    <fails_when>non-zero exit; any "FAIL fixtures/..." line; fewer than six "OK fixtures/" lines</fails_when>
    <automated>node scripts/check-capture-roundtrip.cjs</automated>
    <fails_when>non-zero exit; output lacks either OK line ("OK capture round-trip: 8 raw events restored unmodified" prefix, "OK capture round-trip: pass split 3/2/1/1 restored after reload mid-session"); any line starting "FAIL capture round-trip"</fails_when>
    <automated>node scripts/check-paint.cjs</automated>
    <fails_when>non-zero exit; any line starting "FAIL paint:"; fewer than thirteen lines starting "OK paint:"</fails_when>
    <automated>node scripts/check-align-perf.cjs</automated>
    <fails_when>non-zero exit, a line starting "FAIL align perf:", or no line starting "OK align perf:"</fails_when>
    <automated>node scripts/check-live-perf.cjs</automated>
    <fails_when>non-zero exit; a line starting "FAIL live perf:"; no line starting "OK live perf:"</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven commands above exit 0 with their OK lines (`npm test` judged by its exit status)
    - `grep -c 'within 50 ms of each other, in either order' fixtures/README.md` prints 1 and `grep -c 'CaptureApp.analysis.session.passes' fixtures/README.md` prints at least 1 (the stated tolerance and the timestamp diagnosis, review 03-07 MEDIUM round 3)
    - `grep -c 'Phase 3 - rung 3 at the piano' fixtures/README.md` prints 1; `grep -c 'E3, bar 1 beat 3: wrong in 1 of 2 assessed passes (played D3 once)' fixtures/README.md` prints 1; `grep -c 'Phase 3 - rung 4 at the piano' fixtures/README.md` prints 0
  </acceptance_criteria>
  <done>All automated gates are green with rungs 1-3 in the ladder regression, and the checklist tells the user exactly what to play on rung 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: At the FP-60X — rung 3 (both hands), clean and left-hand wrong, plus a rung-2 regression pass</name>
  <what-built>Both hands at the same beat are judged as one slot per beat, each notehead on its own. The ladder regression already runs rungs 1-3 through the real parser against an independent oracle; this is the hardware check before any chord work is built.</what-built>
  <how-to-verify>
fixtures/README.md open at "Phase 3 - rung 3 at the piano". Same ready rule: read after `(finishing)` clears; before the next pass press the mark once more and start on the next accented click.

Scenario E:
1. Open fixtures/03-both-hands.musicxml, 80 BPM (Enter), Start. Both hands clean on the clicks, mark. Session view: no coloured noteheads on the ten notes.
2. Right hand correct and D3 instead of E3 in the left hand, mark. Session view: only the bass E3 red, the treble E4 black; click E3 -> `E3, bar 1 beat 3: wrong in 1 of 2 assessed passes (played D3 once)`.
3. Regression: open fixtures/02-left-hand.xml, Start, play C3 D3 E3 F3 G3 cleanly, mark -> the pass view of the pass you just played shows no coloured noteheads.

If step 1 or 2 showed a blue notehead with a `+` beside it (the later hand read as missed plus an extra), press Stop after that pass, run the console line from the checklist, and paste its output: the gap between the two hands at each beat decides whether the hands were more than 50 ms apart (the supported tolerance) or the engine grouped them wrongly.
  </how-to-verify>
  <resume-signal>Type "approved" only if the clean both-hands pass showed no mark, only the bass E3 turned red in step 2 with the quoted sentence, and the rung-2 regression pass showed no mark. Include the step-2 sentence as shown, and the console line if you ran it. Otherwise describe the step, what you played and what was shown (for example "step 2: both E3 and E4 turned red"). A false mark goes to /gsd-plan-phase 3 --gaps and rung 4 is not built until it is fixed.</resume-signal>
  <read_first>
    - fixtures/README.md section "Phase 3 - rung 3 at the piano" (written by Task 2)
    - docs/analysis-rules.md D-04 and D-07 (to judge a reported mismatch against the written rule)
  </read_first>
  <acceptance_criteria>
    - The user's response is recorded verbatim in 03-07-SUMMARY.md and is either "approved" with the step-2 sentence, or a list of mismatches naming the step, what was played and what was shown
    - If a console line was pasted, 03-07-SUMMARY.md records it and the executor's reading: the largest separation between the two hands' note-ons at one beat. At most 50 ms is an engine grouping defect → /gsd-plan-phase 3 --gaps. More than 50 ms is outside the supported tolerance. Widening it is offered to the user as a gap-closure change that must evaluate both a wider chordWindowMs and score-aware grouping against the repeated-note, near-miss, messy-fumble and chord re-strike cases; it is never applied silently (review 03-07 MEDIUM round 3)
    - A mismatch response leads to /gsd-plan-phase 3 --gaps before plan 03-08 starts
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `test/fixtures/align/rung3-left-hand-wrong.json` (30 fixtures in total)
- `test/align.test.cjs` — both-hands slot tests (left-hand wrong, right-hand wrong, five two-note slots) and the two-hand offset tests (0, 25, 49, 50, 51, 120 ms in both arrival orders)
- `docs/analysis-rules.md` — the chordWindowMs row and D-07 section state the supported two-hand tolerance (50 ms, either order) and the gap-closure rule (wider window versus score-aware grouping, judged against the repeated-note, near-miss, messy and chord re-strike cases)
- `test/align-ladder.test.cjs` — rung 3 added to RUNGS (five cases)
- `fixtures/README.md` — "Phase 3 - rung 3 at the piano" (Scenario E, the tolerance in plain words, the console line for two-hand timing)
- `src/align.js` — changed only if a rung-3 case fails (recorded in the tunables table with the fixture name)
- `03-07-SUMMARY.md` — fixture and ladder test counts, fresh gate output, the user's rung-3 response verbatim (with the console line and its reading if one was pasted)

## Flagged assumptions (not silently dropped)

- Both hands on the same beat are expected within chordWindowMs of each other; hands landing more than 50 ms apart become two tokens, only one of which can pair with the slot, so the later hand's note would read as missed plus an extra — a false mark. If the user sees that in step 1 or 2 it is reported, not explained away, and routes to gap closure as a chordWindowMs change tested with the repeated-note and messy fixtures in the same run (RESEARCH Pitfall 4).
- Supported two-hand tolerance (review 03-07 MEDIUM round 3; refines the bullet above without replacing it): 50 ms in either arrival order, pinned by offset tests at 0, 25, 49, 50, 51 and 120 ms. 50 ms is the existing chordWindowMs; nothing yet shows the user's hands are further apart. A piano report beyond it is judged from stored timestamps. The fix is chosen between a wider window and score-aware grouping, because a wider global window would merge the messy fumble's 80 ms note-ons and could swallow the 150 ms near-miss re-strike.
- Rung 3 introduces no rule beyond the slot spanning both staves, so its piano check is clean + one-hand wrong + a rung-2 regression pass.

<verification>
- `node --test test/align.test.cjs test/align-fixtures.test.cjs test/align-ladder.test.cjs` exits 0; the seven gate commands green; the canonical-record check passes
- The blocking-human checkpoint answered "approved" with the sentence quoted, or its issues recorded as gaps for `/gsd-plan-phase 3 --gaps`
</verification>

<success_criteria>
- ROADMAP success criterion 4 holds on rung 3; criterion 1 holds on rungs 1-3 through the real parser
- Rung 4 is not started before rung 3 is approved
</success_criteria>

<output>
Create `.planning/phases/03-what-you-actually-played/03-07-SUMMARY.md` when done, recording the fixture count (30), the ladder test count, the two-hand offset test results, whether any tunable changed, the fresh gate output and the checkpoint response verbatim.
</output>

## Review Dispositions Ledger

### Round 1 — af0835b

<!-- Carried verbatim from the former 03-07 (rungs 2-5 at the piano). That checklist is now split across 03-06 (rung 2), 03-07 (rung 3), 03-08 (rung 4) and 03-09 (rung 5); see Round 2 below. -->

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-06: rung-4 wrong E then missed E ties in the aggregate and stays red, not blue; the checklist must name the view | MEDIUM | Plan 03-07, Task 2 step 3(c): verified against the D-18 tie rule; blue is read in the pass 3 view and the session view is expected to stay red with both counts in the sentence; every observation names its view |
| 03-06: first piano gate after harder rungs | HIGH | Plan 03-07 (rungs 2-5) depends on Plan 03-06, which depends on the rung-1 approval in Plan 03-05 |
| 03-04: glyph placement across a system break untested on a real render | MEDIUM | Plan 03-07, Task 2 step 4(d): rung 5 wrapped onto two lines with an extra note at the first line's end |
| 03-03: re-strike inside a chord | HIGH | Plan 03-07, Task 2 step 3(e): hardware check of the duplicate rule |
| 03-01: late entry on longer passages | HIGH | Plan 03-07, Task 2 step 4(c): rung 5 played one beat late |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none for this plan) | — |

### Round 2 — 58daf59

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-06: rung-by-rung gating only partly fixed; interleave per-rung implementation and checkpoints (L138@58daf59) | HIGH | This plan is the rung-3 build-then-check step: both-hands fixture and rung-3 ladder cases, then the rung-3 piano checkpoint. It depends on the rung-2 approval (03-06), and 03-08 depends on this approval. Supersedes this file's Round 1 rows: the rung-4 view naming and re-strike check move to Plan 03-08; the system-break glyph and the rung-5 late entry move to Plan 03-09, where they are re-ledgered |
| 03-07: hardware checklists omit repeated-pitch ambiguity, the equidistant chord pairing and a within-pass tempo change, yet claim full criterion-6 acceptance (L158@58daf59) | MEDIUM | Split by rung: the within-pass tempo change and the doubled-E repeated pitch are checked on rung 1 (Plan 03-05, Task 2 steps 10-11); the equidistant chord on rung 4 (Plan 03-08); rung-5 repeated bass G3 by timing (Plan 03-09). This plan claims only criteria 1 and 4 on rung 3 |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none for this plan in Round 2) | — |

### Round 3 — 2098322

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-07: two-hand timing tolerance only tested at 5 ms; wider separation gives false missed-plus-extra marks; widening one global window may create other grouping errors (L161@2098322) | MEDIUM | Plan 03-07, Task 1: offset tests at 0, 25, 49, 50, 51 and 120 ms in both arrival orders. At ≤ 50 ms, all played and cost 0; at ≥ 51 ms, the later hand is missed plus an extra at before:k+1, cost 35000, versus 36735 for pairing the later token (verified with a reference implementation of the contract's cost model, including lag readings). The supported tolerance, "50 ms between the hands, either order", is written into the chordWindowMs row and D-07. A separation reported at the piano is judged from stored timestamps (console line in Tasks 2-3). A fix must evaluate a wider window and score-aware grouping together against repeated-e-unique/tie, rung1-restart-near-miss, rung1-messy-fumble and the rung-4 re-strike/roll cases; no silent widening |
| 03-06: CONTEXT decision amendment has no completion gate (L141@2098322) | LOW | Plan 03-07, Task 1: the precondition and a `node -e` acceptance command require 03-CONTEXT.md to contain `reach: reading` whenever 03-05-SUMMARY.md records that decision, and a non-pending `Reach recheck:` line from 03-06, before any rung-3 work. The amendment itself stays the orchestrator's |
| 03-05: `# fail 0` cannot match the default reporter (L120@2098322) | MEDIUM | Plan 03-07, Tasks 1-2: `<fails_when>` names the exit status and `ℹ` summary; the ladder count uses `--test-reporter=tap`; `npm test` is judged by exit status |
| 03-03: live callback budget missing from the gates (L77@2098322) | MEDIUM | Plan 03-07, Task 2: `node scripts/check-live-perf.cjs` is the seventh gate command |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-07: implement score-aware grouping now (L166@2098322) | Deferred to evidence: the suggestion is conditional ("if the piano requires more"). The tolerance is recorded and pinned at its boundary. Score-aware grouping is evaluated alongside a window change only if the rung-3 checkpoint's stored timestamps show the user's hands land more than 50 ms apart. Building it now would change tokenization for every rung with no observed need (the user's stated priority is accuracy, and the prototype showed the cost of speculative machinery) |
