---
phase: 03-what-you-actually-played
plan: 05
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/align.js
  - test/align.test.cjs
  - test/align-ladder.test.cjs
  - test/fixtures/align/rung1-restart.json
  - test/fixtures/align/rung1-restart-near-miss.json
  - test/fixtures/align/rung1-two-runs.json
  - test/fixtures/align/rung1-tempo-changed.json
  - test/fixtures/align/rung1-no-origin.json
  - docs/analysis-rules.md
autonomous: true
requirements: [ANLZ-01, ANLZ-03, AGGR-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A restart without a mark (C D E, pause, C D E F G) makes the whole pass unassessed with reason restarted; the pass stays stored and listed and counts as one unassessed pass for every note; a re-struck C is an extra, not a restart; two complete runs in one pass are a restart; a score whose own opening repeats is not (D-12, D-09)"
    - "A pass whose BPM changed between its origin click and its end is unassessed with reason tempo-changed for every note, excluded from every tempo group, still listed and viewable alone; a BPM change between passes simply starts a new tempo group (D-16, D-15)"
    - "A pass with no accented click at or after its start has originIndex null, every note not-reached and every played note an extra before the first slot; a slot beyond the recorded clicks is not-reached; an empty click timeline behaves the same with bpm null (ANLZ-01 empty probe, explicit)"
    - "Every ladder rung (1 to 5) parsed through the real OSMD parser aligns a synthesized perfect performance with every note played and no extras, one raised pitch marks exactly that notehead wrong, and rung 5's eighth-note slots take their expected time by interpolation between two recorded clicks (ROADMAP success criterion 1, RESEARCH Pattern 2)"
    - "Unassessed reasons are exactly the fixed vocabulary and each unassessed opportunity carries exactly one; precedence is tempo-changed, then restarted, then not-reached / ambiguous per slot (D-15)"
    - "All seventeen earlier fixtures stay green"
  artifacts:
    - path: "src/align.js"
      provides: "detectRestart, tempoChangedBetween, no-origin and beyond-timeline handling inside alignPass"
      contains: "restarted"
    - path: "test/align-ladder.test.cjs"
      provides: "OSMD-parsed rungs 1-5 with synthesized clicks and performances"
      contains: "05-yanni-4-measures"
    - path: "test/fixtures/align/rung1-restart.json"
      provides: "The D-12 true restart"
      contains: "\"restarted\""
    - path: "test/fixtures/align/rung1-tempo-changed.json"
      provides: "The D-16 mid-pass tempo change"
      contains: "\"tempo-changed\""
  key_links:
    - from: "test/align-ladder.test.cjs"
      to: "test/osmd-node-env.cjs"
      via: "installOsmdNodeEnv / loadSheet / readFixture, the Phase 1 pattern for parsing the ladder under node:test"
      pattern: "installOsmdNodeEnv"
    - from: "src/align.js"
      to: "src/align.js"
      via: "detectRestart runs on the traceback labels (insertions) after the DP; tempoChangedBetween runs before it"
      pattern: "detectRestart"
  prohibitions:
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT split a restarted pass into two passes or drop its first fragment; automatic splitting is out of scope for the milestone and the whole pass is reported restarted (D-12, Deferred Ideas)"
    - requirement_id: AGGR-01
      category: transparency
      statement: "MUST NOT fold a tempo-changed pass into any tempo group or recompute its expected times from a single BPM; expected times always come from the recorded clicks (D-01, D-16)"
    - requirement_id: ANLZ-01
      category: values
      statement: "MUST NOT treat a played note after the last recorded click as anything but an extra, and a slot beyond the recorded clicks as anything but not-reached; the click timeline is the only expected timeline (ROADMAP pass-origin paragraph)"
---

## Phase Goal

**As a** pianist drilling right-hand C D E F G alone at the FP-60X, **I want to** see wrong, missed and extra notes painted on the notation after each pass, with honest counts across the passes at one tempo, **so that** I can tell a recurring mistake from a one-off slip and trust what the score shows me.

<objective>
Close the last interpretation rules the roadmap requires fixtures for — restart without a mark, a tempo change inside a pass, and the no-origin edge — and then prove the whole engine on all five ladder rungs parsed through the real OSMD parser, including rung 5's eighth notes, before the piano gate.

Purpose: ROADMAP success criterion 6 ("an interrupted pass, ambiguous alignment, and tempo changes") and criterion 1 on every rung, so the at-the-piano session for rungs 2-5 starts from green fixtures.

Output: five fixtures, restart / tempo-change / no-origin handling in src/align.js, test/align-ladder.test.cjs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-what-you-actually-played/03-CONTEXT.md
@.planning/phases/03-what-you-actually-played/03-RESEARCH.md
@.planning/phases/03-what-you-actually-played/03-03-SUMMARY.md
@docs/analysis-rules.md
@src/align.js
@test/align.test.cjs
@test/score-model.test.cjs
@test/osmd-node-env.cjs
@fixtures/README.md

<interfaces>
<!-- From 03-02/03-03 (src/align.js) and Phase 1 (test/osmd-node-env.cjs). -->
- Align.alignPass(model, pass, clicks, tunables?) -> PassResult (wholeReason null | 'restarted' | 'tempo-changed'); Align.buildSlots(model); Align.tokenizePlayed(notes, chordWindowMs); Align.resolveOrigin(clicks, start); Align.expectedTime(clicks, originIndex, onsetRational); Align.DEFAULT_TUNABLES.restartPrefixSlots === 3
- test/osmd-node-env.cjs: installOsmdNodeEnv() -> env with restore(); loadSheet(xmlText) -> OSMD Sheet; readFixture('fixtures/01-right-hand.musicxml') -> text. ScoreModel.extract(sheet) -> model. (Pattern: test/score-model.test.cjs lines 1-12.)
- Ladder facts (fixtures/README.md): rungs 1-3 are 5/4 one bar; rung 4 is 4/4 one bar; rung 5 is 3/4 four bars with eighth notes (bar 2 right hand: quarter, dotted quarter, eighth -> onset 5/2 within the bar), 41 notes.
- Click record: { pageTime, bar, beat, bpm, accent }; Metronome.advance increments beat per click and wraps at timeSignatureFor(bar).beats.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restart without a mark (D-12), tempo change inside a pass (D-16), and the no-origin edge — fixtures first</name>
  <files>test/fixtures/align/rung1-restart.json, test/fixtures/align/rung1-restart-near-miss.json, test/fixtures/align/rung1-two-runs.json, test/fixtures/align/rung1-tempo-changed.json, test/fixtures/align/rung1-no-origin.json, test/align.test.cjs, src/align.js, docs/analysis-rules.md</files>
  <read_first>
    - docs/analysis-rules.md sections D-12, D-15, D-16, the "No origin" worked example, and the restart / tempo rules in "Slots, tokens and the cost model"
    - src/align.js alignPass as left by 03-03 (where the DP runs, where labels are produced, the origin branch)
    - test/fixtures/align/rung1-clean.json (shape to copy; the tempo fixture changes only the clicks)
    - .planning/phases/03-what-you-actually-played/03-RESEARCH.md "Open Questions" 2 (restart heuristic discussion) and "Pattern 1" (why BPM arithmetic is never used)
  </read_first>
  <behavior>
    - rung1-restart.json (D-12): rung 1, 60@1000 62@1500 64@2000 then 60@4000 62@4500 64@5000 65@5500 67@6000, end 6400 -> attempt true, originIndex 0, bpm 120, wholeReason 'restarted', all five notes { status: 'unassessed', reason: 'restarted' }, extras []
    - rung1-restart-near-miss.json (D-09, D-12): 60@1000 60@1150 62@1500 64@2000 65@2500 67@3000 -> wholeReason null, all five played, extras [{ gap: 'before:1', pitch: 60 }]
    - rung1-two-runs.json (D-12): 60 62 64 65 67 on the bar-1 clicks then 60 62 64 65 67 on the bar-2 clicks (3500..5500), end 5900 -> wholeReason 'restarted', all unassessed restarted, extras []
    - rung1-tempo-changed.json (D-16): clicks i = 0..2 at bpm 120 (1000, 1500, 2000), then bpm 100 from i = 3 (2500, 3100, 3700, 4300, 4900, 5500, 6100, 6700, 7300) with bar/beat continuing in 5/4; pass 900..3400 with 60@1000 62@1500 64@2000 65@2500 67@3100 -> wholeReason 'tempo-changed', bpm 120, all five { status: 'unassessed', reason: 'tempo-changed' }, extras []
    - Between-pass change (unit test on the same clicks): pass 3300..5800 with notes on clicks 5..9 -> originIndex 5, bpm 100, wholeReason null, all played
    - rung1-no-origin.json: the twelve standard clicks, pass startTimeStamp 7000, endTimeStamp 8000, notes 60@7100 62@7300 -> originIndex null, bpm 120 (last click before the start), all five not-reached, extras [{ gap: 'before:0', pitch: 60 }, { gap: 'before:0', pitch: 62 }]
    - Empty timeline (unit test): alignPass with clicks [] and rung1-clean's pass -> originIndex null, bpm null, all not-reached, five extras at before:0
    - Beyond the timeline (unit test): rung1-clean's pass with only the first four clicks -> G4 not-reached, C D E F played, and the played G an extra at before:4
    - Repeated opening is not a restart (unit test): synthetic score C4 D4 E4 C4 D4 E4 (one 6/4 measure, ids from deriveNoteId), played exactly on six clicks -> all six played, wholeReason null
    - Precedence: a pass with both a mid-pass tempo change and a restart shape reports tempo-changed
    - Regression: every fixture in test/fixtures/align/ still matches
  </behavior>
  <action>
1. Write the five fixtures by hand from the contract and add the behavior tests to test/align.test.cjs. Run: the restart, two-runs and tempo-changed cases must fail against the 03-03 engine; the near-miss, no-origin and beyond-timeline cases may already pass.

2. In `src/align.js`:
   - `tempoChangedBetween(clicks, originIndex, endTime)`: the clicks from originIndex through the first index whose pageTime ≥ endTime (or the last click when none); true when any bpm differs from clicks[originIndex].bpm. In alignPass, right after the origin is resolved and before the DP: when true, return the PassResult with wholeReason 'tempo-changed', every note `{ status: 'unassessed', reason: 'tempo-changed' }`, slots with expectedTime/reached still filled, extras [], ambiguousPlayed [].
   - `detectRestart(slots, tokens, insertedTokenIndexes, restartPrefixSlots)`: prefix = the pitch sets of the first min(restartPrefixSlots, slots.length) slots; a run at token index r means tokens r, r+1, … have pitch sets equal (as sorted arrays) to the prefix in order; find the first run r1; find the first run r2 with r2 > r1 + prefix.length − 1; return true only when r2 exists and at least one token index in [r2, r2 + prefix.length) is in insertedTokenIndexes. In alignPass, after the traceback (and after ambiguity marking): when true, replace every note label with `{ status: 'unassessed', reason: 'restarted' }`, set wholeReason 'restarted', extras [] and ambiguousPlayed [].
   - No-origin branch (already present from the tracer): confirm bpm = the last click before the pass start (or null with no clicks), every note not-reached, every token's notes extras at before:0 in seq order. Beyond-timeline: a slot whose expectedTime is null is not reached (already the tracer's rule) — confirm the played notes after the last reached slot become extras at the correct before:k.
   - Keep the reason precedence: tempo-changed (whole pass, before DP) > restarted (whole pass, after DP) > per-slot not-reached / ambiguous.

3. If restartPrefixSlots or any weight changes, record it in the docs/analysis-rules.md tunables table with the fixture name; otherwise the doc is untouched.
  </action>
  <verify>
    <automated>node --test test/align.test.cjs test/align-fixtures.test.cjs</automated>
    <fails_when>non-zero exit, or the TAP summary lacks "# fail 0", or "# pass 0" appears</fails_when>
    <automated>node scripts/check-run-path.cjs</automated>
    <fails_when>non-zero exit, any line starting "FAIL:", or the summary line not ending in "0 failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `ls test/fixtures/align/*.json | wc -l` prints 22
    - `node -e "const f=require('./test/fixtures/align/rung1-restart.json');process.exit(f.expected.wholeReason==='restarted'&&Object.values(f.expected.notes).every(n=>n.reason==='restarted')&&f.expected.extras.length===0?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-tempo-changed.json');process.exit(f.expected.wholeReason==='tempo-changed'&&f.clicks[2].bpm===120&&f.clicks[3].bpm===100&&f.clicks[4].pageTime===3100?0:1)"` exits 0
    - `node -e "const f=require('./test/fixtures/align/rung1-no-origin.json');process.exit(f.expected.originIndex===null&&f.expected.extras.every(x=>x.gap==='before:0')&&Object.values(f.expected.notes).every(n=>n.reason==='not-reached')?0:1)"` exits 0
    - `grep -c 'function detectRestart' src/align.js` prints 1; `grep -c 'function tempoChangedBetween' src/align.js` prints 1
    - `grep -vE '^\s*(/\*|\*|//)' src/align.js | grep -cE 'document\.[a-zA-Z]|navigator\.[a-zA-Z]|new AudioContext|\bwindow\.[a-zA-Z]|requestMIDIAccess'` prints 0
    - `node --test test/align.test.cjs` prints `# fail 0`
  </acceptance_criteria>
  <done>Restart, tempo change and no-origin passes are honestly unassessed with their one reason each, a re-strike and a repeated opening are not mistaken for restarts, and all twenty-two fixtures are green.</done>
</task>

<task type="auto">
  <name>Task 2: Ladder regression — every rung through the real OSMD parser with synthesized clicks and performances (test/align-ladder.test.cjs)</name>
  <files>test/align-ladder.test.cjs</files>
  <read_first>
    - test/score-model.test.cjs lines 1-12 and the rung 5 test (how the env is installed, sheets loaded, models extracted, and the env restored in after())
    - test/osmd-node-env.cjs (installOsmdNodeEnv, loadSheet, readFixture signatures)
    - fixtures/README.md (rung contents; rung 5 bar 2 has an eighth note at onset 5/2 within the bar)
    - src/align.js buildSlots / expectedTime / alignPass (as left by Task 1)
  </read_first>
  <action>
Create `test/align-ladder.test.cjs` (node:test; jsdom is used only to let OSMD parse, exactly as test/score-model.test.cjs does; the alignment calls themselves receive plain JSON). Helpers: `clicksFor(model, bpm, pageTime0)` builds a synthetic timeline by walking model.measures in order — for each measure emit timeSignature.beats clicks (assert beatType === 4 for every rung, else fail loudly) with bar = measure.number, beat 1..beats, accent = beat === 1, bpm, pageTime advancing by 60000 / bpm — then one extra bar so every slot has a following click; `perfectPerformance(model, clicks)` uses Align.buildSlots(model) and Align.expectedTime(clicks, 0, slot.onset) to emit one note-on per note at expectedTime + 5 × (index within the slot) with velocity 80 and seq in time order; `passFor(notes, clicks)` gives { ordinal: 1, startTimeStamp: clicks[0].pageTime − 100, endTimeStamp: last note + 400, notes, noteCount }. For each of `fixtures/01-right-hand.musicxml`, `fixtures/02-left-hand.xml`, `fixtures/03-both-hands.musicxml`, `fixtures/04-chords.musicxml`, `fixtures/05-yanni-4-measures.musicxml`: (a) the perfect performance at 120 BPM yields attempt true, originIndex 0, wholeReason null, every model note played, extras [] and ambiguousPlayed []; (b) raising the pitch of the first note of slot 1 by one semitone yields exactly that noteId wrong with playedPitch = midi + 1 and every other note played (when the raised pitch collides with another note in the same slot, raise by two); (c) omitting every note of the last slot yields those notes missed and everything else played. For rung 5 additionally: assert some slot's onset has den !== 1 and that its expectedTime lies strictly between two consecutive click pageTimes (interpolation exercised), and that the perfect performance at 60 BPM also yields all played. Print nothing but TAP; restore the env in after().
  </action>
  <verify>
    <automated>node --test test/align-ladder.test.cjs</automated>
    <fails_when>non-zero exit, or the TAP summary lacks "# fail 0", or "# pass 0" appears</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or "# fail 0" absent from the TAP summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '05-yanni-4-measures.musicxml' test/align-ladder.test.cjs` prints at least 1 and `grep -c '04-chords.musicxml' test/align-ladder.test.cjs` prints at least 1
    - `grep -c 'installOsmdNodeEnv' test/align-ladder.test.cjs` prints at least 1 and `grep -c 'Align.alignPass' test/align-ladder.test.cjs` prints at least 1
    - `grep -c 'den !== 1' test/align-ladder.test.cjs` prints at least 1 (rung 5 interpolation is asserted, not assumed)
    - `node --test test/align-ladder.test.cjs` prints `# fail 0` with at least 15 tests (three per rung)
    - `npm test` prints `# fail 0`
  </acceptance_criteria>
  <done>Every rung, parsed by the real parser, aligns cleanly and marks a single injected wrong or missing note exactly, with rung 5's eighth notes proving the interpolated expected time; the piano gate can trust the software on all five files.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `src/align.js` — `detectRestart`, `tempoChangedBetween`, whole-pass reason precedence
- `test/fixtures/align/rung1-restart.json`, `rung1-restart-near-miss.json`, `rung1-two-runs.json`, `rung1-tempo-changed.json`, `rung1-no-origin.json`
- `test/align.test.cjs` — D-12, D-16, no-origin, beyond-timeline, repeated-opening, precedence cases
- `test/align-ladder.test.cjs` — rungs 1-5 via OSMD with synthesized clicks (helpers `clicksFor`, `perfectPerformance`, `passFor`)

## Flagged assumptions

- The restart heuristic (second prefix run whose tokens include an insertion) is Claude's discretion under D-12; the true-restart, near-miss, two-runs and repeated-opening cases pin its behaviour. A real piece whose opening returns after a fragment could still look like a restart only if the DP left that return unmatched — accepted, recorded in the contract's D-12 section by the executor if a sentence is missing.
- The ladder regression synthesizes clicks from the model's time signatures at 120 BPM, which matches Metronome.advance's bar/beat walk; it does not exercise the real scheduler (Phase 2 already did).

<verification>
- `node --test test/align.test.cjs test/align-fixtures.test.cjs test/align-ladder.test.cjs` green; `npm test` green; `node scripts/check-run-path.cjs` green
</verification>

<success_criteria>
- ROADMAP success criterion 6's fixture list is complete (missing first note, late entry, repeated pitches, extra note, interrupted pass, restart, ambiguous alignment, tempo change) with each result explained by a numbered rule
- Criterion 1 holds on all five rungs through the real parser
</success_criteria>

<output>
Create `.planning/phases/03-what-you-actually-played/03-05-SUMMARY.md` when done, recording the fixture count (22), the ladder test count, and any tunable that changed.
</output>
