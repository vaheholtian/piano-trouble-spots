---
phase: 03-what-you-actually-played
plan: 09
type: execute
wave: 8
depends_on: ["03-08"]
files_modified:
  - test/align-ladder.test.cjs
  - src/align.js
  - docs/analysis-rules.md
  - fixtures/README.md
autonomous: false
requirements: [ANLZ-01, ANLZ-03, AGGR-01, AGGR-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Rung 5 (fixtures/05-yanni-4-measures.musicxml, 3/4, four bars, 41 notes) passes the five ladder cases through the real OSMD parser against the independent oracle — perfect, one raised pitch, last onset group omitted with a fixed end, one beat late, stray opening note then one beat late — and a perfect performance at 60 BPM is also all played (ROADMAP success criterion 1)"
    - "Rung 5's eighth-note slots take their expected time by interpolation between two recorded clicks, checked against hand-computed values for named note ids (m1-s2-v5-b5_2-p50 at 2250, m2-s1-v1-b5_2-p74 at 3750, m3-s1-v1-b5_2-p78 at 5250, m4-s1-v1-b0_1-p76 at 5500 on a 120 BPM timeline from 1000), the first three strictly between two consecutive clicks (RESEARCH Pattern 2)"
    - "Repeated pitches are assigned by timing on a real parsed score: omitting the bar-1 bass G3 on beat 3 (m1-s2-v5-b2_1-p55) marks only that notehead missed while the G3 on beat 2 (m1-s2-v5-b1_1-p55) stays played (D-06, review 03-07 MEDIUM round 2)"
    - "Rung 5 is checked at the piano after rung 4 was approved (review 03-06 HIGH round 2): the user played it clean (no false mark on any of the 41 notes), with one wrong note in bar 2's right hand, one beat late, with the bass G3 on bar 1 beat 3 left out, and wrapped onto two lines with an extra note at the first line's end, plus a reopen and a rung-4 regression pass"
    - "The reopen check can fail visibly (review 03-09 MEDIUM round 3): before closing the tab the user stops the rung-5 session, whose session view carries a red wrong note, a blue G3 and a +1 (known wrong, missed and extra marks), and writes down its heading and the exact sentences for the wrong note, the G3 and the +; rung 5 is the last piece opened, so reopening restores that same session (restore loads the last piece, then its latest session); after reopening, the heading, the three coloured marks and the three sentences must match the written ones exactly; the clean rung-4 regression pass is played only after this comparison"
    - "The phase-close statement claims only what was checked (review 03-07 MEDIUM round 2): the SUMMARY lists, per ROADMAP success criterion and per criterion-6 case, the plan and step where it was checked at the piano or states that it is fixture-only with the reason (exact-tie repeated-pitch ambiguity cannot be produced by hand)"
    - "npm test (exit 0), node scripts/check-run-path.cjs --fixtures, node scripts/check-svg-map.cjs (six OK lines), node scripts/check-capture-roundtrip.cjs (both OK lines), node scripts/check-paint.cjs (thirteen OK lines), node scripts/check-align-perf.cjs (two OK lines) and node scripts/check-live-perf.cjs (two OK lines) all pass, run fresh before the checkpoint; any false mark on rung 5 routes to /gsd-plan-phase 3 --gaps (VRFY-01)"
  artifacts:
    - path: "test/align-ladder.test.cjs"
      provides: "Ladder regression complete for rungs 1-5, with hand-anchored rung-5 interpolation times, 60 BPM and the repeated bass G3"
      contains: "05-yanni-4-measures"
    - path: "fixtures/README.md"
      provides: "Phase 3 rung-5 at-the-piano checklist"
      contains: "Phase 3 - rung 5 at the piano"
  key_links:
    - from: "test/align-ladder.test.cjs"
      to: "src/align.js"
      via: "Align.alignPass on the OSMD-parsed rung-5 model; expected times read from PassResult.slots by noteId and compared with hand-computed values"
      pattern: "3750"
    - from: "fixtures/README.md"
      to: "src/aggregate.js"
      via: "the checklist quotes the exact detail-panel sentence templates, including 'G3, bar 1 beat 3: missed'"
      pattern: "bar 1 beat 3: missed"
  prohibitions:
    - requirement_id: ANLZ-03
      category: transparency
      statement: "MUST NOT generate expected times for a ladder performance with Align.buildSlots or Align.expectedTime; the oracle is independent of the code under test (review 03-05 MEDIUM round 1)"
    - requirement_id: ANLZ-01
      category: values
      statement: "MUST NOT count this phase done on automated output alone: the at-the-piano checkpoint is blocking-human and never auto-approved (VRFY-01)"
    - requirement_id: ANLZ-01
      category: transparency
      statement: "MUST NOT state in the phase-close SUMMARY that a rule was checked at the piano unless a recorded checkpoint response covers it; fixture-only cases are named as such (review 03-07 MEDIUM round 2)"
    - requirement_id: AGGR-01
      category: transparency
      statement: "MUST NOT edit a fixture's expected labels to match a piano observation without first changing the rule in docs/analysis-rules.md and re-deriving the expectation by hand (D-21)"
---

## Phase Goal

**As a** pianist drilling right-hand C D E F G alone at the FP-60X, **I want to** see wrong, missed and extra notes painted on the notation after each pass, with honest counts across the passes at one tempo, **so that** I can tell a recurring mistake from a one-off slip and trust what the score shows me.

<objective>
Rung 5, the four real bars, after the user approved rung 4 (03-08), then the phase close. The steps:

1. Complete the OSMD-parsed ladder regression with rung 5: interpolated eighth-note times against hand-computed anchors, 60 BPM, and repeated bass pitches assigned by timing.
2. Write the rung-5 checklist.
3. Stop for the user at the FP-60X.
4. Close the phase with a statement that names, criterion by criterion, where each claim was checked.

Round 2 (58daf59): this plan exists because rungs 3-5 were built before the next piano check (review 03-06 HIGH). It carries the rung-5 parts of the former 03-06 Task 3 and the former 03-07, adds the repeated-pitch piano probe, and scopes the acceptance claim to recorded evidence (review 03-07 MEDIUM).

Round 3 (2098322): the reopen check now reopens a session with known wrong, missed and extra marks and compares the heading and sentences word for word; the clean rung-4 regression runs separately afterwards. The gate adds the live-callback budget with its chord session.

Purpose: ROADMAP Phase 3 success criteria 1 and 4 on rung 5, the hardware confirmation of the late-entry reading on a longer passage and of the glyph placement across a line break, and an honest phase close.

Output: rung-5 ladder cases, the rung-5 checklist, gates green, the user's response and the criterion-by-criterion evidence table in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-what-you-actually-played/03-CONTEXT.md
@.planning/phases/03-what-you-actually-played/03-05-SUMMARY.md
@.planning/phases/03-what-you-actually-played/03-06-SUMMARY.md
@.planning/phases/03-what-you-actually-played/03-07-SUMMARY.md
@.planning/phases/03-what-you-actually-played/03-08-SUMMARY.md
@docs/analysis-rules.md
@test/align-ladder.test.cjs
@test/score-model.test.cjs
@fixtures/README.md
@src/aggregate.js

<interfaces>
- test/align-ladder.test.cjs from 03-06..03-08: RUNGS array, clicksFor, oracleTime, onsetGroups, perfectPerformance, withStrayAtOrigin, passFor, cases (a)-(e)
- Rung 5 (test/score-model.test.cjs EXPECTED_RUNG_5_IDS): 3/4, four bars (measure starts 0, 3, 6, 9), 41 notes, eighth notes at onset 1/2, 3/2 and 5/2 within bars, e.g. m1-s2-v5-b5_2-p50, m2-s1-v1-b5_2-p74, m3-s1-v1-b5_2-p78, m4-s1-v1-b0_1-p76; bar-1 bass G2 D3 G3 B3 G3 D3 with G3 at m1-s2-v5-b1_1-p55 (beat 2) and m1-s2-v5-b2_1-p55 (beat 3)
- PassResult.slots [{ index, noteIds, expectedTime, reached }] (lag-0 expected times)
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "the four real bars parsed by OSMD align against the oracle" — rung 5 in the ladder with interpolation anchors, 60 BPM and the repeated bass G3</name>
  <precondition>03-08-SUMMARY.md records "approved" for the rung-4 checkpoint (ROADMAP: fix false marks at the current rung before expanding)</precondition>
  <files>test/align-ladder.test.cjs, src/align.js, docs/analysis-rules.md</files>
  <read_first>
    - test/align-ladder.test.cjs as left by 03-08 (RUNGS and helpers — call Align.alignPass, never borrow expected-time logic)
    - test/score-model.test.cjs lines 147-218 (EXPECTED_RUNG_5 and the rung-5 test: measure starts 0/3/6/9)
    - fixtures/README.md rung 5 (bar contents)
    - docs/analysis-rules.md "Slots, tokens, readings and the cost model" (linear interpolation between two real clicks for fractional onsets) and D-06
  </read_first>
  <behavior>
    - RUNGS gains fixtures/05-yanni-4-measures.musicxml; cases (a)-(e) pass for it at 120 BPM from 1000 (the late cases: every note played with entryLag 1; the stray note an extra at before:0)
    - Interpolation anchors: on the perfect rung-5 performance at 120 BPM from 1000, PassResult.slots' expectedTime for the slot containing m1-s2-v5-b5_2-p50 is 2250, m2-s1-v1-b5_2-p74 is 3750, m3-s1-v1-b5_2-p78 is 5250 and m4-s1-v1-b0_1-p76 is 5500 (hand-computed: 1000 + (measure start + onset) × 500), the first three strictly between two consecutive click pageTimes; some model onset has den !== 1
    - 60 BPM: the perfect rung-5 performance at 60 BPM from 1000 yields every note played, entryLag 0, extras []
    - Repeated pitch by timing (D-06): the perfect rung-5 performance at 120 BPM with the note for m1-s2-v5-b2_1-p55 removed (end fixed as in case (c)) yields m1-s2-v5-b2_1-p55 missed, m1-s2-v5-b1_1-p55 played, every other note played, no ambiguous notes
  </behavior>
  <action>
Append rung 5 to RUNGS and add the three rung-5 tests from the behavior block to test/align-ladder.test.cjs, with the anchor values written as literals computed by hand (not from the oracle helper). Run. Fix src/align.js only where a case fails; record any tunable change with the case name in the docs/analysis-rules.md tunables table, otherwise the doc is untouched.
  </action>
  <verify>
    <automated>node --test test/align-ladder.test.cjs</automated>
    <fails_when>non-zero exit (node --test exits 1 when any test fails or a file cannot load), or the summary shows a non-zero fail count or a pass count of 0 — the default spec reporter prints these as "ℹ fail N" and "ℹ pass N", never "# fail"</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit (npm test propagates node --test's exit 1 on any failing test), or a summary line "ℹ fail N" with N greater than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '05-yanni-4-measures.musicxml' test/align-ladder.test.cjs` prints at least 1 and `grep -c '04-chords.musicxml' test/align-ladder.test.cjs` prints at least 1
    - `grep -vE '^\s*(/\*|\*|//)' test/align-ladder.test.cjs | grep -cE 'Align\.(buildSlots|expectedTime)'` prints 0 (the oracle is independent; comment lines are excluded from the count)
    - `grep -c 'den !== 1' test/align-ladder.test.cjs` prints at least 1; `grep -c '3750' test/align-ladder.test.cjs` prints at least 1; `grep -c 'm1-s2-v5-b2_1-p55' test/align-ladder.test.cjs` prints at least 1
    - `node --test --test-reporter=tap test/align-ladder.test.cjs` exits 0 and its summary shows `# fail 0` and `# pass N` with N at least 28 (five per rung plus the rung-5 anchors, 60 BPM and the repeated G3; TAP selected explicitly, review 03-05 MEDIUM round 3)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Every rung, parsed by the real parser, aligns cleanly, marks a single injected wrong or missing note exactly, reads a one-beat-late performance as late even after a stray note, rung 5's eighth notes land at hand-computed interpolated times, and a repeated bass pitch is assigned by timing.</done>
</task>

<task type="auto">
  <name>Task 2: Rung-5 gate — every automated check fresh and the rung-5 piano checklist</name>
  <precondition>C:\Program Files\Google\Chrome\Application\chrome.exe exists (or CHROME_EXE is set) and the machine has internet access for the pinned CDN bundles</precondition>
  <files>fixtures/README.md</files>
  <read_first>
    - fixtures/README.md sections "Phase 3 - rung 1 at the piano" to "Phase 3 - rung 4 at the piano" (append after them, do not rewrite)
    - docs/analysis-rules.md "Detail-panel sentences" and "Colours, glyphs and views" (the system-break placement)
    - .planning/phases/03-what-you-actually-played/03-08-SUMMARY.md (anything the user reported on rung 4)
  </read_first>
  <action>
1. Append `## Phase 3 - rung 5 at the piano` to `fixtures/README.md`, plain prose and short lists, no JSON: a two-line reminder (legend, finishing delay, ready rule, the chosen reach rule), Scenario G exactly as written in Task 3 with views and sentences — including the write-it-down table for step (f) (heading, wrong-note sentence, G3 sentence, + sentence, before and after reopening) — and at 60 BPM one beat is 1000 ms and the mark grace 500 ms.
2. Run the gates fresh, in this order, and fix anything red before committing:
   1. `npm test`
   2. `node scripts/check-run-path.cjs --fixtures`
   3. `node scripts/check-svg-map.cjs`
   4. `node scripts/check-capture-roundtrip.cjs`
   5. `node scripts/check-paint.cjs`
   6. `node scripts/check-align-perf.cjs`
   7. `node scripts/check-live-perf.cjs`

   Commit fixtures/README.md.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit (npm test propagates node --test's exit 1 on any failing test), or a summary line "ℹ fail N" with N greater than 0</fails_when>
    <automated>node scripts/check-run-path.cjs --fixtures</automated>
    <fails_when>non-zero exit, any line starting "FAIL:", or the summary line not ending in "0 failed"</fails_when>
    <automated>node scripts/check-svg-map.cjs</automated>
    <fails_when>non-zero exit; any "FAIL fixtures/..." line; fewer than six "OK fixtures/" lines</fails_when>
    <automated>node scripts/check-capture-roundtrip.cjs</automated>
    <fails_when>non-zero exit; output lacks either OK line ("OK capture round-trip: 8 raw events restored unmodified" prefix, "OK capture round-trip: pass split 3/2/1/1 restored after reload mid-session"); any line starting "FAIL capture round-trip"</fails_when>
    <automated>node scripts/check-paint.cjs</automated>
    <fails_when>non-zero exit; any line starting "FAIL paint:"; fewer than thirteen lines starting "OK paint:"</fails_when>
    <automated>node scripts/check-align-perf.cjs</automated>
    <fails_when>non-zero exit; a line starting "FAIL align perf"; fewer than two lines starting "OK align perf" (the monophonic line and the chords line)</fails_when>
    <automated>node scripts/check-live-perf.cjs</automated>
    <fails_when>non-zero exit; a line starting "FAIL live perf:"; fewer than two lines starting "OK live perf:" (the rung-1 session and the rung-4 chord session)</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven commands above exit 0 with their OK lines (`npm test` judged by its exit status)
    - Discriminating reopen (review 03-09 MEDIUM round 3): `grep -c 'Write down' fixtures/README.md` prints at least 1 and `grep -c 'missed in 1 of 5 assessed passes' fixtures/README.md` prints at least 1
    - `grep -c 'Phase 3 - rung 5 at the piano' fixtures/README.md` prints 1; `grep -c 'two lines' fixtures/README.md` prints at least 1; `grep -c 'one beat late' fixtures/README.md` prints at least 3 (rung-1 and rung-5 sections); `grep -c 'G3, bar 1 beat 3: missed' fixtures/README.md` prints 1
    - `git ls-files --error-unmatch fixtures/README.md` exits 0
  </acceptance_criteria>
  <done>All automated gates are green against the full phase, and the checklist tells the user exactly what to play on rung 5, which view to read and what sentence to expect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: At the FP-60X — rung 5 (four real bars): clean, wrong, one beat late, repeated bass G3 left out, wrapped with an extra, a reopen with those marks on screen, then a rung-4 regression pass</name>
  <what-built>The same engine and display on four real 3/4 bars, where eighth-note expected times are interpolated between recorded clicks, repeated pitches are assigned by timing, a late start is read as late on a longer passage, and extra-note glyphs follow the notation across a line break. The ladder regression runs all five rungs through the real parser against an independent oracle; this is the last hardware check of the phase.</what-built>
  <how-to-verify>
fixtures/README.md open at "Phase 3 - rung 5 at the piano". Same ready rule: read after `(finishing)` clears; before the next pass press the mark once more and start on the next accented click.

Scenario G — open fixtures/05-yanni-4-measures.musicxml, 60 BPM (Enter), Start:
(a) Play it cleanly, mark -> session view: no marks on any of the 41 notes (look especially at the eighth notes).
(b) Play it with one deliberate wrong note in bar 2's right hand, mark -> session view: only that notehead red; click it -> the sentence names bar 2 and the pitch you played.
(c) Let the accented click pass and play the whole piece starting one beat late, mark -> pass view of the pass you just played: no coloured noteheads (all played, one beat late).
(d) Play it cleanly except leave out the G3 in bar 1's left hand on beat 3 (the second G3 of the bar), mark -> pass view: only that G3 blue, the G3 on beat 2 black; click it -> `G3, bar 1 beat 3: missed`.
(e) Narrow the browser window until the four bars wrap onto two lines; play once cleanly but add one extra note right after the last note of the first line, mark -> pass view: a `+` just right of that last notehead on the first line, not over the second line.
(f) Marks to carry across a reopen (review 03-09 MEDIUM round 3). Press Stop. Widen the window again. Click the heading for the session view, which now holds five passes with notes: the wrong note from (b) red, the bar-1 beat-3 G3 blue, a red `+1` where (e)'s extra was. Write down, exactly as shown:
   - the heading (`This session at 60 BPM - 5 passes`, unless a pass was labelled restarted or tempo changed);
   - the sentence for the (b) note (`…, bar 2 beat …: wrong in 1 of 5 assessed passes (played … once)`);
   - the G3 sentence (`G3, bar 1 beat 3: missed in 1 of 5 assessed passes`);
   - the sentence for the `+1` (`Extra notes … in 1 of 5 assessed passes (… once)`).
(g) Reopen with those marks. Close the tab and reopen index.html by double-click, without opening any other file first. Rung 5 was the last piece opened, so the app reloads it together with the session you just stopped. Session view: the same heading, the (b) note red, the beat-3 G3 blue, the `+1` in the same place, and the three sentences word for word as written in (f). All black notation, a different pass count, or any changed sentence is a failure.
(h) Regression, only after (g): open fixtures/04-chords.musicxml, Start, both chords clean, mark -> pass view of the pass you just played: no coloured noteheads.
  </how-to-verify>
  <resume-signal>Type "approved" only if all of the following held:
- (a) no eighth note or chord was falsely marked;
- (b) only the wrong note was marked;
- (c) nothing was coloured on the late pass;
- (d) only the beat-3 G3 was blue;
- (e) the `+` stayed on the first line;
- (g) after reopening, the heading, the three coloured marks and the three sentences matched what you wrote down in (f);
- (h) the rung-4 regression pass showed no mark.

Include the (b) and (d) sentences as shown, the (c) and (e) observations, and the (f) heading and three sentences with, for each, whether (g) showed it identically. Otherwise describe the step, what you played and what was shown (for example "rung 5 (c): the late pass showed red notes", "(d): the beat-2 G3 turned blue"). A false mark goes to /gsd-plan-phase 3 --gaps.</resume-signal>
  <read_first>
    - fixtures/README.md section "Phase 3 - rung 5 at the piano" (written by Task 2)
    - docs/analysis-rules.md D-02 late-entry examples, D-06, D-10 and the system-break placement (to judge a reported mismatch against the written rule)
  </read_first>
  <acceptance_criteria>
    - The user's response is recorded verbatim in 03-09-SUMMARY.md and is either "approved" with the (b) and (d) sentences, the (c) and (e) observations, and the (f) heading and three sentences each marked identical or not after (g); or a stop naming the step, what was played and what was shown
    - The reopen evidence row in the evidence table cites (f)/(g) with the recorded sentences. A clean-notation reopen is never cited as proof that marks survive (review 03-09 MEDIUM round 3)
    - 03-09-SUMMARY.md contains an evidence table with one row per ROADMAP Phase 3 success criterion (1-6) and one row per criterion-6 case (missing first note, late entry, repeated pitches, extra note, interrupted pass, restart, ambiguous alignment, tempo change), each naming the plan, task and step whose recorded checkpoint response covers it, or the word `fixture-only` with the fixture names and the reason; no row claims a piano check that no SUMMARY records
    - A mismatch response leads to /gsd-plan-phase 3 --gaps
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

- `test/align-ladder.test.cjs` — rung 5 added to RUNGS (five cases), hand-anchored interpolation times (2250 / 3750 / 5250 / 5500), 60 BPM, the repeated bass G3 by timing
- `fixtures/README.md` — "Phase 3 - rung 5 at the piano" (Scenario G)
- `src/align.js` / `docs/analysis-rules.md` — changed only if a rung-5 case fails (recorded in the tunables table)
- `03-09-SUMMARY.md` — the fresh gate output, the user's rung-5 response verbatim, and the phase evidence table (criterion by criterion, case by case, piano step or `fixture-only`)

## Flagged assumptions (not silently dropped)

- ROADMAP success criterion 4 across rungs 2-5 is closed only by the per-rung checkpoints (03-06 to 03-09). The ladder regression is the automated floor; the user's playing is the proof.
- Exact-tie ambiguity for repeated pitches is fixture-only (`repeated-e-tie.json`), because timing costs compare in thousandths and real hand timing practically never ties; the evidence table says so. Deterministic ambiguity was checked at the piano on rung 4 (03-08 step g).
- Restart without a mark and the within-pass tempo change are rung-independent rules checked at rung 1 (03-05); they are not repeated on rungs 2-5.
- The rung-5 late pass (c) reads identically under both reach clocks (every note is reached), so it confirms the late-entry reading, not the reach decision taken at 03-05.
- The system-break glyph rule is confirmed on real OSMD line wrapping in rung 5 (e); headless coverage is the pure geometry test in 03-04.
- Reopen (review 03-09 MEDIUM round 3): the check relies on restore() loading the last piece opened and then that piece's latest session, so steps (f)-(g) keep rung 5 as the last piece and play the rung-4 regression only afterwards. Automated coverage of the same path is check-paint's reload group (live fills equal replayed fills) and group 8c (marks return after a failed load). The piano step is the proof on the user's real Chrome profile and IndexedDB.

<verification>
- `node --test test/align-ladder.test.cjs` exits 0; the seven gate commands green
- The blocking-human checkpoint answered "approved" with the sentences quoted, or its issues recorded as gaps for `/gsd-plan-phase 3 --gaps`
- The evidence table in 03-09-SUMMARY.md covers every success criterion and criterion-6 case with a recorded source
</verification>

<success_criteria>
ROADMAP Phase 3 success criteria 1-6 are all true, each backed by a named source in the evidence table:
- 1: fixture tests with no DOM (03-01 to 03-09).
- 2: single-pass classification without false marks and with visible unassessed reasons (03-02 to 03-08, piano 03-05 to 03-09).
- 3: marks on the noteheads (03-02, 03-04, every checkpoint).
- 4: clean and wrong passes matched on every rung in ladder order (03-05 to 03-09).
- 5: the 12-pass experiment with the habit distinguishable from the slip (03-05).
- 6: every required fixture case explained by a numbered rule and checked on a relevant piano example before the next rung, or honestly marked fixture-only.

The Phase 4 hand-off is clean:
- D-01 to D-03's origin rule and the reach decision are recorded.
- The late-entry reading is proven at the piano on rungs 1 and 5.
- Per-note deviationMs is recorded from each matched event.
- No timing verdict was claimed.
</success_criteria>

<output>
Create `.planning/phases/03-what-you-actually-played/03-09-SUMMARY.md` when done. Quote:
- the fresh gate output;
- the checkpoint response verbatim, including the (b) and (d) sentences, the (c) and (e) observations, the (f) heading and sentences with their (g) comparison, and any step that did not match;
- the evidence table required by Task 3.
</output>

## Review Dispositions Ledger

### Round 1 — af0835b

<!-- Rows carried verbatim from the former 03-06 and 03-07 ledgers, whose rung-5 work this plan now holds. -->

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-05: performance generator reuses Align.buildSlots / Align.expectedTime | MEDIUM | Plan 03-06, Task 3: independent `oracleTime` from model rationals, a negative grep forbidding the Align helpers in the ladder test, and hand-computed rung-5 anchor times |
| 03-01: late entry untested on multi-note slots and longer passages | HIGH | Plan 03-06, Task 3 case (d): every rung including rung 4 chords and 41-note rung 5 played one beat late must read all played with entryLag 1 |
| 03-04: glyph placement across a system break untested on a real render | MEDIUM | Plan 03-07, Task 2 step 4(d): rung 5 wrapped onto two lines with an extra note at the first line's end |
| 03-01: late entry on longer passages | HIGH | Plan 03-07, Task 2 step 4(c): rung 5 played one beat late |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none carried) | — |

### Round 2 — 58daf59

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-06: rung-by-rung gating only partly fixed (L138@58daf59) | HIGH | This plan is the rung-5 build-then-check step and the phase close, depending on the rung-4 approval (03-08). The Round 1 rows above now point at this plan's Task 1 (independent oracle, anchors, rung-5 late case) and Task 3 steps (c) and (e) (late pass, wrapped glyph), in place of the former 03-06 Task 3 and 03-07 step 4 |
| 03-07: hardware checklists omit relevant probes yet claim full criterion-6 acceptance (L158@58daf59) | MEDIUM | Plan 03-09, Tasks 1 and 3: rung-5 repeated bass G3 by timing at the piano (step d) and in the ladder test. The phase close requires an evidence table naming the piano step or `fixture-only` for every success criterion and criterion-6 case, and a prohibition forbids claiming unrecorded piano checks. Supersedes the former 03-07 success criterion "every required fixture case ... checked at the piano" |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| 03-01: lagged reach vs D-11 (L32@58daf59) | Deferred — awaiting user decision (conflicts with D-11); decided at Plan 03-05 and applied at Plan 03-06. This plan's late rung-5 pass reads the same under both clocks and does not re-decide it |

### Round 3 — 2098322

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| 03-09: the final reopen check uses a fresh clean rung-4 session, so all-black notation cannot distinguish restored marks from missing analysis; restoration loads the last piece, not necessarily the marked rung-5 session (L204@2098322) | MEDIUM | Plan 03-09, Task 3: verified in src/capture-app.js restore() (the last piece, then its latest session). The steps are reordered. (f) stops the rung-5 session, whose session view has known wrong, missed and extra marks, and the user writes down the heading and the three sentences. (g) closes and reopens with rung 5 as the last piece, and the heading, marks and sentences must match word for word. All-black notation or a changed count is a stated failure. (h) runs the clean rung-4 regression separately, afterwards. Resume signal, acceptance criteria and evidence table require the recorded sentences; Task 2's checklist carries the write-down table |
| 03-05: `# fail 0` cannot match the default reporter (L120@2098322) | MEDIUM | Plan 03-09, Tasks 1-2: `<fails_when>` names the exit status and `ℹ` summary; the ladder count uses `--test-reporter=tap`; `npm test` is judged by exit status |
| 03-03 / 03-08: live and chord performance missing from the final gate (L77@2098322, L183@2098322) | MEDIUM | Plan 03-09, Task 2: `node scripts/check-live-perf.cjs` is a gate command, and both perf scripts must print two OK lines (monophonic and chords) |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none for this plan in Round 3) | — |
